# iCloudモードのオフライン利用 — 設計と検証中の範囲

更新日: 2026-09-13

この変更は開発・検証中です。提出済みの1.0.1 (7)には含まれません。
保存先切り替え・オフライン利用のPRはマージ済みですが、実機で見つかった追加の問題を検証しています。

## 利用者の動線

通信確認のためにタイマーや記録を長時間使えなくしないことを目的とします。
iCloudを選択した状態と、その時点で同期できるかどうかを分けます。

| 状況 | 動作 |
| --- | --- |
| 初回で端末に確認済みデータがない | iCloudの確認・取得が必要。非同期の起動確認は30秒で区切り、再試行できる画面にする |
| 以前に利用できた端末データがあり、通信できない | 同じ端末の保存領域を同期なしで開き、タイマー・記録・設定を利用する |
| 通常のiCloud画面を使っている最中に通信が切れた | 表示と端末への記録は続ける。通信の回復後はシステムが同期を再試行する |
| 電波はあるがiCloudへの応答が遅い | 利用済み端末では非同期の起動確認を12秒で区切る。同期用の保存領域をまだ作っておらず、安全に開ける端末データがあればオフライン利用へ進む |
| 同期用の保存領域を作った後に起動確認が時間切れになる | 「iCloudの確認に時間がかかっています」と「オンラインで再試行」を表示する。時間切れを通信断と決めつけず、同じプロセスで同期なしの保存領域へ切り替えない |
| この起動中に同期用の保存領域を一度開き、閉じた後で通信できなくなった | 同期処理の停止を保証できないため、同じファイルを同期なしで開き直さず、アプリの終了・再起動を案内する。通信が戻れば、同じ画面の「オンラインで再試行」から確認できる |
| オフラインで利用中 | 「このiPhoneに保存・同期は待機中」と表示。接続確認だけで同期完了とは表示しない |
| 同期なしで利用している画面から別のアプリへ移り、戻る | 同じ端末データと画面を保持。復帰時に端末の利用条件を再検証し、通信があれば画面を残したまま接続を確認する |
| 同期なしの端末データで起動した後、通信が回復 | 端末の記録を表示したままアカウント・保存先・リセット履歴を確認し、確認できた後に同期を再開する |
| 別端末で保存先を切り替えている／データが置き換わった | 端末の変更を保護して「復旧手順」を表示。閉じれば利用を続けられる。明示的に手順へ進むとオンラインで再確認する。未完了の置き換えは再開不可で、開始前の取消しだけを認める。完了済みの別世代は端末データを失う確認後に再取得する |
| 端末とiCloudのリセット履歴が食い違う | 自動で結合・送信せず、端末での利用継続、設定からのデータ書き出し、サポートを案内する。未対応の履歴を自動修復できるとは表示しない |
| アカウント変更が確認された | オフライン利用の許可を永続的に失効させる。再起動だけでは解除せず、オンラインで本人確認する |
| アカウント状態の通知が届いた／識別の読み取りが一度で揃わなかった | 画面と書き込みを止めて識別をやり直すだけで、利用記録には触れない。別アカウントが確定したときだけ失効させる |
| 保存先切り替え・取消・復旧の途中 | 保存状態の確認を優先。禁止中の置き換えは記録と復旧用コピーを保持して停止する。オフライン起動を迂回路として使わない |

保存先の切り替えはオンラインで行います。オフライン利用は「このiPhoneのみ」へのモード変更では
なく、iCloudモードの同期待ちです。アプリ自体を削除すると未送信の記録は失われます。
通常のDebug／Releaseでは、複数端末間の競合を防ぎきれないため、iCloudを端末データで置き換える操作と
受付済み・別端末の置き換えの復旧再開を一時的に禁止しています。iCloudの内容を使う有効化と、
cloudを残す解除、置き換え開始前の取消しは維持します。[複数端末の安全性](MultiDeviceCloudSafety.md)も
参照してください。

## 実装の境界

- 同期を伴うコンテナは、現在のApple Accountと確認済みの保存先が一致した後に作ります。
  タイムアウトやネットワークの到達性を、この許可へ変換しません。
- 通常起動のcontrol読み取りは、`accountStatus → identity → 対象controlの取得 → identity`の順で
  確認します。対象のサーバー応答を通信確認にも使い、別途zone一覧を取る確認の重複を減らします。
  アカウントとnamespaceの照合、controlの2回取得、記録画面を公開する直前の確認は維持します。
  過去の応答を別の読み取りや起動へ使い回す方式ではありません。
- オフライン利用には、確定済みのcloud選択、過去の正常マウント、完全な保存領域の組、正常な
  スキーマと永続的な利用記録が必要です。未知・破損した情報は削除して作り直しません。
- オフラインのコンテナは同じstore URL・構成名を使い、CloudKitを`.none`にします。
  新しいnamespaceへのコピー・snapshot再インポートで、同期履歴を作り直す方式ではありません。
- 書き込み可能なコンテナを公開する前に、オフラインで利用したことを永続化します。
  次のCloudKitコンテナを作る前に、保存先の世代とリセット履歴を確認します。
- 通常のiCloud利用後も未送信の変更が残り得るため、次の同期用コンテナを作る前の履歴確認は
  オフライン利用フラグにかかわらず、旧版から引き継ぐデータを含むすべての確認済み端末データで行います。
  サーバーの履歴が以前の確認結果から変わっていれば、端末の現在の履歴を読み取り専用で比較します。
  既に端末へ反映済みの正規のリセットまで、古い確認結果だけで停止させないためです。
  読み取りエラーを「履歴なし」に置き換えることはありません。
- 古いオンライン確認の遅延応答が、途中で受けたアカウント変更を取り消さないよう、
  利用記録には比較交換と改訂IDを使います。アカウント変更は通知と明示的なAPIエラーの両方で扱います。
- 利用記録の失効を書けるのは、**保存済みの対応付けと確認済みの識別を実際に突き合わせた結果**だけです。
  `.CKAccountChanged`通知はサインイン・サインアウト、このアプリのiCloud利用のオン/オフ、
  トークン更新、可用性の遷移など、アカウント状態が動いたことだけを伝えるもので、識別情報を持たず
  何とも比較していません。1回の確認の中で識別を2回読んで食い違った場合も、その場の2つの値を
  比べただけで、保存済みの対応付けは見ていません。どちらも画面と書き込みを止めて確認をやり直す
  理由にはなりますが、失効の根拠にはしません。本当に別のアカウントであれば、やり直した確認が
  `accountMismatch`として確実に止めます。
- 失効の取り消しは、上記の理由で書かれた`accountChanged`に限り、**確認済みの識別が保存済みの
  対応付けと一致したとき**に行います。安全なのは、その記録がもともと「別アカウントである」証拠では
  なかったからです。`accountMismatch`は突き合わせの結論そのもの、`noAccount`と`restricted`は
  アカウント状態についての明確な事実なので、いずれも取り消さず、これまでどおり同期用保存領域の
  マウント成功（`recordVerifiedOnline`）でしか解除しません。取り消しても利用記録の由来・世代・
  リセット基準・オフライン利用の有無は変えず、改訂IDだけを更新します。これが必要なのは、
  失効したままだと起動が保存領域の確認で止まり、解除に必要なマウントへ到達できないためです。
- 同期なしで既に公開した画面だけは通常のバックグラウンド移行で保持します。復帰時は保存先・
  利用記録・失効・切り替え処理の有無を再検証します。アカウント変更通知や失効の確認では画面と
  書き込みを停止し、通常の同期用コンテナの退役条件は緩めません。
- 復旧手順の確認は、表示中の端末セッションとアカウントに結び付けた一度限りの操作です。
  古い画面から遅れて届いた操作で、新しい端末セッションを閉じません。手順の確認自体は
  データ置換・削除への同意ではなく、通信だけの失敗では利用中の画面を閉じません。
- 起動処理は個々の通信ではなく、単調時計による一つの絶対期限を共有します。
  期限切れ時は最初に起動の世代を無効化し、遅い成功・失敗が画面や許可を変更することを防ぎます。
  同期的なOSの保存領域作成そのものを途中で中断する機能ではないため、その呼び出しが停止する場合まで
  画面の応答時間を保証するものではありません。通常画面と実通信での待機時間は別途測定します。
- タイムアウトは、まだ生きているコンテナの解放を意味しません。以前の同期処理が残ったまま
  同じファイルをオフラインで開かないため、コンテナの退役とプロセス内の同期履歴も確認します。
  同じプロセスで同期用コンテナを一度でも作った場合、弱参照が解放されていてもオフライン用
  コンテナを作りません。この制約により、オンライン利用後にバックグラウンドへ移り、通信の
  ない状態で戻った場合などは再起動が必要です。すべての復帰が途切れずに使えるとは扱いません。
  同期用コンテナ作成後の時間切れでは、自動オフライン移行によって再起動の案内へ上書きせず、
  オンライン再試行を表示します。解放確認中はボタンを無効にし、利用許可の条件は緩めません。
- 既に同期用コンテナを開いている間の、システムによる自動再接続・送受信は、この起動前確認の
  対象外です。別端末との変更を原子的に排他できるという保証や、すべての未送信データの受領確認とは
  区別します。同期の接続確認・起動成功を、アップロード完了として表示しません。

AppleはSwiftDataの`.none`による自動同期停止を説明しています。
[SwiftDataの同期](https://developer.apple.com/documentation/swiftdata/syncing-model-data-across-a-persons-devices)
また、Core Dataでは同期を無効にする間もpersistent historyを保持する必要があります。
[Apple Frameworks Engineerの説明](https://developer.apple.com/forums/thread/118924)
これだけでSwiftDataの同一プロセス内の退役・再構成まで検証済みとは扱いません。
履歴の比較には、Appleが説明する読み取り専用の`ModelConfiguration`を使います。
[WWDC23: Dive deeper into SwiftData](https://developer.apple.com/videos/play/wwdc2023/10196/)
`allowsSave: false`による利用者データへの保存禁止を、共有メモリや内部管理情報も含むすべての
ファイルのバイト不変、または以前の同期処理の停止保証へ広げて解釈しません。

## 確認結果

2026-09-12以降の記録です。通信確認とタイムアウト案内の修正後の単体・画面検証と、
専用ホストで行った過去の実機試験を区別します。

- 起動期限・遅延応答・オフライン利用記録・旧版の記録の引き継ぎ・履歴確認・保存先構成・
  マウント許可・CloudKit adapterをSimulatorで検証。実際のSQLite保存領域で、
  読み取り専用コンテナから同期元・端末専用データへの保存が両方とも拒否され、再読込後も
  データが変わらないことを含みます。
- 9月12日のFull-07は1,206件中1,200件成功、明示的な実機専用6件skip、失敗0件。
  専用control readerの16件と、期限切れ・安全な移行条件の追加2件を含みます。
  Full-06で失敗した2件は新しいテストの保存先名が安全条件を満たさない問題で、テストのみ修正して
  単体テスト全体を再実行しました。アプリの安全条件は変更していません。
  Release-analyze-09のSimulator arm64／x86_64解析、OSS-current、サイト・掲載文検査、
  スクリプト回帰12件も成功しました。
- 通信確認の修正前に実施したRelease構成の置き換え禁止ポリシー5件もすべて成功。検証コピーでテスト対象だけを限定し、
  アプリ・テストのソースは変更せず、DEBUG定義なしで実行しました。Releaseの全単体テストの合格とは扱いません。
- 通常factoryの置き換え開始・復旧再開の禁止に加え、OS側のパス別名を許容し所有ディレクトリの
  リンクを拒否する4件、端末ごとの手動記録枠4件、途中コピーを保持する取消し15件
  （Runtime 10件、Journal／Cleanup 5件）が成功。保持記録の保存後に終了しても取消しを完了し、
  元データ・途中コピーを削除しないことを含みます。端末内の判定・ファイルと代替通信境界の検証であり、
  実際の2台間の競合やCloudKit配送の検証ではありません。
- 利用制限を含む保存先切り替え8ケース、オフライン7ケース、同期用コンテナ作成後の時間切れ1ケースの
  計16ケースがFull-06の同一runですべて成功。Full-07との差は上記の単体テスト修正だけです。
  通常サイズ・AX5・再試行・自動同期の説明・復旧説明の取消・二重操作・履歴不一致の案内を確認。
  バナーを小型化し、全文をスクロールできる別画面へ移し、閉じる操作も44pt以上で届くことを確認。
  通常アプリの実機動作を、このfixtureの成功だけで検証済みとは扱いません。
  iCloud置き換えの禁止表示と操作不能、使える選択肢への移動、同意の初期化も確認しました。
  既存の確認・復旧画面は前回のUI-07でスクリーンショットも目視しました。新しい時間切れ画面は
  AX5で再試行ボタンの表示・44pt以上の操作・1回の再試行を検証し、ボタンが見える1枚も目視しました。
  この追加画像はスクロール途中で、見出しや説明全文、再試行後の画面をすべて目視した証拠ではありません。
- Developmentの実機で、既存CloudKit storeと同じURLを`.none`で開き、テーマの変更、
  テーマと記録の追加、成果の削除を保存。60秒間保持し、iCloudの全同期元データが変わらないことを確認。
- 次の新しいプロセスで同じURLのCloudKit同期を再開し、全11種類の端末データと全7種類の
  同期元データを比較。追加・変更・削除がすべて反映されたことを確認。
- 前後で両storeのUUIDが維持され、同期元のnative persistent historyにオフラインの変更が
  追加され、再同期後も履歴が維持されていることを確認。
- 実機Developmentで、製品の読み取り専用factoryから既存の全11モデルと最新リセット履歴を読む
  試験が成功。読み取り専用で10秒間保持し、両storeのUUID・native履歴・データ・DB/WAL/SHMの
  バイトと、全7モデルのサーバーデータに変化がないことをその実行で観測しました。
- 専用ホストで同じプロセスの同期あり→同期なし→同期ありも観測。同期なしの63秒間は
  サーバーの全同期元データと同期イベントに変化がなく、再同期後の全データが一致しました。
  これはその実行の観測結果です。過去の同期処理が常に停止済みだというAPI上の保証にはならず、
  製品の同一プロセスでの再構成を許可する根拠にはしていません。

上記のstore構成切り替えの実機確認は、通常の画面を起動しない専用テストホストで行い、ネットワーク接続を
維持しています。別途、通常Hostの実機起動で`verifiedOnline`の利用記録が生成されることを確認しました。
XCTestの本人確認も通過し、修正前の通常HostでHomeとiCloud設定へ到達するオンライン試験が1回成功しました。
ただし、その前後の2回は同期用コンテナ作成後に12秒の期限へ達し、自動オフライン移行が同一プロセスの
安全条件に拒否され、再起動の案内で停止しました。オンライン起動が安定して成功したとは扱いません。

この実機結果を受け、重複する通信確認を減らし、同期用コンテナ作成後の時間切れではオンライン再試行を
明示する修正を加えました。期限やアカウント・世代の確認、同一プロセスの制限を取り除く修正ではありません。
修正後の通常Hostを実機へインストールし、Home・実際のiCloud設定へ進むオンライン試験が1回成功しました。
試験用のアプリ起動フラグは使っていません。1回の成功を、通信不良時の安定性や送信完了の証拠にはしません。
続く通信断試験はMacがロックされて通信条件を設定できず、操作側から中断しました。
「100% Loss」はプロファイルを選択しただけで、Startは一度も実行していません。
この記録時点では、通常画面で実際の通信断・復帰を検証した証拠はありません。

### 9月13日の通常アプリでの通信断試験

MacとiPhoneの本人確認後、Xcodeの100% packet lossを実際に開始し、開始前・有効中・解除後の
状態と時刻を保存しました。通常のDevelopmentアプリを使用し、アプリへのfixture用起動フラグは
注入していません。各失敗後はアプリを終了し、保存領域とSQLiteの付随ファイルを非公開領域へ
コピーしてから読み取り専用で比較しました。

- `4353a12`の通信断中の新規起動はホームとオフライン案内に到達しましたが、案内がメニューに
  重なり、記録画面へ進めませんでした。操作待ちを含む到達計測は約15.6秒で、12秒以内の画面表示を
  証明する測定ではありません。新しい手動記録を作る前に失敗しています。
- 同じアプリでタイマー実行中に通信を遮断すると、30秒以上の進行と一時停止はできました。
  バックグラウンドから戻る際に保存領域の解放確認が失敗し、オンライン再試行の案内へ進めませんでした。
- `c26322f`で、案内に独立した高さを確保し、通常画面と新規・復元の集中／休憩画面へ同じ配置を
  適用しました。保存先操作の登録を画面離脱時に解除する修正、実行できない同一プロセス内の
  オフライン操作を表示・実行の両方で防ぐ修正、端末内の集計をiCloud通信と誤認させない表示も含みます。
- 旧配置を使う実RootのSimulator試験で重なりを再現しました。修正後の通常サイズ・AX5で、
  メニュー、記録、設定、戻る操作、全画面タイマーの一時停止・再起動復元、休憩からの復帰を確認しました。
  `NetworkValidation-Full-01`は1,228件中1,222件成功、明示的な6件skip、失敗0件です。
  単体1,209件と保存先設定UI19件を同じrunで実行しました。Releaseのarm64／x86_64解析は
  エラー・警告0、OSS-current・サイト・掲載文の検査とスクリプト回帰12件も成功しました。
- `c26322f`を実機へ上書きインストールし、前の試験で残った検証用の一時停止タイマーを復元し、
  通常操作で終了する独立した試験は成功しました。ただし、続くタイマー中の通信断と画面復帰は、
  保存領域の解放確認で再び停止しました。画面・単体試験の成功を、実機の復帰修正が完了した証拠にはしません。
- その実機試験後も、既存のテーマ・記録・成果・設定・リセット履歴の全値と、両保存領域のUUIDは
  維持されていました。検証用タイマーの終了・新規開始・一時停止に伴う履歴と、端末内集計には変更が
  あります。接続確認や端末の履歴は、CloudKitが新しい変更を受領した証明ではありません。
- 追加したUIHostingControllerの回帰試験では、空のHostから実際のSwiftDataコンテナを読み込み、
  その表示から得たコールバックを保持すると、画面が消えても`@State`の古い値がコンテナを保持し、
  2秒の解放待ちが失敗することを再現しました。セッションを共有の参照型で保持し、保存先操作には
  セッションIDだけを渡して実行時に照合する修正後は同じ試験が成功しました。古い操作が新セッションを
  使えないこと、受付済みの操作は完了まで保存領域を保持することも含め、関連46件が成功しています。
  実機での再試験は別途必要です。
- `5be981f`の単体試験は1,212件中1,206件成功、明示的な6件skip、失敗0件でした。
  Releaseのarm64／x86_64解析もエラー・警告0でしたが、通常アプリの実機通信断試験では、
  タイマーを一時停止してバックグラウンドから戻る途中にアプリが終了しました。実行ログの
  Store Removed直後に、SwiftData／SwiftUIのQuery更新中のSIGTRAPが記録されています。
  単純なTextを表示するコンテナ解放試験だけでは、実際のQueryと全画面表示の終了順序を
  検証できていませんでした。退避した端末データでは既存の学習記録のIDと全値を維持しており、
  検証用の一時停止タイマーを復元して通常操作で終了する独立した試験も成功しました。
- Queryを使う旧画面が破棄されるまでコンテナだけを保持する環境値を追加しました。
  セッションの失効と旧画面の破棄を分離し、保存先操作のID照合、実コンテナの弱参照による
  次のオープン条件、2秒の解放待ち上限は維持しています。UIKitの全画面表示完了を待ち、
  別contextで保存した直後にセッションを外す追加試験で、Query読み込み中のクラッシュを再現しました。
  同じ条件に環境値の保持だけを加えると成功し、古いHostのコールバックを保持したままでも
  コンテナは既存の2秒の上限内に解放されました。この変更は実機での再検証が必要です。
- `5dca269`では関連7件と全単体1,216件（1,210件成功、6件skip）が成功し、Release解析の
  両アーキテクチャもエラー・警告0でした。実機の通信断中の一時停止・画面復帰では、保存領域の
  解放ログが記録され、前のクラッシュは発生しませんでした。ただし、その後の接続確認が30秒間の
  観測中に再試行の案内へ進まず、試験は失敗しました。既存データのID・全値と保存領域UUIDは保持されています。
- 期限切れで起動世代を失効させた後、保存領域の終了処理が先に終わると、SwiftUIが同じ失効世代で
  新しい接続確認を開始できる問題を追加試験で再現しました。処理の順序を明示的なテスト用の待合せで
  制御した結果であり、実機ログからその順序を直接観測したものではありません。失効のために作った
  世代からの自動起動を禁止すると、同じ条件でも時間切れの案内を維持しました。明示的な再試行は
  次の世代で行います。実機で選ばれた期限・取消・期限切れを区別できるよう、個人情報を含まない
  世代番号と状態のログも追加しています。
- `5dca269`の別の実機試験では、実際の通信断のまま新規起動してオフラインHomeへ到達し、
  手動記録を追加して画面復帰後も表示しました。タイマーの一時停止と再起動後の同じ残り時間の復元も
  確認しましたが、全画面の背面に残るHomeのバナーまでテストが選び、複数一致で失敗しました。
  その失敗以後のタッチ配送は確認できないため、再開・取消・通信復帰の成功とは扱いません。
  通信を戻す前に退避した保存領域には、追加した記録1件とオフライン利用済みの記録がありました。
  操作可能な前面のバナーを1個だけ確認するヘルパーへ修正し、保持したデータから再開する独立試験を
  追加しています。サーバーへの送信完了は、端末への保存とは別に検証が必要です。

- `eaa83fb`の全単体試験は1,219件中1,213件成功、明示的な実機専用6件skip、失敗0件でした。
  寿命・期限の10件を含み、前後の製品・試験・設定211ファイルのハッシュは一致しています。
  Releaseのarm64／x86_64解析はエラー・警告・解析警告0、製品・設定140ファイルは確定コミットと一致しました。
- 同じコミットの通常Developmentアプリを実機へ上書きし、既存の手動記録と一時停止タイマーを
  維持して検証しました。Xcodeで上り・下りとも100%のパケット損失を適用し、別に状態を記録しています。
  通信断のまま新規起動して同じ残り時間を復元し、前面バナー・再開操作の配送・カウント進行・
  取消し・再起動後の取消し維持・手動記録の表示まで成功しました（149.423秒の試験）。
  手動記録の作成自体は前述の`5dca269`の通信断試験で行い、この試験では作り直していません。
- 通信を戻す前に終了済みアプリの保存領域を退避しました。接続を戻した後の通常Home・実iCloud設定・
  手動記録の表示も成功しました（72.962秒の試験）。前後の11モデルで既存行の削除はなく、
  手動記録と関連テーマのID・全モデル項目・両保存領域のUUIDは同一でした。取消しに伴うタイマー履歴の
  追加とSQLite内部の更新番号は別に比較しており、すべてのファイルがバイト不変という主張ではありません。
- オンライン利用中の実タイマーに通信断を適用し、カウント継続、一時停止、バックグラウンドからの復帰、
  時間切れの再試行案内、通信回復後の画面内の再試行、同じ残り時間の復元、取消し後の再起動まで
  成功しました（191.099秒の試験）。この実行では以前のQueryクラッシュと読み込み継続を再現せず、
  安全でない同一プロセス内のオフライン再オープンは引き続き禁止されています。
- 通常画面の検証後、保存領域を開かない専用ホストから製品の厳格なCloudKit読み取りを実行しました。
  前後のアカウント・controlを確認し、元の手動記録とテーマがDevelopmentサーバーに一意に存在することを
  確認しています。別レビューでもサーバーJSONと通信再開前のSQLiteを照合し、ID、epoch、関連付け、
  記録18項目・テーマ10項目、日時のビットまで一致しました。特定の送信トランザクションのACKや、
  全データの不変、別端末との将来の競合不在を証明するものではありません。
- 不成功の試験も保持しています。通信遮断の切り替えが間に合わなかった実行と、通信断中に別実行の
  テストアプリを起動できなかったiOSのDevelopment署名確認エラーは、オフライン動作の成功に数えません。
  また、時間選択メニューが配送済みタップで開かなかった事象を1回観測しました。同一ビルド・同一試験の
  再実行では開きましたが、原因は未確定です。テストの失敗メッセージが未出現要素のidentifierを先に読み、
  待機前に別のエラーを発生させる問題は修正しました。この診断修正をメニュー非表示の原因解消とは扱いません。

今回の候補では通常アプリの通信断・再起動・再接続と、対象記録のDevelopmentサーバーでの存在を確認しました。
修正版の提出はまだ行っていません。Production署名の最終成果物、実アカウント変更、複数実機の競合は別途必要です。

実験用の置き換え・復旧試験で利用した許可を、通常アプリの起動経路が選ぶことはありません。
実アカウント変更、旧公開版からの移行、複数端末での競合、未送信データがある状態でのアプリ自体の削除は
今回の通常アプリの通信断試験では検証していません。検証用ビルド間の上書きやプロセス終了とは区別します。
実データ・アカウント情報・端末識別子・署名・生ログは非公開の検証記録に保持します。
