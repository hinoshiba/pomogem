# PomoGem 宝石体験デザイン ―「磨く一粒、重さの旅」

> **状態：提案（オーナー判断待ち）** ・ 2026-09-24　本書は提案であり、§11 のオーナー判断（D1〜D26）が決まるまで、確定した製品仕様ではありません。
> 2026-09-25：オーナーから判断を委ねられたリードデザイナーが、D4（瓶全体の適応倍率に改訂）・D25（ジェムベッドを採用し「光の堆積」に再調整）・D26（黒い石を艶のない黒曜石に、×N を銅の刻印札に）を決めた（§7.5、§7.9.1、§7.14、§11）。
> 2026-09-25（ラウンド9）：v1.2 の D17（演出の強さ「標準／控えめ」）・D19（データを持たない原石ウィジェット）・D21（Pro の月刻印をまとまり粒の札に常に表示）を実装した（§7.2、§7.6、§8.7、§8.9、§11）。

| 項目 | 内容 |
|---|---|
| 作成日 | 2026-09-24 |
| 対象 | `main` 3246ab6（v1.1.0）。描画層の実装はブランチ `claude/gem-brilliance` で行う |
| 状態 | **提案（オーナー判断待ち）**。設計としては最終稿で、ドラフトに対するレッドチーム指摘22件（high 7・medium 11・low 4）をすべて処理済み（対応表は付録B）。残りは §11 のオーナー判断 |
| 目標画像 | `concept-target.webp`。雰囲気の参考にとどめ、仕様として写し取らない。出所を確かめるまでは、ストア画像にも画像生成の参照にも使わない（D22） |
| 検討の経緯 | 5つのコンセプト案を「行動・継続」「倫理・審査」「実現性・見た目の適合」の3観点で評価して1案に統合し（付録A）、4つの視点（習慣アプリの熟練デザイナー、一般ユーザーの知人、審査・倫理・13歳の保護者、実装エンジニア）のレッドチームで検証した |
| 用語 | 画面の文言では製品用語の「粒」「まとまり粒」を使う。本書で見た目を説明するときは「宝石」と書く |

---

## 0. 結論

1. **「gemを貯めるだけ」は、最初の10日ほどを過ぎると引力が弱くなります。** 宝石そのものが悪いのではありません。**近い目標・戻るきっかけ・目に見える蓄積・一粒ごとの物語**の4つが欠けていることが原因です（§1）。
2. 「依存性」は、**「集中を始める行動に付く、習慣の引力」**（もう一本やりたくなる、また戻りたくなる力）と定義します。連続日数・ガチャ・期間限定・失う演出・ランキングは使いません（§2）。
3. **v1.2「煌めき」で、宝石を目標画像の水準に作り替えます。** 3Dローポリのカット、白いエッジ、加算の光、傾けると光る星を入れ、**1粒目から全開で輝かせます**。宝石の大きさは瓶全体の適応倍率で決めます（D4、決定）。若い瓶では25分の粒が瓶の内側の幅の約1/5（約55pt）の大きな宝石になり、瓶が埋まるにつれて出荷中の大きさへ戻り、融合で体が減るとまた少し大きくなります。重い瓶は今の大きさのままなので、最悪ケースの余白は変わりません（§7.5）。蓋は付けず、銅の首カラーで高級感を出します（D2）。時間の核は v1.2 から共有画像に写します。
4. **見た目の格（カット・光・グリントの数）は、その宝石が含む時間（グラム）だけで決めます。** 粒の個数・1回の長さ・課金では変わりません。Pro の1分タイマーで結晶の格を先取りすることはできません（D8）。
5. **PomoGem の「月」は「ゾウ1頭ぶん（1万時間・約6t）」です。** 道のりは二層で作ります（§5）。
   - **一里塚**：10時間ごとに、時間の核の軌道に◇が1つ灯り、消えずに残ります。10個で「100時間の星」に畳まれます（融合と同じ十進）。1日1本でも24日ごとに必ず来ます。
   - **名所**：1・2・5系列の時間（1時間、2時間、5時間、10時間、20時間、50時間、100時間＝米俵1俵…）に、身近な重さの比較を添えます。核の段（2.5kg・25kg…）とは重ならないようにずらしてあります。
6. **時間の単位ごとに、必ず来る拍を置きます。** 合言葉は「一回は磨く、一日は区切る、一週はかけら、一か月は小瓶、一年は星、生涯は重さ」です。
7. **戻るきっかけは、本人が選ぶものに限ります。** 初回完走カードの「明日もこの時間に？」、if-then のきっかけ、データを持たない原石ウィジェットの3つです。集中中の原石は既定オフ（D9）、休憩明けは「ここで区切る」を常に主ボタンにします（D12）。
8. **v1.2〜v1.5 はスキーマ変更なしで出せます。** 到達は保存せず、毎回既存データから導きます（§9）。
9. **オーナー判断は26件です（§11）。** v1.2 に最優先で必要なのは、D2（蓋）・D3（多色の核）・D4（大きさ）・D5（HUDの位置）・D8（格をグラムで決める）の5件です。ラウンド2で足したジェムベッド（「積み上がりの光」の結晶化）は D25、ラウンド5で足した「カジノ連想の残り（黒い石のまとまりの形と×N の札）」は D26 です。

---

## 1. 診断：「gemを貯めるだけ」は引力が弱いか

### 1.1 率直な答え

**はい、中期（2週間〜数年）の引力は弱いです。** ただし、悪いのは「宝石を貯める」という発想ではありません。貯めること以外の、習慣を回す部品が抜けていることが原因です。

最初の約10日間は、今のままでも引力があります。物理の瓶の手触り、完走すれば必ず一粒落ちる確実さ、2.5kg（最初の時間の核）までの「あと○時間」という明確な目標がそろっているからです。

弱くなるのはその先です。習慣が自動化するまでには18〜254日（中央値66日）かかりますが（[Lally et al. 2010](https://onlinelibrary.wiley.com/doi/10.1002/ejsp.674)）、PomoGem ではこの期間に新しい出来事がほとんど起きません。

### 1.2 弱い理由（影響の大きい順）と、この提案での処方

| # | 欠けているもの | 今の事実 | 起きていること | 処方 |
|---|---|---|---|---|
| 1 | 近い、名前の付いた目標 | 2.5kg の次は 25kg（41時間40分）、その次は 250kg（416時間40分）。1日1本なら、10日目の次は100日目、その次は2.7年目 | 習慣が固まる時期に節目が来ない。巡（2.5kgごと）は、同じ1.2秒の演出と8ptのチップの繰り返し | 一里塚（10時間ごと）と名所（§5） |
| 2 | 戻るきっかけ | ウィジェットはデータを持たない静的なランチャー。通知は既定オフで固定文面。Live Activity はタイマーだけ | 戻るかどうかが本人の記憶に100%依存している。1タップで始められるので「能力」は十分。ボトルネックは「きっかけ」 | 「明日もこの時間に？」、if-then、原石ウィジェット（§4.2） |
| 3 | 目に見える蓄積 | 十進融合と上限128体のため、15回完走しても瓶の中は6体（×10が1個＋粒5個）。40年分でも root は18個 | 3kg の瓶と3t の瓶がほぼ同じに見える | 格が上がるカット、軌道の◇と星、年輪、月の小瓶、年の星（§7.7、§4.3） |
| 4 | 一粒ごとの物語 | 通常の粒はすべて同じ10点の小石形。面の不透明度は0.13／0.055で、ほぼ見えない。1.0ではレアも無効 | 「次の一粒」を楽しみにする理由がない | カットした宝石、内訳チップ（日付・テーマ・時間） |
| 5 | 報酬の瞬間の主役 | 完走カードは細いバー1本と、会計の説明が2行。融合は「瓶の整理」「価値の段階ではない」と説明している | いちばん派手な物理イベントの意味を、文言が打ち消している | 肖像が主役の完走カード、融合の文言（§8.3） |
| 6 | 育つ見た目 | 核はレベル2以降ほとんど変わらない。到達痕は直径5ptの点。到達の演出は1.2秒で消え、画面を見ていない間に越えた節目は祝われない | 1か月目と1年目で、瓶も体験もほぼ同じ | 核の熟達、首カラーの刻線、見逃した節目の再生（§5.6） |
| 7 | 実感できる単位 | 「3.68kg」を比べる相手がない | 数字が増えても意味にならない | 名所の重さの比較（§5.7） |

### 1.3 すでに強い部分（壊さないもの）

- **物理の瓶の手触り**：タップ、傾き、揺らすと粒が動く。音と触覚もある。
- **完走すれば必ず一粒落ちる確実さ**：Reward Receipt がこれを守っている。
- **2.5kg までの明確な残り時間**：最初の10日間の目標として機能している。
- **休んでも減らない、責めない設計**：連続日数も失効もない。これは弱点ではなく差別化の軸です。英国の子ども向けデザインコード（[ICO 基準13](https://ico.org.uk/for-organisations/uk-gdpr-guidance-and-resources/childrens-information/childrens-code-guidance-and-resources/age-appropriate-design-a-code-of-practice-for-online-services/13-nudge-techniques/)）、[EU DSA の未成年者保護ガイドライン](https://digital-strategy.ec.europa.eu/en/library/commission-publishes-guidelines-protection-minors)、[消費者庁のコンプガチャ規制](https://www.caa.go.jp/policies/policy/representation/fair_labeling/faq/card/)と同じ方向を向いています。「急かさないのに戻りたくなる」と言葉にして、製品の強みとして打ち出す価値があります。

### 1.4 処方：報酬を増やすのではなく、5つの部品を足す

1. **近い物差し**：一定間隔の一里塚と、名前の付いた名所（§5）
2. **戻るきっかけ**：本人が選ぶ時刻と if-then（§4.2）
3. **目に見える蓄積**：格が上がるカット、軌道の星、年輪、月の小瓶の棚、年の星（§4.3、§7.7）
4. **期待の瞬間**：完走で原石に色が入り、瓶の口から落ちる（§4.1）
5. **見た目の熟達**：核と結晶が時間（グラム）で育ち、後退しない（§7.7）

見た目の刷新（知人の要望）は、この全部の土台です。輝いていないものは「見届けたい」対象になりません。ただし見た目の新しさはおよそ2週間で薄れます（[Lin et al. 2006](https://link.springer.com/chapter/10.1007/11853565_16)、[Koivisto & Hamari 2014](https://www.sciencedirect.com/science/article/abs/pii/S0747563214001289)）。見た目だけでは中期の引力は作れないので、v1.3 以降の部品と組み合わせます。

---

## 2. 「依存性」の定義と線引き

### 2.1 定義

オーナーの言う「依存性」を、この提案では次のように定義します。

> **集中を始めるという行動に付く、習慣の引力。** 「もう一本やりたくなる」「また戻りたくなる」力。

アプリを開く・眺める・触ることへの強迫は目標にしません。アプリへの依存と習慣は別物です。リマインダーや連続記録に頼った行動は、アプリがないと続かないもろいものになります。一方、本人の文脈（「朝食のあと」など）に結び付いた行動ほど自動化が進みます（[Stawarz et al. 2015](https://dl.acm.org/doi/10.1145/2702123.2702230)、[Renfree et al. 2016](https://dl.acm.org/doi/10.1145/2851581.2892495)、[Wood & Neal 2007](https://dornsife.usc.edu/wendy-wood/wp-content/uploads/sites/183/2023/10/wood.neal_.2007psychrev_a_new_look_at_habits_and_the_interface_between_habits_and_goals.pdf)）。

UI でも宣伝でも「依存」という言葉は使わず、「もう一本」「戻りたくなる」と言います。

### 2.2 採用する引力

| 引力 | PomoGem での形 | 根拠 |
|---|---|---|
| 近い目標 | 10時間ごとの一里塚と、1・2・5系列の名所。区間の前半は積んだ量、半分を過ぎたら残りを表示 | 目標に近づくほど行動が加速する（[Kivetz et al. 2006](https://home.uchicago.edu/ourminsky/Goal-Gradient_Illusionary_Goal_Progress.pdf)）。序盤は積んだ量、終盤は残りを見せたほうが続く（[Koo & Fishbach 2012](https://academic.oup.com/jcr/article-abstract/39/3/493/1822606)） |
| 付与された進捗 | 1本目で最初の名所に必ず届く（質量は水増ししない） | [Nunes & Drèze 2006](https://msbfile03.usc.edu/digitalmeasures/jnunes/intellcont/endowed%20progress%20effect-1.pdf) |
| 切りのいい数 | 1時間・10時間・100時間・1,000時間・1万時間を目盛りに置く | [Pope & Simonsohn 2011](https://journals.sagepub.com/doi/abs/10.1177/0956797610391098)、[Allen et al. 2017](https://pubsonline.informs.org/doi/10.1287/mnsc.2015.2417) |
| 実感できる比較 | 「米俵1俵（60kg）」「ゾウ1頭ぶん」 | 身近な比較は、数字の理解と記憶を大きく改善する（[Barrio et al. 2016](https://dangoldstein.com/papers/Barrio_Goldstein_Hofman_Improving_Comprehension_Numbers_News_CHI16.pdf)） |
| 本人が選ぶきっかけ | 初回完走での「明日もこの時間に？」（既存のリマインダー設定を使う）。if-then（例：「朝食のあと、英語を25分」）を最大3件。どれも既定オフ | 実行意図は、行動を始める確率を中〜大の効果量で高める（[Gollwitzer & Sheeran 2006](https://doi.org/10.1016/S0065-2601(06)38002-1)） |
| 区切りと始まり | 今日はここで区切る／週のかけら／月の小瓶／年の星 | 週・月・年の始まりには意欲的な行動が増える（[Dai et al. 2014](https://pubsonline.informs.org/doi/10.1287/mnsc.2014.1901)） |
| 価値の差がない個体差 | session UUID から決まる、同じ格の4変種と星の位置 | 予期しない報酬は内発的動機を損なわない（[Deci et al. 1999](https://home.ubalt.edu/tmitch/642/articles%20syllabus/Deci%20Koestner%20Ryan%20meta%20IM%20psy%20bull%2099.pdf)）。過程に注目しているとき、不確実さは楽しい（[Shen et al. 2015](https://academic.oup.com/jcr/article-abstract/41/5/1301/2962097)） |
| 見た目の熟達 | 核と結晶が含む時間で育ち、後退しない | 有能感へのフィードバック（[Ryan et al. 2006](https://selfdeterminationtheory.org/SDT/documents/2006_RyanRigbyPrzybylski_MandE.pdf)）。演出は多すぎても逆効果なので上限を置く（[Kao 2020](https://www.sciencedirect.com/science/article/pii/S1875952118300879)） |
| 目に見える蓄積 | 軌道の星、年輪、月の小瓶、年の星 | 一目で分かる表示は、休暇中も行動を支えた（[Consolvo et al. 2008](https://dl.acm.org/doi/10.1145/1357054.1357335)） |
| 休んでも失わない | 何も減らない。空白の日数を出さない | 1回抜けても習慣形成は大きく損なわれない（Lally 2010）。途切れた記録を強調すると、継続が下がる（[Silverman & Barasch 2023](https://knowledge.insead.edu/marketing/consumer-streaks-are-motivating-key-keeping-them-alive)） |

### 2.3 採用しない手法

| 手法 | 採用しない理由 | 根拠・既存の約束 |
|---|---|---|
| 連続日数（streak）、streak の保護 | 短期の継続は上がりうる（[Duolingo の自社報告](https://blog.duolingo.com/how-streaks-keep-duolingo-learners-committed-to-their-language-goals/)）。しかし途切れたあとの再開率を下げる。公開済みの説明「連続日数で警告したりしません」とも矛盾する | Silverman & Barasch 2023、`AppStore/metadata/ja-JP/description.txt` L24、EA:L49,52 |
| 失効・劣化、中断で割れる／枯れる／消える演出 | 損失回避に訴える。「休んだ日も、積み上げた記録は減りません」と矛盾する | `description.txt` L3。罰が逆効果になった研究（[Diefenbach & Müssig 2019](https://www.sciencedirect.com/science/article/abs/pii/S1071581918305135)） |
| 期間限定、暦で入手時期が決まる収集（誕生石12種など） | 取り逃しの不安（FOMO）を生む。未成年も使うアプリでは審査5.6に触れる | EA:L29,231、[App Review Guidelines](https://developer.apple.com/app-store/review/guidelines/) |
| ランキング、他人との比較 | 不安と一気学習を生む。サーバーも Social 機能も持たない方針に反する | [Mogavi et al. 2022](https://arxiv.org/abs/2203.16175)、`age-rating.md` L15,33 |
| ランダムな希少度・ガチャ。レアの再有効化を「煌びやかさ」の手段にすること | 4+ の回答「ランダム報酬なし」と矛盾する。ルートボックスは問題ギャンブルと相関がある | `age-rating.md` L36-37、[Zendle & Cairns 2018](https://journals.plos.org/plosone/article?id=10.1371%2Fjournal.pone.0206767) |
| ニアミス（惜しかった演出） | ギャンブルを真似た手法。決定論的な「あと1本」の事実表示とは別物 | EA:L116-117 |
| 図鑑のコンプリート、未発見のシルエット | 枠を埋めるために、本人に合わない長さや時間帯を選ばせる。セットを揃えたい動機は、劣った選択肢でも選ばせる | [Evers et al. 2014](https://www.apa.org/pubs/journals/features/xge-a0033343.pdf) |
| 次のタイマーの自動開始、「もう一本」を主ボタンにすること | 自律性と休憩の方針に反する | EA:L48,565、ICO 基準13 |
| 課金で進行・輝き・見た目の格を買える（先取りできる）こと | 「課金で報酬は変わらない」という約束に反する | EA:L242,367、`AppStore/iap-review-notes.md` |
| 共有すると何かがもらえる | 審査ガイドライン3.2.2、ステルスマーケティング告示に触れる | EA:L316-343 |
| 通知で到達や残りを知らせる、ログインボーナス | 引力の向き先が、集中ではなくアプリの起動になる | EA:L19,56 |

### 2.4 線を引く6つの原則

1. 引力は**集中した時間**からだけ生まれる。起動や滞在には報酬を出さない。
2. **終わりどころ**がある。休憩と「区切る」が主ボタンで、次のタイマーは自動で始めない。
3. 休んでも、中断しても、**何も失わない**。
4. **不確実さを主な仕組みにしない**。驚きは、事実から決まる価値の差がない違いで作る。
5. **お金と交換しない**。見た目の格も含む。
6. **本人が強さを下げられる**（演出の強さ「控えめ」、Reduce Motion、通知オフ、次の目盛りの非表示）。

### 2.5 グレーゾーンと、この版での位置

| 論点 | この版での位置 | 判断 |
|---|---|---|
| 集中中の原石 | 既定オフ。表示しても静止した完成形で、途中の進捗も中断時の消滅もない | D9 |
| Live Activity の原石 | 静止画。「+Ng」は記録の保存後だけ | D10 |
| 「あと1本」 | 開始ボタンには付けない。HUD の目盛りの行に事実として出すだけ。21時〜翌4時と、休憩を2回続けて飛ばした後は出さない | D11 |
| 休憩明けの「もう一粒」 | 常に「ここで区切る」が主ボタン、「もう一粒」は文字リンク | D12 |
| 一粒ごとの個体差 | UUID から決まる同格の4変種と星の位置だけ。長さで格を変えない | D14 |
| 融合の意味づけ | 「10粒ぶんの時間が、ひとつの結晶に。重さはそのまま」。演出の強さは含む時間で決まる | D8 |
| 月の小瓶の蓋 | 小瓶だけに蓋、色はその月にいちばん積んだテーマ | D20 |
| データを表示するウィジェット | 採用しない。データを持たない原石ウィジェットだけ | D19 |

---

## 3. コンセプト全体像

### 3.1 コアファンタジー

> **完走の瞬間、無色の原石に自分のテーマの色が入り、瓶の口から落ちる。瓶の奥では時間の核が育ち、10時間ごとにその軌道へ一里塚の◇がひとつ灯る。重さはりんご、キャベツ、スイカ、米俵と育ち、いつか1万時間でゾウ1頭ぶんに届く。月ごとに小瓶に封をして棚に並べ、一年ごとに星がひとつ昇る。休んでも、一粒も減らない。**

合言葉は「**一回は磨く、一日は区切る、一週はかけら、一か月は小瓶、一年は星、生涯は重さ**」です。

中心はあくまで瓶です。部屋や図鑑を主役にはしません。瓶と時間の核を、いちばん美しく、いちばん意味のある場所にします。

### 3.2 推奨スタック：時間の単位ごとに、必ず来る拍を1つずつ

| 時間の単位 | 拍 | 起きること | 担う役割 | 出どころ |
|---|---|---|---|---|
| 一回（25分） | 磨く | 完走すると原石に色が入り、瓶の口から落ちる。今週のかけらが1つ増える | 期待と手応え | 案5「習慣ループ」 |
| 一日 | 区切る | 休憩明けに「ここで区切る」（主）と「もう一粒」（文字リンク）。区切ると今日の粒がひと巡り光る | 終わりどころと、翌日の再訪 | 案5 |
| 2.5kgごと（4時間10分） | 巡 | 玉ボケが満ち、底ガラスに年輪が1本沈む | 名前のない短い周期 | 既存＋案1 |
| 10時間ごと | 一里塚 | 核の軌道に◇が1つ灯り、消えずに残る。10個で100時間の星に畳まれる | 名前（時間数）の付いた一定間隔の目標 | レッドチーム指摘で追加 |
| 1・2・5系列の時間 | 名所 | 身近な重さの比較カード（例：100時間＝米俵1俵） | 意味と実感 | 案1「時間の天秤」 |
| 一週 | かけら | 今週積んだ250gごとに、種の周りへかけらが1つ増える（完成形なし） | 毎回かならず動く進捗 | 案1・案2 |
| 一か月 | 小瓶 | 先月の時間を小瓶に封じ、棚に並べる | 目に見える蓄積 | 案4「瓶が育つ世界」 |
| 一年 | 星 | その年の重さが星になって昇る | 長期の区切りと振り返り | 案4 |
| 生涯 | 重さと核 | 名所の旅と、時間の核の進化（2.5kg・25kg・250kg…） | 意味と方向 | 案1＋案3「研ぎの瓶」 |
| いつでも | きっかけ | 本人が選んだ時刻・if-then・「おかえり」 | 戻ってくる力 | 案5 |

等比の目盛り（名所・核の段）だけだと、同じペースで続けるほど間隔が倍々に開きます。そこで、**一里塚・巡・週・月・年という一定間隔の拍**で間を埋めます。一里塚は「名前（時間数）がある」一定間隔の目標で、1日1本でも24日を超えて次の目標が遠のくことはありません（§5.4）。

### 3.3 採用しなかったもの（要約）

- **鉱物図鑑のカット見本盤とシルエット**：2〜6週で埋まって失速する。枠を埋めるために長さや時間帯を選ばせてしまう。
- **1粒目を原石にして段階的に輝かせる設計**：知人の要望と初日の印象を犠牲にする。
- **長さで面の細かさを変えること**：細かいほど良く見えるので序列になり、休憩を飛ばして長く積む誘因になる。Screen Time の10分粒が永久に「粗い」ままになる。
- **時間帯で見た目を変えること**：夜の集中を誘う。端末のタイムゾーンによって見た目がずれる。
- **場所の5段階（窓辺から星空まで）**：制作費が最も大きく、iPhone 12 mini ではほとんど見えない。
- **動物の星座、天秤の儀式**：比喩と見た目の文法が増えすぎる。星は「年の星」と「100時間の星」に限る。

各案の評価は付録Aにまとめました。

---

## 4. ループ設計

### 4.1 micro：1回の集中（25分＋休憩）

| 段階 | 起きること | ガード |
|---|---|---|
| 開始前 | Home の瓶を傾けると、宝石の決まった面が順に光る。開始ボタンは「25分、集中する」「英語・完走で+250g」（既存）。開始ボタンには何も足さない | 「あと1本」は HUD の目盛りの行にだけ事実として出す（D11、条件は §5.5） |
| 集中中 | 既定では今と同じ静かな画面。設定「集中中に原石を表示」をオンにした人だけ、リング中央に**完成形の無色の原石**を静止表示する（D9） | 途中の面の進捗は出さない。一時停止中も変化させない。中断しても消滅アニメーションは出さず、そのまま画面から外す。既存の文言「この回の粒は積まれません。これまでの瓶はそのままです。」は残す |
| 完走 | 原石の中心から、テーマの色が面ごとに注がれる（500ms以内）→ 白い一閃を1回 → 瓶の口から落ちて着地 → ハローが一度強まり、頂点に星 | 数字の数え上げはしない（「+250g」は静的に出す）。Reward Receipt により、同じ完走の演出は再生しない |
| 完走カード | 主役は「この一粒の肖像」と「+250g」。次の一里塚・名所への進みを1行、今週のかけらが1つ増える小さな動き。**初回完走のときだけ**、任意の1行「明日もこの時間に？」 | 会計の説明は「詳しく」に移す。主ボタンは「5分休む」、副ボタンは「閉じる」。共有チップは5秒後（既存） |
| 節目 | 一里塚・名所・核の段を越えた回だけ、演出を1つ出す（§7.10） | 大きな演出は完走1回につき1つまで。重なったら1つの振り付けにまとめ、合計5秒以内、タップでスキップ可 |
| 休憩 | 作ったばかりの粒を大きく表示し、傾けると面が光る（「光にかざす」）。「瓶の粒は、そのまま待っています。」（既存）は残す | 次のタイマーは自動で始めない |
| 休憩明け | 主ボタン「ここで区切る」、その下に文字リンク「もう一粒磨く」（D12） | どの時刻でもこの順序。夜間に「もう一粒」をさらに弱める必要はない（最初から主ではない） |
| ここで区切る | Home に戻ると今日の粒がひと巡り光り、「今日 4粒・1.00kg。おつかれさまでした」と出る | 初めて区切ったときだけ、if-then のきっかけ作りを1回提案する（任意。断ったら二度と出さない。初回完走で「明日もこの時間に？」を設定済みなら出さない） |

**文言の衝突を避けます（D13）。** 既存の中断ボタンとその確認アラートは「今日はここまで」です（`Constants.UIStrings.giveUp`、`FocusView.swift:759-763`。押すと「この回の粒は積まれません」）。同じ文言を「満足して一日を締める」ボタンにも使うと、子どもは休憩明けのボタンを怖がるか、中断ボタンを気軽に押すかのどちらかになります。そこで、**新しい区切りのボタンは「ここで区切る」**とし、既存の中断の文言は変えません。既存の文言は責めない言い方として定着しており、UI テスト約20箇所と監査文書が参照しているためです。中断を「この回をやめる」に改名する案は、D13 の選択肢として残します。

1回の集中は、**期待（色が入る）→ 解決（瓶に落ちる）→ 意味（一里塚・名所が進む）→ 終わりどころ（休憩・区切る）**の流れで完結します。

### 4.2 meso：1日〜1週間

- **巡（2.5kgごと）**
  - 間隔は、1日1本なら10日、2本なら5日、4本なら2.5日。
  - 瓶の奥の玉ボケが下から満ちていき、満杯になると光が底ガラスに年輪として沈む。光が抜けて消えるようには見せない。
  - HUD の「N巡」ピルが1つ増える。大きく祝うのは、その日最初の満杯だけ（EA:L287-290 の仮説どおり）。
- **今週のかけら（週の結晶の形を変える、D16）**
  - 今週積んだ実測グラム250gごとに、中心の小さな種の周りへかけらが1つ増える。25分の完走1回で必ず1つ。
  - **完成形も正規の個数も持たない。** かけらは黄金角の渦で外へ並び、数が増えると種の内側の光が深まる（上限あり）。1本/日の人の7個も、4本/日の人の28個も、「欠けた結晶」ではなく「育っている群れ」に見える。
  - 本人が任意の「週の目安」を設定したときだけ、その目安を外周の細い弧で示す。届かなくても失敗として表示しない。
  - 12面の `ProgressCrystalGlyph` は Overview の装飾にだけ使い、週のノルマには使わない。
  - 週の区切り（既存の `completionMetrics` と同じ暦）で、先週のかけらは棚へ移る。週の最初の起動で一度だけ見せる。未達、前週比、「あと○個」は出さない。
- **戻るきっかけ（すべて既定オフ・本人が選ぶ）**
  - **「明日もこの時間に？」**：初回の完走カードに任意の1行。タップしたときだけ、既存のリマインダー設定（同期している Prefs の既存項目）に、今の時刻を初期値として登録する。登録前に時刻を変えられる。通知は1日1回まで、テーマ名なし。既存のリマインダーがあれば出さない。
  - **if-then**：「〈いつ／何のあと〉に〈テーマ〉を〈何分〉」の形で最大3件（端末ローカル）。時刻型は既存のリマインダー予約に合流させる。出来事型（「朝食のあと」など）は Home の瓶の下に1行出す。実行しなかった記録は取らない。
  - **原石ウィジェット（D19）**：データを持たない静止画のウィジェット。ホーム画面で「次に磨く原石」を見せ、タップでアプリの開始画面へ。
- **おかえり**
  - 最後の完走から7日以上空いたら「おかえりなさい。瓶はそのままです」と出す。
  - 最後の粒が一度光り、その日付を添える。いつものテーマ・いちばん短い時間で始められる開始チップを出す。
  - 空白の日数は出さない。

### 4.3 macro：月〜年〜生涯

- **重さの旅**：一里塚（10時間ごと）と名所（1・2・5系列）、1万時間のゾウと、その先の群れ（§5）。
- **時間の核の熟達**：2.5kg で誕生、25kg でガードルの分散光が強まる、250kg で2本目の軌道、2.5t で星冠、25t で3本目の軌道。含む時間だけで進み、後退しない（§7.7）。
- **月の小瓶**
  - 新しい月の最初の完走のあと、先月の時間を小瓶に封じて棚に並べる。初回は4秒以内、2回目以降は1.5秒、どちらもスキップ可。
  - 活動した月だけを詰めて並べ、棚に穴を作らない。
  - 蓋の色は、その月にいちばん積んだテーマの色（D20）。
  - タップすると既存の「月の瓶」（Wrapped）が開く。
- **年の星**
  - 新しい年の最初の完走のあと、12月の小瓶の封に続けて、その年の重さが星になって昇る。
  - 明るさは年の重さの対数。星は年の順に細い線で結ばれ、一生で約40個の自分の星座になる。
- **はじまりと今**：1粒目の肖像と、いまの時間の核を並べたカード。節目や一年の区切りで、任意に共有できる。
- **積み上がり計画**：実績とつなぎ、「このペースなら、次の名所は○月ごろ」を計画画面の中でだけ見せる。Home には到達予測を出さない。

### 4.4 ループに「止まれる場所」を作る

- 1回ごとに完結し、主ボタンは休憩。
- 1日ごとに「ここで区切る」で、満足して閉じられる。
- 週・月・年は、区切りで「しまう」儀式があり、次は新しい器から始まる（フレッシュスタート、Dai et al. 2014）。
- 長い空白のあとも、一里塚・名所・核・棚・星はすべてそのまま残る。

---

## 5. マイルストーン設計：PomoGem の「月」

### 5.1 「本の高さが月に届く」にあたるもの

PomoGem はすでに物理量を持っています（質量、1分＝10g）。高さや距離に換算しようとすると、恣意的な係数と第二の通貨が要り、正直さの不変条件を壊します。そこで、**重さのまま、身近な実物と比べる**旅にします。

- **PomoGem の「月」は「ゾウ1頭ぶん」（1万時間＝6t、アフリカゾウのオス1頭ほど）**です。遠いけれど、人生の中で届く距離です（1日4本で約16年、1日10時間で約2年9か月）。
  - 名前はゾウを主にし、「1万時間」は副表示にします。「1万時間の法則＝熟達の保証」と読まれないよう、カードの「詳しく」に「時間は学力の証明ではありません」を常に置きます（[Macnamara et al. 2014](https://journals.sagepub.com/doi/abs/10.1177/0956797614535810)、EA:L50）。
- その先も、1万時間ごとにゾウが1頭ずつ増える「群れ」が続きます。40年×1日10時間（87.66t、約14.6万時間）でもゾウ14頭で、旅は途切れません。
- 参考：オーナーの記憶にある読書アプリは特定できませんでした。最も近い実例は、読んだ本を高さとして積む [Book Towers](https://apps.apple.com/us/app/book-towers-reading-tracker/id1472538417) と、歩数で東海道を進む[「徒歩でゆく～東海道五十三次」](https://apps.apple.com/jp/app/id1170912296)です。アプリ名が分かれば再調査します。

### 5.2 二層の構造と、核の段との役割分担

| 層 | 間隔 | 見える場所 | 役割 |
|---|---|---|---|
| **一里塚** | 10時間（6kg）ごと。一定 | 時間の核の軌道の◇。10個で外側の軌道に「100時間の星」1つ、10個の星で「1,000時間の冠」1つ、10個の冠で「ゾウ」1頭に畳まれる | 名前（「30時間」など）のある一定間隔の目標。消えずに核の一部として残る |
| **名所** | 1・2・5系列の時間（等比） | 名所カード（完走カード内、Overview の「旅」） | 意味と実感（重さの比較） |
| **核の段** | 2.5kg × 10ⁿ（既存） | 時間の核そのものの形（面・分散光・軌道の本数）と、首カラーの刻線 | 長期の熟達。核の演出だけで祝う |

- 一里塚は十進で畳むので、40年×1日10時間でも、◇ 0個・星1個・冠6個・ゾウ14頭で収まります（上限あり）。
- 名所は1・2・5系列の時間に置き、**核の段（4時間10分、41時間40分、416時間40分、4,166時間40分）と重ならないようにずらしています**。25分中心の人では、核の段は十進融合（×10・×100…）と同じ完走で起きるので、名所まで同じ点に集めると報酬がそこに集中し、その前後が空くためです。
- 10時間の倍数にある名所（10時間、20時間、50時間、100時間…）は、その一里塚に名前が付いたものとして扱います。◇が灯る演出と名所カードを1つの振り付けにまとめます。

### 5.3 目盛り一覧

到達の目安は、25分の完走を1日1本／2本／4本続けた場合です。比較の重さは候補値で、v1.3 までに出典台帳で確定します（§5.7）。

| 種別 | 閾値（時間・重さ） | 名前 | 比較（候補） | 1本/日 | 2本/日 | 4本/日 | 演出 |
|---|---|---|---|---|---|---|---|
| 名所 | 25分・250g | 最初の一粒 | りんご1個ほど | 1日目 | 1日目 | 1日目 | 小（既存の初期スポットライト） |
| 名所 | 1時間・600g | はじめての1時間 | ハリネズミ1匹ほど | 3日目 | 2日目 | 1日目 | 中 |
| 名所 | 2時間・1.2kg | 2時間 | キャベツ1玉ほど | 5日目 | 3日目 | 2日目 | 小 |
| 核 | 4時間10分・2.5kg | 時間の核（誕生） | ―（核の演出） | 10日目 | 5日目 | 3日目 | 大 |
| 名所 | 5時間・3kg | 5時間 | 小玉スイカ1玉ほど | 12日目 | 6日目 | 3日目 | 小 |
| 名所＋一里塚1 | 10時間・6kg | 10時間 | 大玉スイカ1玉ほど | 24日目 | 12日目 | 6日目 | 中（最初の◇） |
| 名所＋一里塚2 | 20時間・12kg | 20時間 | コーギー1頭ほど | 48日目 | 24日目 | 12日目 | 中 |
| 一里塚3・4 | 30時間・40時間 | 30時間／40時間 | ― | 72日目／96日目 | 36日目／48日目 | 18日目／24日目 | 小（◇） |
| 核 | 41時間40分・25kg | 時間の核・二段目 | ―（核の演出） | 100日目 | 50日目 | 25日目 | 大 |
| 名所＋一里塚5 | 50時間・30kg | 50時間 | コウテイペンギン1羽ほど | 120日目 | 60日目 | 30日目 | 中 |
| 名所＋一里塚10 | 100時間・60kg | 100時間 | 米俵1俵（60kg） | 約8か月 | 約4か月 | 約2か月 | 中（最初の星、共有カード付き。旅の画面に遠い目印のゾウが現れる） |
| 名所 | 200時間・120kg | 200時間 | ジャイアントパンダ1頭ほど | 約1年4か月 | 約8か月 | 約4か月 | 中 |
| 核 | 416時間40分・250kg | 時間の核・三段目 | ―（核の演出） | 約2年9か月 | 約1年4か月 | 約8か月 | 大 |
| 名所 | 500時間・300kg | 500時間 | ヒグマ（オス）1頭ほど | 約3年3か月 | 約1年8か月 | 約10か月 | 中 |
| 名所 | 1,000時間・600kg | 1,000時間 | 乳牛1頭ほど | 約6年7か月 | 約3年3か月 | 約1年8か月 | 中（最初の冠） |
| 名所 | 2,000時間・1.2t | 2,000時間 | キリン1頭ほど | 約13年2か月 | 約6年7か月 | 約3年3か月 | 中 |
| 核 | 4,166時間40分・2.5t | 時間の核・四段目（星冠） | ―（核の演出） | 約27年5か月 | 約13年8か月 | 約6年10か月 | 大 |
| 名所 | 5,000時間・3t | 5,000時間 | シロサイ1頭ほど | 約32年10か月 | 約16年5か月 | 約8年3か月 | 中 |
| 名所 | 1万時間・6t | **ゾウ1頭ぶん**（1万時間） | アフリカゾウ（オス）1頭ほど | 約66年 | 約32年10か月 | 約16年5か月 | 特大（一度だけのズームアウト） |
| 名所 | 以降1万時間ごと | ゾウの群れ（2頭、3頭…） | ゾウ○頭ぶん | ― | 2頭：約66年 | 2頭：約32年10か月 | 中 |
| 名所 | 5万時間・30t | ゾウ5頭ぶん | ザトウクジラ1頭ほど（副表示） | ― | ― | ― | 中（1日10時間なら約13年8か月） |
| 核 | 41,666時間40分・25t | 時間の核・五段目 | ―（核の演出） | ― | ― | ― | 大（1日10時間なら約11年5か月） |

一里塚は表の途中を省略しています。10時間の倍数ごとにすべて来ます。

演出の大きさは4段階です。

- **小**：完走カードの中の1行チップ（例：「2時間。キャベツ1玉ほどの重さになりました」）。一里塚だけのときは「◇ 30時間」と◇が灯る動き。
- **中**：名所カード。そのシルエットの中に、自分の宝石の面が満ちていく（1.2〜2.4秒）
- **大**：核の誕生・段の上昇（合計5秒以内）
- **特大**：ゾウ1頭ぶんだけ。瓶から棚、棚から地平へ、一度だけカメラが引く（[Powers of Ten](https://eamesfoundation.org/work/powers-of-ten-and-the-relative-size-of-things-in-the-universe/) の手法、3秒以内）

### 5.4 空白の最大日数（受け入れ基準）

**受け入れ基準：1日1本（25分）以上のペースなら、名前の付いた次の目標（一里塚または名所）までの間隔が、どの時点でも30日を超えないこと。**

| ペース | 名前の付いた目標の最大間隔 | 巡（年輪が残る） | 今週のかけら |
|---|---|---|---|
| 1本/日 | 24日（一里塚） | 10日ごと | 毎回 |
| 2本/日 | 12日 | 5日ごと | 毎回 |
| 4本/日 | 6日 | 2.5日ごと | 毎回 |
| 週3本（参考） | 約8週 | 約23日ごと | 毎回 |

- 最初の10時間までは、名所が1時間・2時間・5時間・10時間と来るので、1本/日でも最大12日です。
- 週3本の軽い利用者は一里塚まで約8週かかりますが、その間も巡（約3週ごと、底ガラスに消えない年輪が残る）と今週のかけら（毎回）が必ず進みます。一里塚の間隔をこれ以上縮めると、巡（4時間10分）と区別が付かなくなるため、10時間で固定します。
- 同じ週に大きな演出（核の段・特大）が2つ以上重なる回数：25分中心の人で、ドラフトの4か所から0か所になりました（核の段と名所をずらしたため）。

### 5.5 次の目標の見せ方

- **Home に出すのは1行だけ**です。
  - 次の名所まで10時間以内なら名所を出す：「つぎの名所　米俵1俵・100時間」
  - それ以外は一里塚を出す：「つぎの一里塚　80時間」
- **区間の半分までは積んだ量、半分を過ぎたら残り**：区間は「直前の一里塚または名所」から「表示中の目標」まで。前半は「+3時間10分 積みました」、後半は「あと4時間50分」（Koo & Fishbach 2012）。
- **「あと1本」（D11）**：残りが、いま選んでいる時間以下のときだけ、同じ行に「・あと1本」を添える。開始ボタンには付けない。次のどれかに当てはまるときは出さない。
  - 21時〜翌4時（日付の切り替えと同じ4時まで）
  - 休憩を飛ばした完走が2回続いた後（その日のうちは出さない）
  - その日すでに4時間以上積んだ
  - 1kgごとの長い休憩の提案の直後
- **10倍先の「核まであと35時間32分」は撤去します（D6）。** 進捗15%の時点で10倍先の残りを見せるのは逆効果の可能性が高く、EA:L270-271（Home で10倍段階の残りを煽らない）とコードの食い違いも解消できます。核の段は、核の形が育つことで示します。
- **大きな節目の直後は、必ず次の近い目標を出します**（報酬の直後はやる気が落ちやすいため、Kivetz et al. 2006）。
- **遠い目印（ゾウ）は、100時間（米俵）に届いてから旅の画面に輪郭で出します。** それより前は遠すぎて、序盤の進みが小さく見えてしまうためです。到達予測の日数は Home に出しません。
- **同期の途中**（`projectionIsLowerBound` または `isCloudVerificationPending`）は「80時間以上・同期中」と表示し、区間の進みを推測しません。
- **設定で非表示にできます**（「Home に次の目標を表示」）。

### 5.6 祝福の基準（二重祝福・新しい端末・同期）

到達は保存しません。「どこまで祝ったか」だけを端末ローカルに持ちます（`AccountScopedLocalState` のキーで UserDefaults、activity reset で消える）。

| 状況 | 規則 |
|---|---|
| 通常の完走 | Reward Receipt の「完走前の合計（`totalStudyGrams − grams`）」と「完走後の合計（`totalStudyGrams`）」の間に閾値があり、かつ Receipt の `projectionIsLowerBound == false` のときだけ、大きな演出を出す |
| 一度に複数の閾値を越えた | 最上位だけを祝い、残りは同じカードに並べる |
| **祝福の基準がまだ無い端末**（新しい端末・再インストール・機種変更・既存利用者のアップデート初回） | その端末で初めて「下限値ではなく、クラウド検証も済んだ」射影を観測するまで、何も祝わない。観測したら、その時点の到達済みの番号を**黙って基準として採用**し、「重さの旅を読み込みました。いまは○○、つぎは○○です」のチップを1回だけ出す。通過済みの段を一つずつ祝うことはしない（1万時間の特大演出が新しい iPad で再生されることはない） |
| 同期経由で閾値を越えた（その端末での完走ではない） | 次に Home を開いたときに、静かなチップを最大1枚にまとめて出す（最上位の名前だけを名指し） |
| 画面を見ていない間に越えた | 同上。今の「1.2秒の演出を見逃すと二度と見られない」問題もこれで解消する |
| 記録の削除で閾値を下回った | 正直に表示を戻す。取り消しの演出は作らない。祝福の基準も下げる（再到達で再び祝う） |

### 5.7 比較物のルール

- 定義どおりの量（米俵＝60kg）を優先する。それ以外は「ほど」「約」と書き、「釣り合った」とは断定しない。
- 使うのは自然物・食べ物・動物だけ。建物・キャラクター・ロゴ・車は使わない（権利の確認が要り、どの国でも通じるとは限らないため）。**人の体重とは比べない**（身体イメージへの配慮）。
- すべての名所に重さの比較を付ける（ドラフトの「丸一日（24時間）」は比較が無く梯子の比喩が崩れていたので、20時間・12kg に置き換えた）。
- v1.3 までに、日本語版と英語版の出典台帳を作る。個体差の幅が大きすぎる比較物（コーギー、ヒグマ、シロサイなど）は、台帳で幅を確認し、必要なら差し替える。英語などのロケールでは比較物を選び直す（米俵は英語圏では伝わらない）。
- 学力や熟達の証明だとは書かない（EA:L50）。

---

## 6. 学習曲線

方針：仕組みは、それが**初めて起きた瞬間**に一つずつ教えます。チュートリアルは、必要になった瞬間に出すほうが効きます（[Andersen et al. 2012](https://grail.cs.washington.edu/projects/game-abtesting/chi2012/chi2012.pdf)）。基本機能はどれも最初から使え、閉じることはありません。案内はすべて一度きりです。

| 段階 | 開示されるもの | 覚えること | 一度だけの案内 |
|---|---|---|---|
| 初回起動 | オンボーディング2枚目に「1分＝10g。25分で1粒（250g）」。最後の画面に、空の瓶と、瓶の奥の無色透明の器 | 集中は重さになる | ― |
| 1本目の完走 | 色が入って落ちる。初日から全開の輝き（カット、白いエッジ、ハロー、星）。最初の名所「最初の一粒」。HUD に次の名所。完走カードに「明日もこの時間に？」 | 完走すると必ず一粒。旅の一歩目 | 着地のあとに「傾けると、宝石が光ります」（Reduce Motion のときは出さない） |
| 初日（2〜4本） | 粒をタップすると跳ねて、内訳チップ（日付・テーマ・時間）が出る。「はじめての1時間」。休憩明けの「ここで区切る」 | 一粒ごとに日付とテーマが刻まれている。自分で区切れる | 「粒をタップすると、内訳を見られます」（初めてタップしたら消える）。初めて区切ったときに if-then を提案 |
| 1〜2週（2.5kg 前後） | 10粒の融合、時間の核の誕生、1巡目と最初の年輪、首カラーの最初の刻線。最初の週替わりに先週のかけら | 融合しても重さはそのまま。核は生涯の重さ。巡は2.5kgのリズム | それぞれ初めて起きた瞬間に1行ずつ（例：「10粒ぶんの時間が、ひとつの結晶に。重さはそのまま」） |
| 1か月前後（6〜12kg） | 最初の一里塚（10時間、◇が灯る）、20時間、最初の月の小瓶と棚 | 10時間ごとに核へ印が残る。月は区切りで、しまえる | 最初の◇で「10時間ごとに、核の軌道に印が残ります」。最初の封で「月ごとに、小瓶が棚に並びます」 |
| 3〜8か月（25〜60kg） | 核の二段目、50時間、100時間の米俵と最初の星、遠い目印（ゾウ） | 100時間がどれほどの重さか。10個の◇が1つの星になる。遠い目標があること | 米俵のあとに「いちばん遠い目印は、ゾウ1頭ぶんです」 |
| 1年 | 最初の年の星、「一年の重さ」。1日4本の人は核の三段目。積み上がり計画が実績とつながる | 一年で積める重さ。休んでも何も減らないこと | 年の星が昇るときに一度だけ |
| 空白後の復帰（7日以上） | 「おかえり」、最後の粒が光る、開始チップ | 何も失っていない | 空白の日数は出さない |
| 既存利用者のアップデート初回・新しい端末 | 通過済みの一里塚・名所・核・棚・星を黙って読み込む（§5.6） | ― | 「重さの旅を読み込みました。いまは○○、つぎは○○です」を一度だけ |

見た目が育っていく順序は §7.7 にまとめます。

---

## 7. グラフィック高度化仕様

この章の v1.2 部分は、**描画層だけで実装でき、スキーマにも進行（価値・閾値・融合の規則）にも触れません**。例外は §7.5 の物理半径の拡大（D4）で、これは物理と容量の再調整を伴うため、D4 の承認後に同じリリースで行います。

### 7.1 目標画像との対応

| 目標画像の要素 | 判定 | 実装 |
|---|---|---|
| 大きく光る3Dローポリの宝石（コーラル・青・紫） | 採用 | GemArtwork（§7.2〜7.4）。大きさは瓶全体の適応倍率（D4、§7.5）：若い瓶で25分の粒が内側の幅の約0.18〜0.20。内側の光（`gem.innerGlow`）とハローで、芯から光って見せる |
| 大きな多色の「時間の核」 | 条件付きで採用 | 色はテーマの割合（比率はそのまま）を、色相の順に並べた大きな色の場として塗る（§7.9）。テーマが1つでも、面ごとの明暗と色相の揺らぎ、広い白熱の中心、ガードルの弱い分散光で多色感を出す。「虹」とは呼ばない（D3） |
| 点線の軌道と◇ | 採用 | v1.2 は既存の意味のまま見た目だけ刷新。v1.3 で◇を一里塚（10時間）に読み替える（D7） |
| 玉ボケ | 採用 | 巡の進みと連動（既存の Canvas の見た目だけ変える） |
| 床の光と反射 | 採用 | 左右2色の光だまり、映り込み、底の光の線、静止した十字のきらめき（瓶の舞台の光、§7.8） |
| 厚いガラス、左右で色の違うリム | 採用 | ガラス v2（ラウンド5でリムと壁の鏡面を明るくし、瓶の内側を光で満たした。§7.8） |
| 銅の蓋 | 不採用 | 銅の首カラーで代える（§7.8、D2） |
| 「1巡 ●」ピル | 採用 | 「N巡」を13pt bold で。●（長期の到達痕）は首カラーの刻線に移す |
| HUD が瓶の外（上） | 採用（条件付き） | 瓶の口の上に、コンパクトな HUD を置く。高さが足りない端末では内側にフォールバック（D5、§8.1） |
| 核のラベルカード2枚 | 縮小 | 「時間の核」の小さな名前だけ残す。数値は HUD と重複するので撤去し、核の部分をタップしたときに内訳を出す |
| 「宝石をタップすると、内訳を見られます」 | 文言を変えて採用 | 「粒をタップすると、内訳を見られます」（初回だけ）（D15） |
| 「核まであと35時間32分」 | 不採用 | 一里塚・名所の1行に置き換える（D6） |
| 同じ大きさの粒が約12個 | 補正する | 実際には15粒は「×10が1個＋粒5個」になる。×10の結晶はステップカットで格が上がって見えるので、密度の不足を補う。床には、生涯の重さで育つ小さな結晶片の層（ジェムベッド、§7.9.1、D25）を物理の背面に敷く |

### 7.2 カットの梯子（段ごとのカット）

**原則**

- **ばらの粒の基本の輝きは、1粒目から全部同じです。** 長さ・時間帯・Screen Time 由来かどうかで格を変えません。粒の間に序列を作りません。
- **まとまり粒と時間の核の格は、含む時間（グラム）で決めます。** レベル（×10、×100…）や粒の個数では決めません。「×N」のラベルは事実なので残します。
- 物理はすべて円のままで、輪郭は物理円の1.02r以内に収めます（面積∝質量の不変条件を守る）。

**まとまり粒の格（グラム段）**

g＝そのまとまり粒が含むグラム。25分中心の人では、今のレベルと同じ段になります。

| 段 | 条件 | 25分中心の人 | 10分粒中心（Screen Time） | 1分×10（Pro） | 90分中心 |
|---|---|---|---|---|---|
| A0 | g < 2.5kg | ― | ×10（1kg） | ×10（100g）、×100（1kg） | ― |
| A1 | 2.5kg ≤ g < 25kg | ×10 | ×100（10kg） | ×1000（10kg） | ×10（9kg） |
| A2 | 25kg ≤ g < 250kg | ×100 | ×1000（100kg） | ― | ×100（90kg） |
| A3 | 250kg ≤ g < 2.5t | ×1000 | ― | ― | ×1000（900kg） |
| A4 | g ≥ 2.5t | ×1万〜 | ― | ― | ― |

**段ごとのカットと光の予算**

| 段 | 対象 | カット | 面の数（目安） | 変種 | ハロー（径＝本体径×／α） | 動的グリント | 焼き込み微光 | 面のコントラスト c |
|---|---|---|---|---|---|---|---|---|
| L0 | ばら（実測・Screen Time） | タンブル：正二十面体を2回細分化したジオデシック球を、UUID で決まる向きから正射影 | 約80 | 4（UUID） | 2.2／0.56×s(g) | 1 | 2 | 0.85 |
| L0r | ばら（自己申告） | ラフ：正二十面体そのまま。彩度−15%、既存の破線リング | 20 | 4 | 1.9／0.20 | 0 | 0 | 0.40 |
| L0g | チュートリアル | 無色ガラスのタンブル | 約80 | 1 | 1.8／0.08 | 0 | 1 | 0.35 |
| A0 | まとまり g<2.5kg | タンブル＋構成色の扇形（ばらと同じ格） | 約80 | 4 | 2.3／0.60 | 1 | 2 | 0.85 |
| A1 | 2.5〜25kg | 八角ステップカット（大きな鏡のテーブルと段） | 8回対称・約33 | ― | 2.4／0.60 | 1 | 2 | 0.75 |
| A2 | 25〜250kg | ラウンドブリリアント（テーブル・スター・カイト・ガードル） | 8回対称・57相当 | ― | 2.5／0.68 | 2 | 3 | 0.95 |
| A3 | 250kg〜2.5t | ラディアント（10回対称、白い芯） | 10回対称 | ― | 2.6／0.74 | 3 | 4 | 1.0 |
| A4 | 2.5t〜 | ラディアント（12回対称、頂点側に冠のグリント） | 12回対称 | ― | 2.7／0.80 | 3 | 5 | 1.0 |
| 記念石 | 成果の星 | 八角ステップカット＋銅の爪4本。マークとその背景は今のまま（正立、コントラスト比7以上） | 8回対称 | ― | 2.3／0.62（ばらより強い） | 1 | 1 | 0.80 |
| 黒い石 | Screen Time の障害物 | **ラウンド6で艶のない黒曜石に**（9〜11角の不揃いな輪郭、明るい縁なし、glow 0、数は下側の小さな刻印。D26、§7.14） | ― | ― | 加算ハローなし。代わりに通常合成の暗いハロー（1.6R、黒 α0.35）で周囲の光を吸う | 0 | 0 | ― |
| 時間の核 | SwiftUI の核 | v4：十角形のラディアル・ブリリアント（放射面20、外周の冠の小面30、明るいガードル。色は面から切り離した「色の場」。§7.9） | 50 | ― | 左右2つのローブ（左半分・右半分の平均色）1.35R・α0.45＋ガードルに沿う白寄りのブルーム | 静止した中心星 | ― | 各面が広い白熱の中心→飽和→深い縁の放射グラデーション。半分ごとに明度0.86倍。稜線は色と無関係の白線 |

- s(g)＝clamp(√(g ÷ 250g), 0.6, 1.0)。ばらの粒のハローは25分で頭打ちです（大きさは質量で既に増えるため）。
- ラウンド5：物理の半径（D4）は変えずに、見かけの存在感だけを補った。ばらのハロー α を0.50→0.56、A0 を0.55→0.60（記念石は0.62でばらより強いまま）、面の中間調の彩度を s₀×(1.22−0.70b) から s₀×(1.36−0.70b) に上げた（撮影画像の粒の帯で平均彩度0.38→0.44）。
- ハローのテクスチャは中心から外周へガウス減衰し、半値の半径は本体半径の約1.2倍です（スプライト径2.2倍は減衰の裾まで含んだ大きさ）。
- 「×N」は、首カラーと同じ銅の小さな刻印札に彫り、常に正立させます（ラウンド6、D26。濃紺の不透明なプレートはやめた。文言は同じ）。札は瓶の倍率に逆らって縮め、画面上の文字は7〜9pt のまま。Pro の月刻印は、札の下に「2026.9」と常に表示します（D21）。
  - **ラウンド9で実装**：同じ銅の札を2行にし、数の行の下に淡い溝と、数の0.8倍（5.6〜7.2pt）の「2026.9」を同じ彫り方で刻む。札は1枚の焼き込みスプライトのままで、数の行は1行の札と同じ位置（中心から0.40R 下）に残る（アンカーで合わせる）。月は**その結晶ができた月**で、融合シートで Pro に出している月（`JarAggregateRequest.monthLabel`「2026年9月」）と同じ。数字と点だけの刻印なので、言語によらず同じ表記にする（年は日付の欄として整形し、桁区切りの数にはしない）。
  - Pro でも、結晶のカット・光・ハロー・大きさ・星の数は1画素も変わらない（Pro 限定の新しい見た目は足さない）。変わるのは札の2行目だけ。Pro の購入状態は今までどおり `JarScene.showsMonthLabels`（Home が `PurchaseManager.isPro` から設定）で受け取り、今まで何も描いていなかったこの値が、札の2行目を出し入れする。
  - 課金の約束はそのまま：購入画面の「まとまり粒の月刻印／積み重ねた月を残す」、ストアの説明・`AppStore/review-notes.md`・`iap-review-notes.md` の「まとまり粒の月刻印」の文言は変えていない。以前は瓶の中に見える形がなく（層の描画を廃止してから `showsMonthLabels` は描画に使われていなかった）、融合シートの「まとまり」の欄だけに月が出ていた。これで約束が瓶の中で目に見える。
- ラウンド6：瓶の倍率で大きくなった宝石が平らに見えないよう、回転対称の加算の「内側の光」を面の上に重ねた（ばら α0.56、まとまり粒0.44、記念石0.22。§7.5）。
- 山の中で強調する（ハローを10%増す）のは、**含むグラムが最大の1体**です。レベルが最大の1体ではありません。

### 7.3 材質とライティング（回転しても光源が回らない構成）

**問題**：粒は物理で回転します（`allowsRotation = true`）。2D の剛体は画面に垂直な軸でしか回らないので、左上から光を当てた陰影を本体のテクスチャに焼き込むと、転がった粒は下側が光ってしまいます。3Dローポリで描いても、この問題は解消しません。

**解決（v1.2）：光を2つに分けます。**

1. **本体のテクスチャ（物理と一緒に回る）**：方向を持たない光だけを焼き込みます。
   - 光源は**視線方向 V=(0, 0, 1)** だけにする。画面に垂直な軸の回転で陰影が変わらないので、回っても光源が回って見えない。
   - 面の明るさ：b_f = lerp(1−0.7c, 1.0, h_f) × (0.55 + 0.45·(N·V)^0.6)。h_f は面ごとのハッシュ（0〜1）。中心の面ほど明るく、縁の面ほど暗い。
   - 縁のフレネル光：(1 − N·V)^3 × 0.35 を、白と寒色 #8ACBFF の中間色で加える。
   - 面の色：色相＝基準色相＋8°×(2h_f − 1)（±8°）、彩度＝基準彩度×(1.36 − 0.70·b)（上限1）、明度＝0.50＋0.50·b（`GemTone.facet`。ラウンド5で彩度の係数を1.22から1.36へ）。
   - 内側の発光：中心から、light 色 α0.45 を 0.7R で0へ。
   - エッジ：内部は白 α0.28・0.6pt、外周は白 α0.75・1pt。
   - 焼き込み微光（面の交点の小さな光）は、**120°ずつ回転対称に**置く（どこを向いても偏らない）。
   - **左上の鏡面ハイライトや、上側の頂点の星は焼き込まない。**
2. **画面に固定した光のリグ（`gem.lightRig`、`updatePresentationLighting` で `zRotation = −本体の zRotation`）**：方向を持つ光はすべてここに置きます。全宝石で同じテクスチャを共有するので、描画は1回にまとまります。
   - キーライトの艶：中心 (−0.28R, +0.34R)、0.62R×0.40R の楕円、暖かい白 #FFF3E6、加算 α0.35
   - パビリオンの陰：右下の三日月、黒 α0.16（通常合成）
   - 寒色のバウンス光：左下の縁の弧、#8ACBFF、加算 α0.22
   - 暖色のリム：左上の縁の弧、#FFB38A、加算 α0.18
   - 接地影：1.5R×0.5R の楕円、y −0.63R、紺 α0.38（既存の値）
   - 動的グリント：UUID で決めた位置（上半分の輪郭の頂点付近）。リグが画面に固定されるので、転がっても上側に残る

**テスト（追加）**

- 本体テクスチャの輝度の重心が、中心から半径の4%以内にある（焼き込みに方向の偏りがない）。
- `updatePresentationLighting` の後、`gem.lightRig.zRotation == −node.zRotation`（180°回しても、キーライトの艶は左上にある）。

**将来の改善（v1.5 以降・任意）**：面ごとの法線テクスチャとアルベドを分け、共有 SKShader に `u_light`（ワールド座標）と、ノードごとの回転（SKAttribute）を渡す方式にすると、面ごとの明暗が光源に正しく追従します。Simulator の確認で「面が車輪のように回って見える」ことが目立つ場合にだけ行います。

### 7.4 テーマ12色の宝石化

色相は変えません（色がテーマを見分ける手がかりのため）。変えるのは、彩度の下限と、面の明暗の幅と、明度です。**明度はパレットの明るさの順序を保つ**（ラウンド7）。

**パレットの一本化（ラウンド7）**：テーマの色の候補12色は `SubjectPalette`（`PomoGem/Core/SubjectPalette.swift`）の1本のリストにし、テーマの編集画面とオンボーディングはここを読む（以前は2か所に同じ16進の列があった）。瓶の変換のテストも、目印（§7.12）の割り当ても、このリストを直接読む。並びの見直し（CVD に配慮した順序など）は並行セッションの pr-33 が行い、このリストを直すだけで瓶の側は自動で追従する（テストも同じリストで回る）。保存済みのテーマの色は書き換えない。

**共通の規則**

- **明度の順序を保つ変換（`GemToneLightness`）**：テーマの色の OKLab の明度 L から、粒の本体色の明度 L′ を1本の狭義単調増加の曲線で決める。L′ ＝ 0.61 ＋ 0.21 × tanh(1.4 ×（L − 0.63）÷ 0.21)。
  - 中心の傾きは1.4倍で、パレットの近い明るさの差（琥珀と赤銅、菫と藍など）を広げる。どの色でも 0.40〜0.82 に収まる。
  - 色相と彩度の下限（s≥0.74、空色は0.60）はそのまま。動かすのは HSB の明度だけ。彩度の下限のままでは届かない明るさ（明るい青紫など）は、明度1のまま彩度を下げて（白へ寄せて）合わせる。
  - 本体色（`GemTone.body`）は、面の光 b＝0.514 の面の色そのもの。面の明度は (0.50＋0.50b) × 本体の明度 ÷ 0.757（上限1）で、テーマの明るさがすべての面に入る。deep も同じ倍率を掛ける。
  - 以前の `vivid(s≥0.74, b≥0.88)` は、明度の下限でパレットの明るさの差を消していた（しかも実際の面の明度は全テーマ共通の 0.50＋0.50b で、テーマの明度はどこにも入っていなかった）。そのため、瓶では若草が琥珀より明るく、赤銅が菫より明るいなど、順序が入れ替わっていた。
  - 計算は（16進、彩度）ごとに1回の二分探索で、結果をキャッシュする（光の計算は1秒に数回トーンを作るため）。
- light：色相+8°、s 0.30、b 1.0
- deep：色相−6°、s 0.85、b 0.44 × 本体の明度 ÷ 0.757
- halo：色相そのまま、s 0.75、b 1.0
- グリントの色：白70%＋halo 30%（朱色・珊瑚は白85%。彩度の高い赤の点滅を避ける）
- 旧い素材の粒（金・虹の内部テスト用）の塗りも、同じ本体色を使う。

**テスト（`GemThemeDistinctionTests`）**：`SubjectPalette` のすべての組で、(1) 本体色の OKLab L の順序がパレットの順序と同じ（実測・自己申告の両方、差0.001より大きい組）、(2) 焼いた粒の画像の平均の OKLab L でも、差0.02以上の組は順序が同じ、(3) 曲線は狭義単調増加で範囲内、パレットの帯では差を狭めない。以前の変換に戻すと77件の失敗になることを確かめた。

| テーマ色 | 入力の L | 本体の L′ | 焼いた粒の平均 L |
|---|---|---|---|
| 藍 #5967C8 | 0.552 | 0.509 | 0.619 |
| 赤銅 #A76A3F | 0.581 | 0.544 | 0.634 |
| 瑠璃 #4D7CDE | 0.602 | 0.571 | 0.671 |
| 空色 #5688A8 | 0.604 | 0.574 | 0.673 |
| 菫 #8A6FD1 | 0.611 | 0.584 | 0.653 |
| 紅藤 #C25FA3 | 0.626 | 0.604 | 0.682 |
| 若草 #739B45 | 0.641 | 0.625 | 0.711 |
| 緑青 #3FA57C | 0.651 | 0.639 | 0.717 |
| 朱色 #E85D4A | 0.654 | 0.643 | 0.705 |
| 珊瑚 #D56B82 | 0.658 | 0.648 | 0.702 |
| 青緑 #36A7AE | 0.669 | 0.664 | 0.738 |
| 琥珀 #D6863A | 0.691 | 0.690 | 0.756 |

焼いた粒では、明るさが近い組（差0.02未満。菫と瑠璃・空色、若草と朱色・珊瑚など）の順序が内側の光やふちの光で入れ替わることがある。これは色相で見分けられる組で、Differentiate Without Color の目印（§7.12）でも区別できる。時間の核（§7.9）は光の場として描くので、この変換は掛けていない。

| テーマ色 | hex | 内部での見立て（UI には出さない） | 個別の調整 |
|---|---|---|---|
| 朱色 | #E85D4A | パパラチア | グリントは白85% |
| 瑠璃 | #4D7CDE | サファイア | ― |
| 紅藤 | #C25FA3 | ピンクトルマリン | ― |
| 緑青 | #3FA57C | エメラルド | light の色相シフトは−4°（黄緑に寄せない） |
| 菫 | #8A6FD1 | アメジスト | ― |
| 琥珀 | #D6863A | インペリアルトパーズ | ― |
| 青緑 | #36A7AE | アクアマリン | ― |
| 珊瑚 | #D56B82 | ロードライト | グリントは白85% |
| 若草 | #739B45 | ペリドット | ― |
| 藍 | #5967C8 | タンザナイト | ― |
| 赤銅 | #A76A3F | コニャックトパーズ | 面のコントラスト c を+0.1（茶色は濁りやすい） |
| 空色 | #5688A8 | 淡いアクアマリン | 彩度の下限0.60 |

宝石名は価格の序列を連想させるので、UI では使いません。

### 7.5 大きさ（D4）― 瓶全体の適応倍率

**決定（2026-09-25、リードデザイナー。ラウンド6で実装）**：固定の倍率（×1.35／×1.6 の案）をやめ、**瓶の中のすべての体に、1つの倍率 s を掛ける**。若い瓶は大きな宝石が数個、瓶が埋まるにつれて出荷中の大きさ（s＝1）へ戻り、融合で体が減るたびにまた少し大きくなる。

- **比率はそのまま**：各体の半径は今までどおり `PebbleRadiusPolicy`（面積∝質量、上下限つき）で決め、s はその上から全体に掛ける。粒どうしの「面積∝質量」は保たれる。保存する行・グラム・容量の計上（`capacityUnits`）・融合の閾値と規則は s を知らない（見た目と物理の大きさだけが変わる）。
- **式**：s ＝ clamp(√(A_budget ÷ A0), 1.0, s_max)（`JarScalePolicy`）
  - A0：瓶にいる体（学習の粒・まとまり粒・記念石・Screen Time の黒い石）の、自分の半径での断面積の合計 Σπr₀² に、落ちてくる1粒を足したもの。落下待ちの列にいる粒は、落ち始めるまで数えない。
  - A_budget：瓶の内側の長方形の面積 × **0.30**。拡大した体の断面積は、s＞1 のあいだこの予算を超えない。
  - s_max：**2.4**。25分の粒の直径は約55pt で、Home の瓶の内側の幅（Simulator の実測で iPhone 17 Pro 306pt、iPhone 12 mini 279pt）の約0.18〜0.20（目標画像は0.15〜0.20）。
  - 下限1.0：出荷中の大きさ（最悪ケースで口の下の余白17%）より小さくはしない。A0 が予算を超える重い瓶は、今の見た目のまま。
- **段と履歴（ヒステリシス）**：s は4%刻みの段（1、1.04、1.04²、…、上端は2.4）に丸める。目標が今の段より下がれば、その場で下の段へ縮む（予算を超えない）。大きくなるのは、目標が今の段より2段以上上に出たときだけ。1粒の出入りを繰り返しても、瓶は1回しか変わらない（脈打たない。テストで固定）。
- **変わる時（これ以外では変わらない）**
  - 着地：落ちてくる粒は、着地したあとの倍率で生まれ、ほかの体は**着地の瞬間に**その倍率へ縮む（SpriteKit の接触コールバックの中では変えず、次のフレームで適用）。
  - 融合：10体が1体になると A0 が減り、**瓶全体が少し大きくなる**（融合は足し算で、瓶を空にしない）。新しい結晶は新しい倍率で生まれ、ほかの体はそこへ育つ。
  - 復元（起動、データの再読み込み）：内容から決め直し、動きなしで並べる。1行の粒は中央に寄せて並べる（1粒目は核の下に座る）。
  - 内容の差し替え（iCloud の同期での置き換え、記念石の入れ替え、Screen Time の黒い石の即時の配置や繰り上がり）：同じ規則で決め直す。瓶そのものが小さくなったとき（完走カードで縮むなど）は縮むだけで、瓶が大きくなっても自分では育たない（次の着地か融合を待つ）。
- **動き**：0.5秒の smoothstep。見た目と物理の半径は一緒に動く（SpriteKit はノードの拡大率で物理の円も拡大する。macOS の SKRenderer で確認）。毎フレーム数%ずつ変わるので、隣り合う体は少しずつ押し離され、重なりの爆発・壁のすり抜け・口からの飛び出しは起きない。止まっていた体は起こし、終わりに壁の内側への救出を1回行う。アイドルで止める前には必ず終点に揃える。最初のフレームを描く前（起動直後の配置）は動きなしで適用する。融合の誕生の弾み（0.6→1.08→1）は倍率に対する相対値にし、途中で倍率が変わったら弾みを引き継ぐ。
- **黒い石**：A0 に入る。倍率は学習の粒の0.72倍（下限1.0、上限1.6）。学習の粒の倍率を超えず、大きくなるときは同じ時間の学習の粒より大きくならない（10分の石＝半径10は、同じ瓶の10分の粒＝7.27×s 以下。すべての段でテストで固定）。
- **焼き込み**：体のテクスチャは表示する大きさ（半径×s）で焼き、局所座標のスプライトに戻して貼る（拡大してもぼやけない）。段が変わると、変わった体の分だけ並列で1回焼く（重い瓶は s＝1 のままなので焼き直さない）。起動時の先焼きは若い瓶の倍率（2.4）で、5テーマの25分の粒と自己申告30分の4変種（40体、3倍で約5MB）。
- **変えないもの**：音と触覚は拡大前の半径で計算する（大きく見えても同じ音）。シェイクは拡大前の質量で割り、押しの力は拡大に合わせて増やす（若い大きな瓶も同じように揺れ、重い結晶が遅れるのは今のまま）。×N の札と黒い石の刻印は拡大に逆らって縮め、画面上の大きさを保つ（§7.14）。容量・融合・価値の計算・永続化・CloudKit・IAP は変えていない。
- **大きな宝石の光（ラウンド6）**：大きくなった粒が暗く平らに見えないよう、回転対称の加算の「内側の光」（`gem.innerGlow`：柔らかな芯と、輪郭のすぐ内側の明るい帯。粒の色相の淡い色で染める）を面の上に重ねた。α はばら0.56、まとまり粒0.44、記念石0.22、自己申告0.34、チュートリアル0.18。方向を持たないので、転がっても光源は回らない（§7.3）。Reduce Transparency では0.6倍。
- **Debug の比較用スイッチ**：この決定で `GemSizePolicy` と `POMOGEM_UI_TEST_GEM_SIZE`（`d4`／`d4-agg1`／`d4-lite`）は撤去した。

**受け入れ基準 (a)：典型の瓶で、ばらの粒が内側の幅の約0.15〜0.20**（計算値。瓶の内側の大きさは Simulator の Home で実測。`testTypicalLoadsShowLooseGemsAtAFifthToASixthOfTheJar` で固定）

| 瓶の中身 | 17 Pro の Home（内側 306×398pt） | 12 mini の Home（279×356） | 既定の390pt の場面（334×398） |
|---|---|---|---|
| 1粒目 | s 2.4・0.180 | s 2.4・0.198 | s 2.4・0.165 |
| ばら5粒 | s 2.4・0.180 | s 2.4・0.198 | s 2.4・0.165 |
| 3.75kg（×10＋ばら5） | s 2.4・0.180 | s 2.4・0.198 | s 2.4・0.165 |
| ばら9＋×10が3個 | s 2.28・0.171 | s 2.03・0.167 | s 2.4・0.165 |

完走カードで瓶が縮んでいる間（17 Pro で内側 約306×288。見積もり）は、ばら9＋×10×3 の瓶が s 1.95（0.146）まで縮む。瓶が元の大きさに戻っても自分では育たず、カードを閉じたあとの着地で、元の大きさの瓶に合わせて決め直す（1粒増えた分、s 2.19 前後）。

**受け入れ基準 (b)：すべての fixture で、口の下の余白が15%以上**（10回以上の沈降の最小値）

- 測り方：`POMOGEM_UI_TEST_SETTLE_PROBE=10`。起動後の最初の沈降に加え、全力のシェイク（左右交互）で山を崩して沈降させることを10回繰り返し、11回すべての余白を tmp の `jar-frames.log` に書く。余白＝（口の高さ − いちばん高い体の上端）÷ 瓶の内側の高さ。
- 予算の面の保証：どの fixture・どの瓶でも、s＞1 なら拡大した断面積は内側の30%以下、そうでなければ s＝1（出荷中の大きさ）であること（`testEveryShowcaseFixtureStaysWithinTheAreaBudgetOrAtTheShippingSize`）。

実測（ラウンド6の最終コード、専用の Simulator。各 fixture で起動後の沈降1回＋シェイク後の沈降10回＝11回の最小値。s は fixture の倍率）

| fixture | 中身 | 17 Pro：s・最小余白 | 12 mini：s・最小余白 |
|---|---|---|---|
| first | ばら1 | 2.4・86.0% | 2.4・86.0% |
| home | ×10＋ばら5（3.75kg） | 2.4・72.5% | 2.4・69.0% |
| tiers | ×100＋×10＋ばら7＋記念石1（29.25kg） | 2.4・50.2% | 2.19・49.6% |
| heavy | ×1000＋ばら4（約251kg） | 2.4・71.9% | 2.4・68.6% |
| veteran | ×1万＋ばら6（約2.5t） | 2.4・68.3% | 2.4・55.2% |
| gallery | 段の見本16体＋黒い石 | 1.80・48.8% | 1.80・49.5% |
| fusionfx | ばら9＋×100 6＋×10 5、10粒目で融合 | 1.54 →（融合）1.73・54.0% | 1.54 →（着地）1.48 →（融合）1.67・54.5% |
| worstcase | 320pt の瓶の最悪ケース | 1.0・24.0% | 1.0・20.6% |
| stress | 物理ボディの上限＋記念石12＋黒い石36 | 1.0・26.6% | 1.0・22.7% |

- すべて15%以上。worstcase と stress は s＝1 で、体の大きさは出荷中と同じ。worstcase の最小値（24.0%／20.6%）は、以前に起動直後だけを3回読んだ値（16〜17%）より高い。1行を中央に寄せる並べ方と、シェイクのあとの沈降のしかたの違いによると考えられるが、原因は切り分けていない。
- stress は1,200体近くあり20秒以内に止まりきらないので、11回とも20秒で打ち切った時点の値（`timeout`）。
- 融合の瞬間：`gem-shots/r3/jar-fusion-moment.png`（録画から取り出した、融合の前・閃光・育つ途中・後の4コマ。瓶全体が約12%大きくなる）。

**以前の案（記録）**：固定倍率の案は、最悪ケース（320pt の瓶にばら9・root 18・記念石12・黒い石36）で、A（ばら×1.6・まとまり×1.2・記念石×1.15）が余白1〜2%、A'（ばらだけ×1.6）が9〜11%、B（ばらだけ×1.35）が15〜16% で、B でも25分の粒は約31pt（目標画像の約6割）にとどまった。最悪ケースの体は融合でこれ以上減らないので、固定倍率では「若い瓶の大きさ」と「重い瓶の余白」を両立できなかった。適応倍率は、重い瓶では s＝1 に戻るので最悪ケースの余白は出荷中と同じで、若い瓶だけを大きくできる。

**残る提案（この決定とは独立、未実装）**：まとまり粒の基準半径を、レベルではなく含むグラムの段で決める（18＋2.5×(段−1)、上限28。段は A0/A1→1、A2→2、A3→3、A4→4）。今の `PebbleRadiusPolicy.aggregateRadius` はレベルの基準半径にグラムの補正（0.85〜1.15）を掛けている。

### 7.6 光・グリント・粒子（上限つき）

**加算ハロー**

- 全宝石で1枚の放射テクスチャ（128px）を共有し、色だけを乗せて加算合成する。
- HUD の帯（瓶の内側に HUD がある場合）では α を半分にする。朝凪（明るい背景）では×0.7。Reduce Transparency では×0.45。
- **山の露出の上限**：40年 fixture（root 18＋ばら9）で、宝石以外の画素の最大輝度が0.85を超えないこと。超える場合は、全ハローの α に 1/√max(1, 体数÷12) を掛ける。

**グリント（星）**

| 項目 | 上限・値 |
|---|---|
| 1体あたりの動的グリント | §7.2 の表（0〜3） |
| シーン全体の動的グリントのスプライト | 64個まで。超える場合は、含むグラムの大きい順に割り当てる |
| 傾きのきらめき | 各グリントは、傾き量（−1〜1、平滑化済み）の中に幅0.12の「窓」を持つ（位相は UUID 由来）。期待同時点灯数は約4個 |
| 自発のきらめき（演出の強さ「標準」のときだけ、D17） | 同時に3個まで。発生間隔0.6秒以上。同じ宝石は2.5秒以上あける。1回420ms（立ち上がり140ms、保持60ms、減衰220ms）、拡大は1.25倍まで。シーンが起きている間（操作後、アイドル停止まで）だけ |
| 点滅の安全 | 同じ場所は1秒に1回まで。グリント1つの面積は画面の0.2%以下。彩度の高い赤では点滅させない（グリントを白寄りにする） |
| 止める条件 | Reduce Motion、「控えめ」、低電力モード、`thermalState ≥ .serious`、アイドル停止中（傾きの窓だけは残す。§7.13） |

**演出の強さ「標準／控えめ」（D17、ラウンド9で実装）**

設定の「瓶の表示」に「演出の強さ」（標準／控えめ、既定は標準）を置き、瓶・時間の核・融合シート・共有カードがすぐに従う。**「視差効果を減らす」がオンのときは、選んだ値によらず常に控えめ**（`JarEffectsIntensity.resolved`）。控えめでも、粒の重さ・数・カット・融合・物理は変わらない。値はすべて `JarEffectsIntensity` に集め、テストはそこから読む。

| 演出 | 標準（今までどおり） | 控えめ |
|---|---|---|
| 自発のきらめき | 上の表の上限で | なし |
| 傾きのきらめき（グリントの窓） | 窓に入ると光る | なし。各宝石に静止した星を1つ（α0.42＝以前の Reduce Motion の星 α0.6 × 0.7） |
| ハロー（宝石のハロー、初期の数粒の光、核のブルーム・左右のローブ・ガードルのブルーム、融合シートと共有カードの光） | ×1 | ×0.7 |
| 内側の光（大きな宝石の `gem.innerGlow`、2.5kg 未満の透明な器の光） | ×1 | ×0.8 |
| ゆっくりした呼吸（結晶のオーラ、初期の数粒の光、核、Overview の結晶、共有 GIF の核と ×10 の光） | あり | なし（静止） |
| 着地 | ハローが0.95まで 80ms→360ms、最初の星が一閃、光の粒6〜8個（0.52秒）、山の光+12%（0.12＋0.28秒）、カメラの揺れ（0.9秒） | ハローは途中まで（半分）60ms→200ms、星は光らない、光の粒3〜4個（0.31秒、距離0.7倍）、山の光+6%（0.08＋0.16秒）、揺れは半分の振れ幅で0.4秒 |
| 融合（瓶） | 10粒の収束0.52秒 → 閃光（2.2R、160ms）・衝撃リング（1R→3R、420ms）・ばね（0.6→1.08→1.0、380ms）、A2 以上は破片12個（合計1.12秒） | 収束0.32秒 → 弱い閃光（α0.55、100ms）・小さなリング（1R→2.2R、260ms）・行き過ぎのない出現（0.85→1.0、220ms）、破片なし（合計0.58秒）。A0 のフェードは200ms→120ms |
| 融合シート（`FusionOrbitStage`） | ばねの動き、白い光条4本、結晶1.16倍、合計約0.9秒 | 短いイーズ、光条なし、結晶1.06倍、10粒の寄りも浅い（0.285→0.345）、合計約0.5秒 |
| 共有カード | GIF で核が呼吸し、瓶の星が順に光り、周りのきらめきが揺れる | 核と光は静止し（光は0.7倍）、星は出さず、周りのきらめきも静止。ガラスを横切る光だけが動く（GIF のコマは変わる） |

- 「視差効果を減らす」は控えめを含む：光の倍率・静止した星・呼吸なしは控えめと同じで、着地・融合・融合シートの動きは §7.10 の Reduce Motion の代替（動かない）になる。
- 保存：`UserDefaults.standard` の `jar.effects-intensity`（`standard`／`subtle`）。**端末の見え方の設定**として、タイマーの既定の向きと同じく、アカウントに依存しない1つのキーに置く（iCloud で同期しない、スキーマに入らない、記録のリセットでも消えない）。§9.2 の「アカウント別のキー」の案からはこの点だけ変えた（見え方は端末の持ち主の好みで、記録やアカウントに属さないため）。
- 瓶（SpriteKit）は `JarSpriteView` がこの値を `JarScene.effectsIntensity` に渡し、各粒（`PebbleNode.setEffectsIntensity`）に配る。切り替えは光だけの描き直しで、止まった瓶の物理は起こさない（§7.13）。SwiftUI の核・融合シート・Overview の結晶は同じキーを `@AppStorage` で読み、共有カードの書き出し（画面の外）は保存値とシステムの Reduce Motion を直接読む。
- レアの「控えめ」（同期する抽選モード）とは別の設定で、互いに影響しない。

**粒子（イベントのときだけ。常に漂う粒子は置かない）**

| イベント | 数 | 寿命 |
|---|---|---|
| 着地 | 光の粒6〜8個 | 0.52秒 |
| 融合（A1以上） | 閃光1＋衝撃リング1。A2以上は破片12個を追加 | 0.8秒以内 |
| 核の誕生・段の上昇 | 中心へ集まる光24個まで | 1.5秒以内 |
| 画面全体の同時上限 | スプライト48個（使い回しのプール） | ― |

- ノード名は `drop.*`／`ambient.*` にして、Reduce Motion のときの削除と、撮影時の非表示に乗せる。
- Reduce Motion のときは粒子を出さない。

**玉ボケ（SwiftUI の既存 Canvas の見た目だけ変える）**

- 直径2〜7pt。ぼかしではなく放射グラデーションの円で描く。
- 色は暖色 #FFC27A／#FF9E6B／#FFE3B0 に、1割だけ寒色 #8ACBFF。7個に1個は白い芯を入れる。
- 量は巡の進みに連動（既存）。新しいアニメーションループは作らない。

**床（ラウンド5：瓶の舞台の光 `JarStageArtwork` に焼き込み、静止）**

- 左に #FF8A5B α0.62、右に #5BA8FF α0.56 の光だまり（それぞれ瓶の中心から±0.22の位置、横半径は瓶幅の0.30、縦46pt）と、瓶の光の映り込み（淡い桃→紫、α0.30）。ぼかし（SwiftUI の blur）は使わない。舞台の下端より60pt 先まで描き、光が途中で切れない。
- 瓶底の水平の光の線（1.6pt、中央が白い横グラデーション）。
- 静止した十字のきらめき4個。
- Reduce Transparency ではきらめきを消し、舞台の光を半分にする。

**山の光（`jar.pileGlow`、ラウンド5で修正）**

- 光の形を決めるのは、着地して止まっている粒だけ（`pileProfileRestingSpeed` より遅い粒）。瓶の口から落ちている完走の粒や、弾んでいる粒は数えない。止まった粒が1つもない間は、直前の光をそのまま保つ。
- 大きさ：幅は止まった粒の範囲の1.3倍（96pt 以上、瓶幅の0.9倍まで）、高さは1.8倍（64pt 以上、瓶の内側の高さの0.45倍まで）。中心はジェムベッドの上端＋40pt より上に行かない。形が変わるときは0.4秒で補間する。
- ジェムベッドより奥（z はストラタ＋0.52）に置き、ベッドの片側だけを桃色に染めない。α は0.45（瓶の内側が明るくなったので0.55から下げた）。
- 共有・ウィジェットの撮影では、通常合成に切り替えるときに床の光を0.5倍、山の光を0.45倍にする（透明なテクスチャに通常合成した光は、暗い瓶に加算した光より強く見えるため）。

### 7.7 見た目の熟達（含む時間で開き、後退しない）

粒の基本の輝きは初日から全開です。時間とともに育つのは、**まとまり粒のカット・時間の核・瓶の刻み**だけです。

| 生涯の重さ | 時間の核 | まとまり粒（25分中心の人） | 瓶 |
|---|---|---|---|
| 0〜2.5kg | 無色透明のブリリアントカットの器。ラウンド5で「透明な水晶」に描き直した：氷のような白い面（彩度0.08以下、未点灯の面の中間調は L 約0.7、点灯した面はほぼ白）、白い光（α0.3、1.3R）、ガードルの弱い分散光（α0.24）と冠の小さな火。2.5kgまでの進みに合わせて、10面が250gごとに白く灯り、灯った区画に小さな星が1つずつ入る（色は入らない） | ― | ガラス v2、銅の首カラー（無地）、床の光 |
| 2.5kg | 誕生：色が入る。ガードルの分散光 α0.10、軌道1本、◇10個 | ×10（A1 ステップ） | 首カラーに刻線1本、年輪の1本目 |
| 25kg | ガードルの分散光を α0.20 に | ×100（A2 ブリリアント） | 刻線2本 |
| 250kg | 2本目の軌道、中心の光条が強まる（分散光 α0.25） | ×1000（A3 ラディアント） | 刻線3本 |
| 2.5t | 星冠（頂点側の光の冠） | ×1万（A4） | 刻線4本 |
| 25t | 3本目の軌道 | ― | 刻線5本 |
| 250t | 最終形 | ― | 刻線6本（EA:L143 の到達痕の上限6と一致） |

- 首カラーの刻線は、今の「巡チップの横に並ぶ直径5ptの点（到達痕、`visibleMajorMilestoneTraceCount`）」をそのまま置き換えるものです。v1.2 で描く場所を移すだけで、数え方は変えません。
- 分散光は、どのカット石にもある**光学的な性質**として描きます（ガードルの細い虹色、面積は核の10%以下）。テーマ比の扇形（**稼いだ多様性**）とは別の層です。単色の人の核も、分散光と白い輝きで宝石らしく見えます。

### 7.8 瓶：蓋は付けない（D2）

**判断：Home の瓶には蓋を付けず、銅の首カラーで高級感を出します。**

1. 完走した粒が**瓶の口から入る**ことが、このアプリの儀式です（`CompletionEntryPhysics`）。蓋を付けると、完走のたびに開閉の演出（約0.5秒）が要り、毎回の待ち時間が延びます。
2. ブランド文書（`PomoGem/Resources/GENERATED-ASSETS.md`、`Brand/APP_ICON.md`）が「蓋なし、開いた厚いガラスの器」「器を宝箱として描かない」と決めています。蓋は「貯め込む宝箱」の意味を強めます。
3. 目標画像の高級感の大半は、蓋そのものより、**銅の金属と厚いガラスの反射**から来ています。首カラーでそれを取り込めます。

**ガラス v2**（前面用と背面用の2枚を事前に描き、瓶のサイズごとに既存のキャッシュへ）

- 壁の厚み7pt（外輪郭から7pt内側に内輪郭。帯は寒色 α0.18）。
- 左は暖色のリム（外縁 α1.0、コーラルから桃、内側15ptで0）。右は寒色のリム（外縁 α0.95、シアンから青、右下にフレアの筋）。（ラウンド5：α0.9／0.8・10pt から上げた）
- 左右の壁に、肩から高さ70%まで、1.5pt の白い鏡面線（左 α0.70、右 α0.56、両端はフェード）。（ラウンド5）
- 縦の反射帯2本（瓶幅の0.2と0.75の位置、白 α0.08）、肩のハイライト（α0.7）。
- 厚さ14ptの底レンズと、明るいコースティクスの線（ラウンド5：2pt、中央の白 α0.85、左右の色 α0.75）、その上に暗い屈折帯。
- 首のガラスのリッジ2本（白いハイライト付き）。
- 背面の吸収（`glassAbsorption`）は、中央0.06・側壁0.20（ラウンド5：0.08／0.32 から下げた）。背面の下の隅と肩の下に、内側の影（α0.18）。
- 傾きに合わせて、加算ハイライトのスプライト2〜3枚を±6pt動かす（既存の `updateOpticalTilt`）。
- 既存の細い SKShapeNode 約12個は、口の縁とタップ時の光（`jar.tap.caustic`）を除いて撤去する。
- 本物の屈折（SKEffectNode／CIFilter）は使わない。

**瓶の舞台の光**（ラウンド5、`JarStageArtwork`。SwiftUI の最背面に、舞台のサイズごとに Core Graphics で1回だけ焼く静止画。1.5倍、約1.7MB）

- 床の光だまりと映り込み（§7.6）。
- 瓶の輪郭から出る光（左は暖色 α0.55、右は寒色 α0.48 のぼかし12pt。段差の出る重ね塗りではなく、影のぼかしで描く）。
- 瓶の内側の光：紫の光の層（上 α0.50 → 下 α0.60、下ほど暖かい）、床から立ち上る山の光だまり（#FFA27E α0.52）、左右の壁から入る光（暖色・寒色 α0.26）、核の位置の弱い紫の光（α0.26）、核の列を避けた静止した玉ボケ10個（直径3〜8pt、α0.26〜0.46）。
- 背面ガラス（SpriteKit）はこの上に重なるので、Home と同じく吸収で少し沈む。`JarSnapshotter` の共有用の書き出しは、同じ絵を瓶の背面に描く（§8.6）。

**銅の首カラー**（`jar.collar.*`、z はガラス前面より上、`jar.tap.caustic`（21.5）より下）

- 幅は口の幅＋8pt、高さ14pt。上端 #FFE3CF → #D9967A → 基調 #B8735A → 下から2割だけ #8A4E3A → 陰 #5A2E22 の縦グラデーション（ラウンド5：茶色の帯を細くした）。高さ30%の位置に白い鏡面の帯（α0.9）。ハイライト #F2C4A8。
- 縦方向の異方性ハイライト帯（左右に強い白桃色、α0.95／0.6／0.75）。
- 傾き −1／0／+1 の3枚を事前に描き、傾きでクロスフェードする（同時に描くのは2枚）。
- 刻線（到達痕）は、帯の下端に細い溝として最大6本。
- 口は開いたまま。

**実測（ラウンド5、Home 3.75kg、iPhone 17 Pro の Simulator の撮影画像）**

輝度 L は Rec.709 の加重（0〜1）。領域は撮影画像（1206×2622px）の中の同じ長方形：瓶の内側（x140〜1060、y480〜1600）、左右の壁（幅50px の縦の帯、y600〜1500）、瓶の下の床（y1640〜1730）、首カラーの帯（y380〜450）、ベッド（粒の無い右側、y1500〜1580）。見た目のレビューの領域とは少し違うので、同じ列どうしで比べる。

| 項目 | main | ラウンド3 | ラウンド5 | 目安・目標画像 |
|---|---|---|---|---|
| 瓶の内側の平均 L | 0.192 | 0.200 | 0.281 | 目安0.30、目標画像0.409 |
| 左／右の壁の平均 L | 0.193／0.178 | 0.271／0.270 | 0.427／0.433 | 目安0.40 |
| 壁で L>0.6 の画素（左／右） | 0／0% | 3.7／6.3% | 16.2／20.7% | 目安20% |
| 瓶の下の床の平均 L | 0.089 | 0.192 | 0.286 | 目標画像0.314 |
| 首カラーの帯の平均 L（茶色に濁った画素） | 0.235（0%） | 0.325（13.7%） | 0.380（7.8%） | ― |
| ベッドの平均 L | ―（床0.157） | 0.307 | 0.472 | 目安0.48〜0.52 |
| 瓶の内側で L>0.6／L>0.9 の画素 | 2.9%／約1% | 5.9%／約1% | 7.0%／約1% | 静止した明るさの予算：30%以下／4%以下（§7.14） |

瓶の内側の平均は目安の0.30に少し届かない。これ以上上げると、瓶の中の HUD の白い文字のコントラストが落ちるので、D5（HUD を瓶の外へ）の判断のあとに見直す。

**底の年輪**：巡の回数を十進で底ガラスに刻む。細線＝1巡、中線＝10巡、太帯＝100巡、金線＝1,000巡、金帯＝1万巡。最大45本なので、40年使っても溢れません（v1.3。v1.2 では描かない）。

### 7.9 時間の核 v4（ラウンド2で v3、ラウンド5で見え方を v4 に）

v2（平らなモザイク）は、目標画像より暗く、ステンドグラスのように見えたため、ラウンド2で放射状のブリリアント（v3）に作り直した。v3 は割合の色が20枠にそのまま交互に並び、ビーチボール・風車・色相環のように読めたので、ラウンド5で「色の場」に直した（v4）。色の決め方（D3、テーマの割合）は変えない。

- **形**：十角形のラディアル・ブリリアント。中心から放射する主な区画が10、それぞれを明るい半分と深い半分に分けた放射面が20。外周0.74〜0.82Rに小さな面30枚の冠の輪、明るいガードル。
- **色の場（v4）**：色は面から切り離す。量子化した割合（上位5＋その他、5%刻み）を**色相の順**に並べ、いちばん広い色相の隙間の次から、7時30分の位置から時計回りに、割合どおりの角度で置く（`GemArtwork.CoreColorField`）。隣り合う色は1.5枠ぶん（約29°）で溶け合う。色相が80°未満の隣どうしは近い回り方で混ぜ（菫と紅藤は蘭色で出会う）、離れた色どうしはそれぞれの淡い光へ抜けて白に近い継ぎ目で出会う（灰色にも虹にもならない）。各放射面は6本の細い帯に分けて、その角度の色で塗る。例：目標画像に近い割合なら、左に青、上に紫、右に赤、下に橙。
- **光**：各放射面は、広い白熱の中心（中心から0.12R まで白70%以上、0.42R で0）→ 淡い光 → 飽和した中間 → 深い縁（茶色にしない）の放射グラデーション。中心に白い星、主の稜線は色と無関係の白線（中心で α0.55、冠で0.30。副稜線は0.26→0.08）、左上に鋭いスペキュラの筋、ガードルの内側に加算の分散光（右は暖色、左は寒色）と冠の中の小さな火（単色の核では数を増やす）。SwiftUI 側で、ガードルに沿う白寄りのブルーム（割合の色＋白74%）を足す。
- **単色の核**：区画ごとに色相を±4°、半分ごとに+7°・明度0.86倍で揺らし、左上からの光で±7%の明暗を付ける。平らにならない（テストで、隣り合う面の輝度差と中心の白さを固定）。
- **大きさ**：直径＝min(96pt, 瓶幅×0.24)（誕生時）→ 0.25（二段目）→ 0.26（三段目以降）（ラウンド5：0.22／0.24／0.26 から）。上限96pt で、動く粒を覆わない（EA:L560）。帯が足りないときだけ、石は少し譲る（下の「配置」）。
- **色**
  - テーマ別の割合で塗る（上位5テーマ＋その他、5%刻みで量子化）。並びは色相の順（上の「色の場」）。テーマが1つの人は単色。
  - **単色でも**、面ごとに明度と色相を±8°揺らし、暖色と寒色のリムを入れ、ガードルに光学的な分散光（§7.7 の α）を出す。
  - 固定の虹色パレットや、時間とともに色相が回る表現は使わない。「虹」はレアの名前として予約済みなので使わない。
  - ハローは左右2つのローブ（ラウンド5）：左半分と右半分の平均の色（彩度0.45〜0.75、明度1）に auroraViolet を10%混ぜた色で、それぞれ中心から0.16R ずつ外へずらした楕円、α0.45 から1.35R あたりで0。広いブルームは全体の平均色、α0.24。
  - **割合の出どころ（正直な呼び方）**：v1.2〜v1.4 は `HomeView.lifetimeCoreColorHex` と同じ重み（root の grams×colorMix ＋ loose の grams）を使う。まとまり粒の colorMix は**粒数で重み付け**されているので、25分中心の人ではほぼ正確、長さが混ざる人では近似になる。そのため UI と VoiceOver では「テーマの割合（おおよそ）」と呼び、「質量配分」とは書かない。v1.5 で、テーマ別の生涯グラムを ModelActor の裏計算＋epoch 指紋付きキャッシュで正確に出し、キャッシュが無い間は近似に戻す。
- **軌道**：既定の直径は核の2.15倍。銅色 #D9967A の破線（α0.55、1pt、[3, 5]）。
  - **配置（ラウンド2）**：HUD の下端は推定値ではなく、レイアウト後の実測（`onGeometryChange`、瓶カードの座標系）を使う。「軌道の列（石・軌道・◇）」は瓶の奥（SpriteKit の背面）にあるので、HUD の下端＋8pt から床のジェムベッドの上端−6pt までに収める。「ラベル（名前＋進みのカード）」は HUD と同じく SpriteKit の手前に描き、軌道の列のすぐ下に置く。下端は、ラベルの真下で静止している宝石の上（少なくとも床の宝石1段ぶん、約30pt。記念石や結晶が下にあればその上）までとする。ラベルは軌道の内側に入れず、ベッドにも宝石にも重ならない。山が高く、軌道の列とラベルをその上に収められなくなったとき（核そのものが埋もれたとき）だけ、ラベルも核と一緒に奥へ下がる。ラウンド6（D4 の大きな宝石）からは、奥へ下がったラベルは描かない（大きな宝石の隙間から文字の切れ端だけが見えるため。レイアウトのための寸法は測り続け、VoiceOver は瓶を1つの要素として読むので変わらない）。山の高さは、瓶の幅を12列に分け、止まっている粒だけで0.5秒ごとに測る（落下中や弾んでいる粒は数えない）。
  - **ラベルの下端（ラウンド5）**：ラベルは手前に描くので、ベッドの欠片が半透明のカード越しに透けて11pt の文字を読みにくくしていた。下端は「床の宝石1段ぶん」「ベッドの上端−6pt」「ラベルの真下で静止している粒の上端−6pt」のいちばん上にする（`JarLifetimeCoreLabelLimits`）。カードの地は α0.94（コントラストを上げる・透明度を下げるでは0.98）。
  - 帯が足りないとき（大きな文字、低い瓶、高いベッド）は、①追加の軌道の間隔を詰める → ②軌道を石に近づける（◇が石に重ならない範囲） → ②' 石が少し譲る（0.72倍まで。◇を残すため） → ③◇を隠す（輪と灯った弧は残し、輪を残すために石がもう一度0.72倍まで譲ることがある） → ④軌道を隠す、の順に譲る。⑤石だけでも入らない帯では、石を0.65倍まで縮め、それでも入らなければ核は埋もれたとみなしてラベルを奥へ下げる（`JarLifetimeCoreLayout.overflows`。以前は石が帯からはみ出してベッドや床に重なっていた）。
  - iPhone 12 mini（375×812pt）の標準の文字サイズの 3.75kg では、軌道と◇が残り、石が少し小さくなる（ラウンド5で専用の Simulator「PomoGem GemBrilliance Mini」で撮影、`fix-mini-home-375kg.png`）。ラウンド6の大きな宝石では、12 mini の 3.75kg から山が核のすぐ下まで来て、ラベルは埋もれて描かれない（`gem-shots/r3/jar-mini-home.png`。HUD を瓶の外へ出す D5 が保留の間の制約）。計算上、帯が約94pt なら石は約0.85倍、ラベルの下に粒が2段たまった約74pt の帯では◇が隠れ、輪は石を約0.74倍にして残る（`testShortStageKeepsTheOrbitMarkersByShrinkingTheStone`）。
  - ◇（ラウンド5）：灯った◇は今までどおり二重の菱形（外12pt、内5pt、白い線）。**まだ灯っていない◇は外8pt・内3.5pt、α0.25** に下げ、等間隔の文字盤（ルーレット盤）に見えないようにした。意味と個数は変えない。コントラストを上げる設定では、灯っていない◇も12pt のまま濃く描く。
  - 瓶が表示枠より低いとき（AX サイズ、大きい画面）は、HUD を瓶の口に合わせて下げ、巡のピルや首と重ならないようにする。◇は二重の菱形（外12pt・線1pt、内5pt）。灯った◇は割合の色で塗り、白い線と静的な光（5pt）。灯った区間の弧は実線（α0.8）。
  - v1.2：◇の意味は既存どおり（`litOrbitSlotCount`）。見た目だけ変える。
  - v1.3：◇を一里塚（10時間）に読み替え、外側に「100時間の星」の軌道（10枠）、頂点に「1,000時間の冠」、その上に「ゾウ」の小さな印を加える（D7、§5.2）。
- **描画**：v1.2 は SwiftUI のまま、（量子化した色の割合, 段, 表示倍率）をキーにした静止画を一度だけ焼き、拡大縮小して表示する（毎フレームの描き直しなし）。倍率は `UIScreen.main` ではなく、表示するビューの `displayScale` を渡す（SpriteKit 側は Home が最初の復元の前に `displayScale` を設定し、SKView の窓の画面の倍率は予備）。キャッシュに無い核（と透明な器）は、ラウンド5からメインスレッドの外で焼き、焼き上がるまで直前の絵を表示したままにする（`HeroArtworkImage`）。呼吸は、ぼかしの無い層にだけ scaleEffect 1.0↔1.02（4秒）を付ける。Reduce Motion と UI テストモードでは止める（既存）。v1.5 で SpriteKit のスプライト（背面ガラスと粒の間、物理なし）に移すかを判断する。
- **ラベル**：核の下に「時間の核」「時間の核・二段目」などの名前だけ（11pt、濃紺のカード α0.94。Dynamic Type に1.3倍まで追従）。重さの数値は HUD と重複するので出さない。核の部分をタップしたとき（その点に宝石が無い場合だけ）に、内訳（生涯の時間・重さ・次の段）を出す。VoiceOver では瓶の「内訳を見る」アクションに含める。
- **共有画像への合成（v1.2、ラウンド5で実装）**：Home は、いま見せている核（割合・段・直径。2.5kg 未満は透明な器と灯った面の数）を `JarScene.shareCore` に渡す。`JarSnapshotter` の共有用の書き出し（`.share`）は、瓶の舞台の光（`JarStageArtwork`）→ 核（ブルーム・左右のローブ・ガードルのブルーム・静かな軌道の輪・同じ焼き込みの石。`JarShareCoreArtwork`）→ 撮影した瓶、の順に重ねる。共有カードには HUD が無いので、核は瓶の上から42%の高さに置く。ウィジェットの書き出し（`.widget`）は今までどおり瓶だけ。
  - GIF：平らにした書き出しの上で、核が1.00↔1.04で呼吸し（光も一緒に膨らむ）、ガラスの上を光の帯が1回横切り、いちばん高い宝石の上で星が順に光る（`ShareJarMotionLayer`。粒が核に重なっている瓶では、石は描き直さず光だけが呼吸する）。静止画は同じ位相で止めた1枚。書き出した GIF（450×563、8コマ）の隣り合うコマで、画素の3%以上（8/255）が変わった割合は7.2〜12.4%（ラウンド3は画素は違っても目では同じだった）。

**見た目の確認（ラウンド5、v4）**

- `cmp-core.png`（どれも120pt 四方）：v4 は左に青、上に紫、右上に紅藤、右下に珊瑚の大きな色の場になり、白熱の中心も広い。ビーチボールや色相環には見えにくくなった。home の fixture は緑が7%あるので、左下に緑→青緑の細い帯が出る（割合どおり）。目標画像はさらに淡く白っぽい（D3 の範囲内の調整として、主観の確認は残る）。
- 見えている面の直径：main 約48pt、v3 約63pt、v4 約69pt、目標画像 約75pt（iPhone 17 Pro の 3.75kg）。
- 2.5t では、ベッド（60pt）の上にラベルを置くようになったぶん帯が狭く、石が少し譲って2本の軌道と◇を残す（撮影画像から読んだ見かけで約0.87倍。`cmp-heavy.png`）。ラウンド3より核は小さく見えるが、ラベルがベッドに重ならない。

**見た目の確認（ラウンド3、ブランチの HEAD を Simulator で撮影）**

- 同じ 3.75kg の瓶で、main・HEAD・目標画像の核を、どれも120pt 四方で並べた。main の核は、色の扇形を白い縁で囲んだ平らな多角形で、光はほとんどない。v3 は、白熱した中心の星から面が放射し、ガードルの白い縁と周りのブルームがあり、目標画像と同じ「光る放射状のブリリアント」として読める。
- 目標画像との差：目標は扇形が大きなまとまり（左に青、右に赤橙）で、白く飛んだハイライトが多く、外周の暖色の光が強い。v3 は割合の色がそのまま20枠に並ぶので、彩度が高く宝石寄りに見える。どちらに寄せるかは、オーナーまたは知人による主観の確認が要る（D3 の範囲内の調整）。
- 見えている面の直径は、main が約48pt、v3 が約63pt、目標画像が約75pt（画面幅を同じ pt に換算。撮影画像から読んだ値）。軌道・◇・「時間の核」の名前と進みのカードは、標準と xxxL（ラウンド3の撮影）、AX3（ラウンド2の撮影）で HUD と重ならない。

### 7.9.1 ジェムベッド ―「積み上がりの光」の結晶化（ラウンド2、D25：採用。ラウンド6で「光の堆積」に再調整）

3.75kg でも瓶の中は6体ほどで、融合のあとはさらに空いて見える。十進の融合で体数が少なく保たれるのは設計どおりなので、体数は増やさず、既存の「積み上がりの光」（EA §3.2 いまの瓶）の見た目を、物理ボディの背面に敷く小さな結晶片の層に作り替えた。

- **入力**：生涯の学習グラムと、生涯の質量加重テーマ割合（核と同じ扇形）だけ。体数・十進の桁・レア・融合のタイミングは入力にしない。10体が1個に融合しても、ベッドは1ピクセルも変わらない（テストで固定）。Screen Time の黒い石は学習グラムもテーマも持たないので、ベッドには入らない。
- **大きさ**：250g までは今までどおりの光だけ。250g を超えると、1粒ぶん（250g）から上限（250g×100万）までの6桁を対数でとり、その平方根で高さを決める（3.75kg≈上限の44%、250kg≈71%、2.5t≈82%）。上限は瓶の内側の高さの18%で、それより上には何も置かない。単調に増え、減らない。
- **描画**：高さを24段に区切り、段ごとに1枚のテクスチャに焼く（スプライト1枚）。欠片の位置は（行, 列）だけで決まるので、段が上がっても下の欠片は動かず、上に足されるだけ。
- **見た目（ラウンド5で「光の層」に描き直し）**：4.5〜8pt（7個に1個は8〜10pt）の多角形の欠片（本物の宝石の約20pt より小さい。ラウンド3は最大12.5pt）。色は扇形の20枠の色相のまま、彩度0.30〜0.45、明度は0.62以上（面の明暗は±10%）。下30%は床に溜まった暖かい光で持ち上がり（明度+0.10、暖色へ少し寄せる）、上25%はやわらかく光に溶ける。欠片の隙間も、暖かい光（下）→ 扇形の穏やかな光 → 透明の地で埋め、暗い床を見せない（ラウンド3の「下ほど濃紺の陰」はやめた）。静止した小さな星は約14個に1個。山の光はベッドより奥に置いたので、ベッドの片側だけが桃色に染まらない。ハロー・グリント・金色・丸いコインの形は使わない。
- **見た目（ラウンド6の再調整：光の堆積）**：手前に大きな宝石が来るので、ベッドは「砂利」や「ラメ」ではなく、柔らかく光る堆積に見えるよう、コントラストを下げた。欠片は2.8〜5pt（10個に1個は5〜6.5pt）に小さくし（行の間隔3.6pt、列5.2pt）、面ごとの明度差を±10%から±4%に、彩度を0.22〜0.34に、光を向いた面の白の混ぜを30%から12%にした。欠片の輪郭線と白い細線はやめ、α は0.95から0.78に下げ、上から明るいベール（下 α0.22 → 上0.06）を重ねて1枚の光の層にした。静止した星は14個に1個から48個に1個に減らし、小さく（1.2＋0.15×大きさ）、淡く（α0.40）した。焼き込みは表示倍率の半分（3倍の画面で1.5倍）にして、手前のくっきりした宝石の奥で少しぼけて見えるようにした（テクスチャの画素は約1/4）。入力・単調性・上限18%・暫定の投影で下げない保持・物理を持たないことは変えていない（下の不変条件）。
- **本物の粒との区別**：本物の宝石は、白いエッジ（外周 α0.75・1pt）・接地影・ハロー・グリントを持ち、ベッドより明るく（macOS のラボで同じ焼き込みを並べた帯の平均輝度で、本物の粒 約0.58、ベッド 約0.47）、ベッドより大きい。
- **位置**：床の光と山の光の上、物理ボディの下（通常のαブレンド、z はストラタ＋0.55。山の光は＋0.52）。当たり判定を持たず、`JarSnapshotter` の書き出しにもそのまま写る。
- **焼き込み（ラウンド5）**：キャッシュに無い段は、メインスレッドの外（userInitiated のキュー）で焼き、焼き上がるまで今のベッドを表示したまま、テクスチャ・大きさ・位置を同時に差し替える。
- **実測の高さ**（Simulator、瓶の内側の高さ398pt、上限72pt）

| 生涯の重さ | 段（24段中） | 高さ |
|---|---|---|
| 250g 以下 | 0（表示しない） | 0pt |
| 3.75kg | 11 | 33pt |
| 29.25kg | 15 | 45pt |
| 251kg | 17 | 51pt |
| 2.5t | 20 | 60pt |
| 250t（上限） | 24 | 72pt |

- **コスト**：スプライト1枚（ノード+1、描画は約+2）。焼き込みは段ごとに1回だけ（3倍で約0.4〜0.9MB）。対数で育つので、段が変わるのはまれ（ただし新しい人は、扇形の5%刻みの色が変わるたびに焼き直す）。

**守る不変条件**（主に `GemBrillianceTests` で固定。テストとの対応は各項目の末尾）

1. 入力は、生涯の学習グラムと生涯のテーマの割合だけ。体数・十進の桁・レア・融合のタイミング・Screen Time の黒い石・課金の状態は入らない（`testGemBedFollowsLifetimeGramsMonotoneAndCapped`、`testFusionAndObstaclesDoNotChangeTheGemBed`）。
2. 融合（10体→1体）や黒い石の追加の前後で、テクスチャ・大きさ・位置・α・表示状態が変わらない（`testFusionAndObstaclesDoNotChangeTheGemBed`）。
3. 生涯グラムの単調関数。グラムが増えて低くなることはない（本人が記録を消してグラムが減ったときだけ、§9.1 のとおり正直に低くなる）。250g 以下と負の値では表示しない。内側の高さの18%を超えない（`Int.max` でもちょうど24段）。**投影が暫定のあいだ（CloudKit の確認待ち、またはローカルの下限表示）は、表示中のベッドより低くしない**（暫定の投影は店にあるグラムより少なく数えるため。確認済みの投影にはそのまま従う。`JarGemBedPresentation.displayed`、`testGemBedHoldsWhileTheProjectionIsProvisional`）。保持は Home の瓶（`JarScene`）の中だけで、アカウントを切り替えると画面ごと作り直されるので、別のアカウントのベッドが残ることはない。
4. 物理ボディを持たない。当たり判定・容量の計上（`capacityUnits`）・融合の閾値・口の下の余白（`pileHeadroomFraction`）には入らない（物理ボディが無いことは `testFusionAndObstaclesDoNotChangeTheGemBed`。余白は最悪ケースの撮影で確認：ラウンド5で16／17／16%）。
5. 物理ボディより奥（z はストラタ＋0.55）で、通常のαブレンド。加算の光・ハロー・グリント・金色・コインの形は使わない（§7.14。`testFusionAndObstaclesDoNotChangeTheGemBed`）。
6. 欠片の位置は（行, 列）だけで決まる。段が上がっても下の欠片は動かないので、「減った」「並び替わった」とは見えない（コードの構造で保証。テストは無い）。
7. `JarSnapshotter` の書き出しにも同じ見た目で写る。もともと静止しているので、Reduce Motion でも変わらない（撮影で確認：`fix-gallery-snapshot-export.png`）。
8. 焼き込みは、表示するビューの倍率で行う（`UIScreen.main` は参照しない）。倍率が丸められたとき（NaN、4 など）も、丸めた倍率で焼き直す（`testClampedArtworkScaleStillRebakesTheBed`）。新しい段はメインスレッドの外で焼き、前のベッドは焼き上がるまで残る（`testGemBedBakesOffTheMainThreadAndKeepsThePreviousBed`）。

**実測（ラウンド5、撮影画像の輝度。Rec.709、0〜1）**：3.75kg のベッドだけの帯で平均 L 0.307（ラウンド3）→ 0.472、彩度 0.32 → 0.28、茶色に濁った画素 1.8% → 0%。約2.5t では右側だけ暗い紫と茶色に沈むことがなくなった（`fix-bed-zoom.png`）。

**残る課題**：3.75kg ではベッドは33pt で、目標画像の山（瓶の約25〜30%）には届かない。対数で育ち上限を持つ仕様なので、序盤は薄いままになる。構図の差は D4（瓶全体の適応倍率、§7.5）で大きな宝石が埋めるようになった。HUD を瓶の外へ出す D5 は保留のまま。

### 7.10 モーション

共通のルール：同じ場所の点滅は1秒に1回まで。**点滅・閃光する部分**は画面の5%以下（WCAG 2.3.1 は点滅についての基準）。静止した明るさは別の予算で見る（§7.14 の5）。

| 場面 | 動き | Reduce Motion のときの代替 | リリース |
|---|---|---|---|
| 傾き | 決まった面（グリントの窓）が順に光る。期待同時点灯数は約4個 | 静止した星（各宝石の頂点1つ、α0.42。ラウンド9から控えめの光の倍率0.7を掛ける） | v1.2 |
| 自発のきらめき | §7.6 の上限。「標準」のときだけ | なし | v1.2 |
| 色が入る（完走） | 面ごとに40msずつ遅らせて色を注ぐ（合計500ms以内）→ 白い一閃160ms | 200msのクロスフェード | v1.2 |
| 着地 | ハロー α0.35→0.95（80ms）→0.35（360ms）。頂点の星（0→1.25→0、280ms）。光の粒6〜8個（520ms）。床の光+12%（400ms） | ハローの明るさを150msで切り替えるだけ | v1.2 |
| 融合 A0（g<2.5kg） | 既存の収束（0.52秒）→ 新しい粒が200msでフェードイン。閃光・リング・破片なし | 200msのフェードアウトとフェードイン | v1.2 |
| 融合 A1以上 | 光の糸で結ぶ（0〜320ms）→ 収束（320〜560ms）→ 白い閃光（2.2R、160ms）と衝撃リング（1R→3R、420ms）→ 新しい結晶がばねの動きで現れる（0.6→1.08→1.0、380ms）。A2以上は破片12個。全体1.1秒以内 | 200msのフェードアウトとフェードイン、静止したリングを600ms | v1.2 |
| 巡の満杯 | 光が瓶の縁を一周（700ms）→ 巡ピルが増える → 光が底ガラスに年輪として沈む（合計2.2秒）。大きく見せるのはその日最初の満杯だけ | 静止した縁の光を1.2秒 → クロスフェード | v1.3 |
| 一里塚 | ◇に光が走って灯る（600ms）。10個目は◇が集まって外側の星になる（1.2秒） | クロスフェード | v1.3 |
| 核の誕生 | 各粒から中心へ淡い光（24個まで）→ 透明な器が1面ずつ色で満ちる（60ms×10）→ 分散光のリング → 中心の星 → 軌道を描き、最初の◇枠が現れる（2.4秒） | 400msのクロスフェード | v1.3（v1.2 は既存の演出の見た目だけ変える） |
| 核の段の上昇 | 面のクロスフェードと新しいリング、首カラーに刻線（1.5秒） | クロスフェード | v1.3 |
| 名所カード | シルエットの中に、自分の宝石の面が満ちていく（中1.2秒、大2.4秒） | 静止したカード | v1.3 |
| 月の封 | 光の帯が小瓶に写る → 縮図が沈む → 蓋が閉まる → 棚へ（初回4秒以内、以降1.5秒） | クロスフェード | v1.5 |
| 年の星 | 核から星が昇り、前の年の星と線で結ばれる（3秒） | クロスフェード | v1.5 |
| ゾウ1頭ぶん | 瓶 → 棚 → 地平へ、一度だけカメラが引く（3秒） | クロスフェード | v1.3 |
| タップ | 既存の衝撃と光に加えて、その粒の星が一度光り、内訳チップを1.5秒出す | 既存のハイライトと内訳チップ | v1.3（内訳チップ） |

控えめ（D17）のときの着地・融合・融合シートの短い版は §7.6 の表のとおり。Reduce Motion のときは、この表の代替（動かない）がそのまま使われ、ハローなどの光は控えめの倍率になる。

**演出が重なったとき**

- 大きな演出は、完走1回につき1つまで。優先順は「核の段 ＞ 名所 ＞ 一里塚 ＞ 融合 ＞ 巡」で、残りは完走カードの1行チップに回す。
- 25分中心の人の10本目（融合と核の誕生が重なる）は、融合を前奏にした1つの振り付けにする。合計5秒以内で、タップでスキップできる。
- 月の封が節目と重なったら、次の完走に送る。

**録画で確かめたこと（ラウンド3、ブランチの HEAD、Simulator）**

- `fusionfx` の録画（60fps）では、10粒目が山に触れてから約80msで10粒の収束が始まり、+611ms で白い閃光と衝撃リングとともに×10 が生まれ、約+1.1秒で落ち着く。コマは毎回違い、ラウンド1の「同じ画素のコマ」の問題は録画では起きない。
- **不具合（ラウンド5で修正）**：完走の粒が瓶の口から落ちている約1秒の間、山の光（`jar.pileGlow`）が瓶全体に広がり、核と HUD の周りが桃色の霞に覆われていた（main では起きない）。`JarScene.refreshPileLight()` が落下中の粒も含めた範囲から光を決めていたため。止まっている粒だけで範囲を取り、床に座る帯に収め、0.4秒で補間するように直した（§7.6）。本物の Home で完走の粒が口から落ちる途中を撮り直し（`fix-drop-a/b/c.png`）、霞が出ないことを確かめた（`testFallingGemNeverStretchesThePileLight` でも固定）。
- 本物の Home の融合では、main と HEAD のどちらでも、×10 が生まれる前後に100〜330ms の間コマが更新されない区間がある（main から続く動き。永続化や融合シートの準備と重なる時間帯）。実機での確認が要る。

### 7.11 触覚と音

- 音は**コードで生成したPCMだけ**を使います（音声ファイルは同梱しない。OSSチェックの前提）。音と触覚は別々にオフにできます（既存）。
- 着地は既存のガラスの音。核の誕生はガラスの和音を1つ。名所は単音で、重いものほど低い音にする（段階的に高くなるファンファーレにはしない）。一里塚は短い澄んだ音。月の封は小さな「カチン」。
- 触覚は `.soft` を1回まで。連打しない。
- 使わない音：コインの金属音、ジャックポット風の音、上昇していくアルペジオ。

### 7.12 アクセシビリティ

- **Reduce Motion**：§7.10 の代替を使う。物理の挙動は同じまま（既存のテストを維持）。PebbleNode と aura にはアクションを付けない。ラウンド9から、Reduce Motion は演出の強さ「控えめ」を含む（D17、§7.6）：ハローは0.7倍、静止した星は α0.42 になる。設定画面では、Reduce Motion がオンのあいだ「いまは控えめで表示しています」と添える。
- **Reduce Transparency**：SpriteKit 側は今まったく対応していないので、新しく対応する。ハロー×0.45、玉ボケと床のきらめきを弱め、エッジを強める。
- **コントラストを上げる**：エッジの α を+0.2、HUD の文字を白100%、カードの背景を α0.98 にする。
- **VoiceOver**：瓶の読み上げに次の目標を含める（例：「3.68kg、15粒。つぎの一里塚は10時間、あと3時間52分」）。名所カード・小瓶・星にも読み上げ文を付ける。
- 色だけで意味を伝えない（段は刻線の本数とカットの形で、テーマは名前でも分かるようにする）。
- **Differentiate Without Color（色を使わずに区別、ラウンド7で実装）**：設定がオンのとき、テーマごとの粒に小さな刻印（`GemThemeMark`）を入れる。オフのときの見た目は1画素も変えない（焼き込みの仕様とキャッシュのキーが同じ）。
  - **12の図形**を `SubjectPalette` の位置で割り当てる：●（朱色）、▲（瑠璃）、■（紅藤）、★（緑青）、✱（菫）、＋（琥珀）、☾（青緑）、✦（珊瑚）、▬（若草）、２つの点（藍）、♥（赤銅）、しずく（空色）。ばらの粒は物理で転がり、刻印も一緒に回るので、回しても見分けられる形だけを使う（正方形とひし形、＋と×、▲と▼のような、回すと同じになる組は入れない）。色が近い組（琥珀と赤銅、菫と藍、瑠璃と空色）には、形が大きく違う図形を当てた。
  - パレットの外の色（以前の色相の回転で作られたテーマなど）は、色相を12に分けた位置の図形を**輪で囲んで**描く。パレットのテーマと取り違えず、旧い色どうしも別の図形になる。灰色は16進から決まる位置を使う。
  - 描き方：図形を濃い墨（α0.84）で彫り、ふちに白い明るい縁（α0.84、図形の半径の30%の線）を付ける。明るい琥珀の上でも暗い藍の上でも読める。
  - **大きさ**：1色の粒は台座の中央に半径0.40R（20pt の粒で8pt の図形）。複数のテーマのまとまり粒は、各テーマの扇形の真ん中、中心から0.58R に半径0.23R（×N の札と重なりにくい位置）。それでは図形が7pt 未満になる小さな結晶（Overview の軌道の結晶など）は、いちばん多いテーマの図形を台座に1つだけ入れる。時間の核は、各テーマの弧の真ん中、中心から0.56R に半径0.105R。その他（6位以下を混ぜた弧）には入れない。記念石・チュートリアルのガラス・黒い石には入れない。
  - **範囲**：瓶のばらの粒とまとまり粒（焼き込みに入るので、ノードも描画回数も増えない）、時間の核（Home・Overview・共有の核）、共有カードの粒と結晶、融合シート（待っている無色の枠には入れない）、Overview の結晶・時間の核・「いま」の宝石。設定を切り替えると、瓶は通知を受けて体をすべて焼き直し、アイドルで止まっていれば1フレームだけ描き直す。SwiftUI の絵は環境値で描き直す（共有カードの書き出しのように画面の外で描く場合は、システムの設定も読む）。
  - **テスト**：パレットの全色が別の図形になる／パレットの外の色は輪で囲まれ、パレットの図形と重ならない／オフでは焼き込みのキーが変わらない／20pt の粒で台座に深い彫りが入り、台座の外は変わらない／複数テーマの図形はそれぞれの扇形の中にある／核の「その他」には入らない／瓶の切り替えは学習の粒とまとまり粒だけに効く。
- 文字サイズ AX5（瓶が520pt）では、HUD は内側のまま（既存の `largeTextFusionProgressCard` を使う）。

### 7.13 パフォーマンス予算（A14、60fps、1フレーム16.7ms）

| 項目 | 予算・方針 |
|---|---|
| 描画回数（典型：30体） | 約50 |
| 描画回数（最悪：学習系128体＋黒い石36体＋記念石12体） | 約140 |
| SpriteKit の GPU | 1.2〜1.8ms |
| CPU（物理を含む） | 3〜5ms |
| 使わないもの | SKEffectNode、CIFilter、毎フレーム描き直す Canvas や TimelineView、ぼかした層の無限アニメーション、常時のエミッター、毎フレームの名前検索（`childNode(withName:)`） |
| テクスチャの鍵 | UUID は「4変種の選択」と「グリントの位置」にだけ量子化して使う。向きはスプライトの回転に任せる（1粒ごとに1枚にはならない）。サイズは1pt（16pt 未満）／2pt（16pt 以上）刻みの区分 |

**実装（ラウンド3、描画の衛生）**

1. **毎フレームの名前検索をやめた**：`PebbleNode.updatePresentationLighting` が、すべての粒について毎フレーム `childNode(withName: "obstacle.count")` を呼んでいた。点を含む名前は SpriteKit がクラス名としても調べるため、呼ぶたびに Swift ランタイムの型情報を走査する。Simulator の stress では、メインスレッドの93%がここだった（2.7fps）。数字のラベルはノードを直接持つようにした。見た目は変わらない。
2. **描画のまとめ（バッチ）**
   - `SpriteView` に `.ignoresSiblingOrder` を付けた（`.shouldCullNonVisibleNodes` と `.allowsTransparency` は従来どおり）。
   - 同じ z の順序は SpriteKit が決めなくなるので、粒ごとに「入った順」の小さな z のずれ（`JarZPosition.stackingStep`＝0.00001、最大0.04）を与えた。1粒の中の層の間隔（最小0.05）より小さいので、影・ハロー・本体・陰・キーライト・グリント・札などの層は、瓶全体でそれぞれ1本の帯になり、帯の中の重なり順は以前のノードの木の順と同じになる。首カラーの3状態も、以前の子の順を z で明示した。層の順序と帯はテスト（`JarRenderingPerformanceTests`）で固定した。
   - 共有の光（ハロー、グリント、接地影、リグのキーライトと陰）と、焼いた本体（宝石の面と黒い石）を、実行時に作る1枚のアトラス（`GemTextureAtlas`、`SKTextureAtlas(dictionary:)`）に入れた。同じ合成方法の帯が続くと1回の描画にまとまる。新しい名前はまず単体のテクスチャで即座に描き（空白にならない）、0.25秒まとめてから別スレッドで作り直したページに差し替える。差し替えても画素は同じ（テストで最大差2/255以内、実測0）。SpriteKit のパッカーは透明な縁を切り詰めて輪郭の最外周の半画素を落とすため、各画像の四隅に alpha 1/255 の画素を置いて切り詰めを止めている。
   - 当初案の「色ごとに1枚、作り直さない」は採らなかった。粒ごとの積み順を守ると帯の中で色が混ざるので、ページが色ごとに分かれていると本体の帯が色の切り替えごとに分断されるため。
   - 黒い石（輪郭・6つの面・ひび＝8個のシェイプノード、1個あたり約15回の描画）を、同じパスで Core Graphics に焼いた1枚のスプライトにした。形の種類（段・枠）ごとに1枚で、数字のラベルだけは正立させるため従来のラベルのまま。テストで、以前のシェイプノードとの差が縁のアンチエイリアスだけであることを確かめている。
   - 休止中は透明になっているタップの光と Reduce Motion のハイライトを、非表示にした。
3. **アイドル時の描画（ラウンド8で描画ループごと止める。実機所見 jar-01）**：瓶が落ち着くと、既存のアイドル停止で物理（scene）が止まる。止まった scene はノードが変わらない限り描かれない（0fps）が、以前は SKView の描画ループ（表示リンク）が動き続け、フレームを出さないまま毎秒60回メインスレッドを起こしていた（実機 iPhone 12 mini で、30秒あたり約0.63秒のメインスレッドの時間）。ラウンド8から、止まった瓶では **SKView の `isPaused` そのものを止める**（`JarScene.isRenderLoopPaused`）。
   - `SpriteView(isPaused:)` には結び付けない。この引数は SwiftUI がビューを作るときにしか SKView に届かず、後から値を変えても SKView は止まらないことを計測で確かめた（`SpriteView(shouldRender:)` も毎フレームは呼ばれない）。そこで scene が自分の `view` の `isPaused` を直接切り替える。
   - SKView は自分を再開するとき scene の `isPaused` も外す（計測）。光だけを描き直すあいだも物理が1歩も進まないよう、同じターンで scene を止め直す。
   - 落ち着いた瞬間は、落ち着いた絵（星の静止、山の光）を描くために0.25秒だけ描画ループを回してから止める。
   - **物理ごと起こすもの**（同じターンで描画ループも戻す。従来の `resumeSimulation()`）：着地と落下の予約、融合、タップ・揺らし・VoiceOver の操作、中身の入れ替え（履歴の同期、記念石の入れ替え、Screen Time）。
   - **物理は起こさず、光だけを描き直すもの**（描画ループを0.25秒回す）：閾値を超えた傾き（0.75秒）、Reduce Motion と演出の強さの切り替え、Reduce Transparency、月のラベル、首カラーの刻線、ジェムベッドの差し替え（別スレッドの焼き上がりを含む）、舞台の大きさの変化、新しい SKView への表示（Home に戻ったとき）、前面に戻ったとき（scenePhase が active）、共有・ウィジェットの撮影。`texture(from:)` は止まった SKView でも撮れることを確かめた。撮影後に描き直すのは、画面を撮影後の状態に合わせるため。
   - 傾き：止まっている間は入力側で間引く。平滑化した傾きが最後に反映した値から0.015を超えて動いたときだけ反映し、多くても毎秒30回にする。反映すると、描画ループと全速のモーション（6.）を最後の変化から0.75秒保つ（ゆっくり傾ける途中で速さが切り替わり続けないため）。手に持って静止しているときのセンサーの揺れ（±0.012）は何も描かず、描画ループも止まったまま。意図して傾けると即座に光る（揺らし±0.42で毎秒15〜25回）。止まっている間に水平に戻すと、光は閾値（0.015）以内まで戻る。閾値で見送ったサンプルは、瓶が起きた瞬間と、止まる瞬間に追いつき、`resetGravity()` では正確に元の位置へ戻る（ラウンド5：以前は0.01未満の差のリセットで、わずかにずれが残っていた）。起きている瓶は、従来どおりすべてのサンプルに追従する。
4. **焼き込みで最初のフレームを止めない**：起動時（`RootView`）に、よく使う組み合わせ（最初の5テーマ×25分・50分の計測と30分の自己申告×4変種＝60個、3倍で約2MB）を別スレッドで焼く。瓶の復元では、足りない本体だけを全コアで並列に焼いてからノードを作る（同期の焼き込みは不足分だけ。テストで、復元の後にノードがその場で焼いた数が0であることを固定）。
   - **起動直後の復元と先焼きの競合（ラウンド5）**：先焼きがまだ動いている間に復元が来ると、同じ本体を二重に焼いて全コアを取り合っていた。復元は先焼きに新しい焼き込みを始めさせず、動いている分（1コアに1枚、数ms）だけ待って、出来上がった画像を引き取ってから、残りを自分で並列に焼く（`testRestoreTakesOverTheLaunchPreBake`：どの画像も1回だけ焼かれる）。
   - **倍率**：Home は最初の復元の前に `displayScale` を瓶に渡す。以前は、2倍の端末で最初の復元が3倍で焼かれ、先焼きの2倍の画像が当たらず、メモリも2.25倍になり得た。
   - **保持する画像**：CPU 側の画像を24MB（LRU、共有の光は固定）で上限を切る。**これは CPU の写しだけの上限**で、GPU 側のページ（ほぼ同じバイト数）と、まだページに入っていない単体のテクスチャが別にある。合計は保持量の約2〜3倍（`Statistics.residentBytes` と、Debug のフレーム記録の「resident」）。いま画面に出ているスプライトの画像は、最後に引いた時刻が古くても追い出さない（以前は画面に残る本体から追い出され、その粒が古いページを使い続けてバッチから外れ、古いページも解放されなかった）。
5. **核とベッドの焼き込みもメインスレッドの外へ**（ラウンド5）：核 v4（576px 四方）と透明な器は、キャッシュに無ければ別スレッドで焼き、焼き上がるまで前の絵を出したままにする。ジェムベッドも同様（§7.9.1）。新しい人は割合の5%刻みが動くたびに核とベッドを焼き直すので、これが Home の最初の表示と完走のたびの引っかかりを避ける。
6. **モーションの間引き（ラウンド8。実機所見の Core Motion。§11 の D2 とは別）**：以前は、Home が見えていて粒が1つでもあれば、瓶が落ち着いた後も傾きを毎秒30回メインスレッドで受けていた（実機で30秒あたり、モーションのスレッド113ms＋受け手21ms）。ラウンド8から3つの速さを切り替える（`JarMotionRate`、`JarMotionObserver`）。

   | 速さ | いつ | どこで受けるか |
   |---|---|---|
   | 全速（毎秒30回） | 瓶が起きている間。傾きが光を動かした後と、揺らしの1つ目の山の後の0.75秒 | メインキュー。従来どおり、すべてのサンプルを重力・光・揺らしの検出に使う |
   | アイドル（毎秒5回） | 瓶が落ち着いている間 | 背景の OperationQueue。`JarIdleTiltMonitor` が平滑化した傾きを持ち（毎秒30回の平滑化0.16を毎秒5回に換算した0.65）、描いている光から0.015を超えて動くとき、または揺らしの山（0.9g 以上）のときだけメインへ移る。移ると全速に戻り、光が動けば描画ループも戻る |
   | 停止 | Home が見えていない（アプリが active でない、シートが覆っている、集中画面）、瓶に学習の粒が無い（黒い石や記念石だけ）、モーションが無効 | ― |

   - アイドルの間に見た最新の平滑化した重力は、全速に戻る瞬間に物理の重力へ入れる。タップで起きた瓶が古い傾きから始まらない（Reduce Motion で光が動かない間も、重力は追い続ける）。
   - Reduce Motion：光は傾きに従わないので、アイドル中の傾きではメインに移らない（揺らしの山だけ）。物理の重力と揺らしの反応は従来どおり。
   - スレッドの安全：背景で触るのは `JarIdleTiltMonitor`（`OSAllocatedUnfairLock` で守った値型のフィルタ）だけで、scene に触るのはメインだけ。止めた後や速さを切り替えた後に届いた古いサンプルは、世代の番号で捨てる。1回の待機でメインを起こすのは1回まで。`-strict-concurrency=complete` でビルドしても、新しいコードに警告は出ない。
   - 「学習の粒」にはチュートリアルの粒を含む（オンボーディングの瓶は従来どおり傾きに応える）。計画プレビューの瓶は、自分の瓶として同じ規則で動く。システムの揺らし（`motionBegan`）の予備は従来どおり。
   - 実機では未検証（本作業では実機に導入しない）。Simulator にはモーションのハードウェアが無いので、Debug 専用の合成モーション（`POMOGEM_UI_TEST_MOTION=synthetic`、`SyntheticJarMotionSource`。Core Motion と同じく自分のタイマーのスレッドで起き、指定のキューへ届ける）で確かめた。

**計測（Simulator だけ。A14 では未検証）**

> 以下はすべて Simulator（iPhone 17 Pro、iOS 26.5、Debug、署名なし、`POMOGEM_UI_TEST_SPRITE_STATS=1`）の値です。3つのビルドは同じ時間帯に交互に計測しましたが、ホストの負荷（load average 6〜660）で fps と CPU は大きく変わり、GPU 時間はこの環境では測れません。**この節の予算の前提である A14（iPhone 12 mini）では、どの値も未検証です**（本作業では実機に導入しないため）。

ノード数／描画回数／fps：

| 状態 | main（d513f9e＝main＋Debug の fixture だけ） | ブランチ・バッチ前（9ceabba） | ブランチ・バッチ後（3515a23） |
|---|---|---|---|
| gallery（段の見本15体＋黒い石） | 138／320／21.4 | 180／153／29.5 | 162／54／60.0 |
| home（3.75kg、6体） | 60／110／60.0 | 81／39／60.1 | 81／32／58.6〜60.0 |
| stress（学習系131体＋記念石12＋黒い石36） | 1,021／2,925／2.4〜2.6 | 1,422／1,253／2.4〜2.6 | 1,206／115／60.0 |

アプリの CPU 使用率（落ち着いたあとの10秒の平均）：

| 状態 | main | バッチ前 | バッチ後 |
|---|---|---|---|
| gallery | 1% | 1% | 1〜2% |
| home | 4% | 4% | 4〜5% |
| stress | 97% | 98% | 24〜25%（60fps で起きたままの状態） |

- 描画回数の予算（典型約50、最悪約140）は、バッチ後の値（home 32、stress 115）で満たす。バッチ前のブランチは、stress で予算の約9倍だった。
- stress の fps を2.4から60にしたのは、主に名前検索の修正（`4181b67`）。この修正だけの時点で、stress は1,422／1,225／60.0、gallery は180／153／60.0 だった。main にも同じ呼び出しがあるので、main の stress が遅いのも同じ理由。
- 再確認（証拠の撮り直しのとき。ブランチの HEAD で、3515a23 との差は Debug の fixture だけ）：home 81／32／60.0、gallery 162／54／60.0、stress 1,206／115／60.1 で、「バッチ後」と同じ値だった（ホストの load 69〜145）。

- fps は、落ち着く直前の、動いている間の値（止まると表示は更新されない）。stress の fixture には融合の担い手がなく、128体の上限で3体が待ち続けるので、ずっと起きている（最悪ケースの計測としてはちょうどよい）。
- stress の1フレームのメインスレッド時間（update から描画の登録まで）：中央値3〜5ms、95パーセンタイル4〜9ms（ホストの負荷による）。以前は約400ms（名前検索）。
- 瓶の復元（最初のフレームの前に同期で行う部分。同じビルドで `POMOGEM_UI_TEST_PREBAKE=0` と交互に比べた）：stress 100〜157ms → 42〜49ms（ホストの load 8〜660 で繰り返し）、gallery 28ms → 19ms、home 12ms → 1ms（起動時の先焼きが当たる）。
- アトラス：home 66件・保持画像2.3MB・ページ約832×841px、stress 69件・2.1MB・約787×823px。
- 実機（iPhone 12 mini）での GPU 時間、発熱と電池、起動時の焼き込み時間は、オーナーが計測する（本作業では実機に導入しない）。A14 での確認項目：①Home を開いてから核とベッドが出るまでの時間（どちらも別スレッドで焼く）、②完走のたびの焼き直し（割合の5%刻みが動いたとき）、③アトラスのページ（約2.6〜2.8MB）を差し替えたときの GPU 転送の引っかかり、④瓶の舞台の光（`JarStageArtwork`、1.5倍で約1.7MB の静止画）を重ねたときの合成の負荷、⑤60fps の維持と発熱。
- 再確認（ラウンド5、ブランチの HEAD、Simulator）：home 81／31／60.0、gallery 162／53／60.0、stress 1,206／114／60.0。瓶の舞台の光は SwiftUI の静止画なので SpriteKit の描画回数は増えない（山の光をベッドの奥へ移したので1回減った）。

**アイドル時の描画（省電力と傾きのきらめきの両立）**

| 状態 | 物理 | 描画ループ | モーション |
|---|---|---|---|
| 操作直後（落下・タップ・揺らし） | 動く | 60fps | 全速（毎秒30回、メイン） |
| 落ち着いて3秒後（既存のアイドル停止） | 停止 | 落ち着いた絵を描いて0.25秒後に止まる（表示リンクも止まる） | アイドル（毎秒5回、背景） |
| 意図して傾ける | 停止 | 光が閾値（\|Δtilt\| > 0.015）を超えて動くたびに描く。上限30fps。最後の変化から0.75秒で再び止まる | 全速（最後の変化から0.75秒） |
| 手に持って静止 | 停止 | 止まったまま（センサーのノイズは閾値で吸収） | アイドル |
| 机に置いた | 停止 | 止まったまま | アイドル |
| シート・集中画面・バックグラウンド | ― | 起きていなければ止まったまま | 停止 |
| 低電力モード、`thermalState ≥ .serious` | ― | 自発のきらめきと粒子を止め、上限30fps | ― |

Simulator では、`showsDrawCount`／`showsNodeCount` と、Debug 専用のフレームの記録（`JarFrameProbe`、`tmp/jar-frames.log`。`POMOGEM_UI_TEST_TILT_SWEEP` で傾きを流し、`POMOGEM_UI_TEST_IDLE_TILT_GATE=0`／`POMOGEM_UI_TEST_PREBAKE=0` で以前の動きと比べられる）で各状態を記録します。ラウンド8から、記録の各行に描画ループの状態（`loop=paused/running`、SKView 自身の `isPaused`）とモーションの速さ（`motion=stopped/full/idle`）が入ります。`POMOGEM_UI_TEST_MOTION=synthetic` で合成モーションを使い（`POMOGEM_UI_TEST_TILT_SWEEP` もこの流れで再生する）、`POMOGEM_UI_TEST_IDLE_ENERGY=0` で「描画ループを止めず、モーションも全速のまま」という以前の動きを再現できます。`POMOGEM_UI_TEST_FX_DELAY=<秒>` は、`fusionfx` の落下と `gallery` の撮影の時刻を遅らせ、描画ループが止まった瓶からの着地・融合・撮影を確かめるためのものです。

**止まった瓶の省電力の計測（ラウンド8。Simulator での代わりの指標で、A14 では未検証）**

iPhone 17 Pro と iPhone 12 mini の Simulator（iOS 26.5、Debug、署名なし）で `home` fixture（3.75kg）を開き、瓶が落ち着いてから30秒、`top -pid … -stats cpu,csw,sysmach -l 32 -s 1` で1秒ごとに記録した（最初の2区間は捨て、30区間の平均）。Simulator のプロセスでは top の `idlew` が常に0なので、起床の回数の代わりにコンテキストスイッチ（csw）と Mach 呼び出しの数を見る。ホストの負荷が高い（load average 60〜250）ので、条件を交互に繰り返して測った（17 Pro は各2回、12 mini は各3回）。

| 条件 | 17 Pro：CPU 平均（中央値） | csw／秒 | Mach 呼び出し／秒 | 12 mini：CPU 平均（中央値） | csw／秒 | Mach 呼び出し／秒 |
|---|---|---|---|---|---|---|
| 以前（95c8a14。Simulator にモーションは無い） | 5.0〜6.1%（4.1〜4.2%） | 723〜951 | 1,192〜1,278 | 4.6〜5.1%（3.6%） | 685〜710 | 1,149〜1,179 |
| 今回（モーションなし） | 4.0〜4.3%（2.6〜2.7%） | 539〜604 | 292〜309 | 4.1%（2.8%） | 636 | 317 |
| 実機の以前の動きの再現（今回のビルドに合成モーションと `POMOGEM_UI_TEST_IDLE_ENERGY=0`：描画ループは回り続け、モーションは全速・メイン） | 5.5〜5.6%（4.1〜4.7%） | 956〜984 | 1,276〜1,380 | 5.1〜5.5%（3.7〜4.1%） | 815〜927 | 1,225〜1,321 |
| 今回（合成モーション。落ち着いた瓶は毎秒5回・背景） | 4.2〜4.3%（2.6〜3.0%） | 569〜586 | 291〜292 | 4.2〜4.4%（2.8〜3.0%） | 597〜681 | 292〜312 |

- 実機に近い比較（下の2行）で、Mach 呼び出しは約77〜78%、csw は約27〜40%、CPU の平均は約20〜23%（中央値は約25〜35%）下がった。描画ループを止めた効果（上の2行）は、主に Mach 呼び出しが約75%減る形で表れる。
- 12 mini の「今回（合成モーション）」の1回目は、CPU が2秒ごとに2%と7%を行き来し、平均5.2%（中央値6.3%）だった（Mach 呼び出しは370で、表示リンクは止まっている）。同じ条件をフレームの記録つきで45秒回すと、瓶は9秒目から一度も起きなかったので、瓶の外の周期的な処理とみて表からは外した（2回目・3回目が表の値）。
- 残り（CPU の中央値約2.6%）を `sample` で見ると、メインスレッドで動いているのは HomeView の body の作り直しと Screen Time の監視で、瓶の側（SpriteKit の表示リンク、Core Motion）は出てこない。これは並行セッションの持ち分（fix-home、#33）。実機での比較は、fix-home を含む main を基準にする約束（PR #30 の計測手順）。
- 動きの確認（録画と記録）：描画ループが止まってから落とした `fusionfx` の10粒目は、口から落ち、着地し、融合し、瓶が育つまで欠けずに描かれる。合成の傾き（±0.42）では、止まっていた瓶が光を動かし（毎秒15〜23回）、傾きが終わると1秒以内に止まってアイドルに戻る。Reduce Motion では同じ傾きで描画ループもモーションも起きない。タップで起き、約5秒で再び止まる。メニューのシートを開くとモーションが止まり、閉じると戻る。前面に戻ると描き直してから再び止まる。止まった瓶で撮った共有の画像は画面と同じ。

### 7.14 カジノ感を避けるための抑制ルール

1. 結果が決まっているものを、ガチャのように溜めて見せない（開封の間、回転、ルーレットのような数え上げ）。
2. 色相が回り続ける虹、コインの形、金色の山積み、ジャックポット風の音、段階的に盛り上がるファンファーレは使わない。
3. 演出の強さはランダムに変えず、グラムで決める。
4. 「あと1個」を点滅で煽らない。
5. **点滅・閃光する部分**は画面の5%以下、同じ場所の点滅は1秒に1回まで。彩度の高い赤や珊瑚色は点滅させない（[WCAG 2.3.1](https://www.w3.org/WAI/WCAG20/Understanding/three-flashes-or-below-threshold)。これは点滅についての基準）。**静止した明るさ**には別の予算を置く：瓶の内側で L>0.6 の画素は30%以下、L>0.9 は4%以下（ラウンド5の Home 3.75kg：L>0.6 が7.0%、L>0.9 は約1%）。以前の書き方（「明るい部分は5%以下」を静止した見た目にも当てはめる）では、目標画像（L>0.6 が27%）に原理的に届かなかった。
6. 格が上がるのはまとまり粒と核だけで、しかも含む時間で決める。ばらの粒の間に序列を作らない。
7. 黒い石は光らせない（減らしたいアプリの時間に報酬を与えないため）。
8. ストア画像では、「時間で決まる」ことが画面から読み取れるようにする（HUD の重さと次の目標を写す）。

**ラウンド5で直したカジノ連想**（見た目のレビューで指摘された箇所）

- 融合シート：10個のプリズムの菱形と、中央へ伸びるスポークの「輪＋スポーク」がルーレット盤に近かった。スポークを撤去し、進みの弧は白 α0.30 以下にした。周りの10個は Home と同じばらの粒の絵（まだ集まっていない枠は色の抜けた薄い粒）、中央は Home と同じ×10 の絵（グラム段のカット）。
- Overview：×10 の段の色が金（amberLamp）で、メダルやコインに見えた。金をやめ（珊瑚ローズ #D56B82）、結晶の絵は Home と同じ焼き込み（`GemArtworkStone`）にした。
- 時間の核の◇：まだ灯っていない◇を小さく淡くして、等間隔の文字盤に見えないようにした（§7.9）。

**ラウンド6で直したカジノ連想（D26、決定）**

- **黒い石 → 不規則な艶のない黒曜石**（`ScreenTimeObstacleAppearance`）：八角形めいた縁取り・チップのような明るい縁・中央の大きな数字をやめた。輪郭は9〜11角の不揃いな多角形（角度と半径 0.80〜0.98 を石ごとにずらす。物理の円の内側）、色はほぼ黒の冷たい灰（明度0.05〜0.16）、貝殻状の割れ面を3〜5枚だけ淡く明暗づけ、ごく淡い波紋と艶消しの粒子、床側の影。縁は左上だけが瓶の光をわずかに拾う（α0.22 の細いグラデーション）。最も明るい画素でも輝度0.40未満で、光らず、きらめかず、報酬に見えない（テストで固定）。暗いハロー（通常合成）は今までどおり。
- **黒い石の数**：石の下側の面に小さく彫った刻印（6〜7.5pt、白 α0.30＋上側の暗い縁、低コントラスト）。10分1個の石には数字を付けない。正立は保つ。文言は今までと同じ「×1,000」（語彙は並行する作業が見直す予定）。数は VoiceOver（「寄り道の黒い石、10分の石N個分」）と内訳には今までどおり全部出る。
- **まとまり粒の ×N → 銅の刻印札**：首カラーと同じ銅（#F2C4A8 → #D9967A → #B8735A → #8A4E3A）の小さな札に、暗い銅の文字を彫り（下に淡い縁）、上端に明るい面取り、下端に影。黒いカプセルでも、スロットの倍率表示でもない。表示の文言は今のまま（`AggregatePresentation.countLabel`）。文字は7〜9pt で、瓶の倍率に逆らって縮めるので、大きな宝石の上でも大きな数字にならない。札は1枚の焼き込みスプライト（アトラスで本体と一緒に描ける）。

### 7.15 worktree の実装中コードへの調整点

2026-09-24 時点で、ブランチ `claude/gem-brilliance` には v1 の実装（`GemArtwork.swift` ほか）があります。読んだ範囲で、本仕様とのずれは次のとおりです。

| 箇所 | 実装中の内容 | 本仕様 |
|---|---|---|
| `GemCutLadder.rung(aggregateLevel:)` | まとまり粒の段をレベルで選ぶ | 含むグラムの段（A0〜A4、§7.2）で選ぶ。A0 はばらと同じ格 |
| `Constants.Jar.gemTwinkleInterval` 0.45秒、`maximumConcurrentGemTwinkles` 6 | 自発のきらめき | 発生間隔0.6秒以上、同時3個まで、同じ宝石は2.5秒あける。「控えめ」・Reduce Motion・低電力では0 |
| `GemArtwork.drawGem` の面の陰影 | `keyLight`（左上）で面の明暗を焼き込み、「左上の鏡面（本体と一緒に回る）」と「上側の頂点の微光」も焼き込んでいる | 焼き込みは視線方向の光と、回転対称な微光だけ。方向を持つ光は画面固定のリグ（既に `gem.lightRig` がある）に移す（§7.3） |
| `GemArtwork.coreImage(colorHex:level:diameter:)` | 1色を基準に分散パレット（虹）を作る | テーマの割合の扇形＋単色時は色相±8°の揺らぎ＋ガードルだけの光学的な分散光（§7.9） |
| `GemArtworkSpec.variantCount` 4 | ― | 本仕様も4変種で一致 |
| `JarAmbientStage` の床 | 左右2色の光だまり、光の線、十字のきらめき4個 | 本仕様と一致 |

---

## 8. 画面別の変更

### 8.1 Home

**HUD の位置（D5）**：瓶の口の上に、コンパクトな HUD を置きます（目標画像と同じく、瓶の中を核と宝石だけの舞台にするため）。

- 1行目：「3巡」ピル（13pt bold、テーマ色の点）
- 2行目：「積み上げた集中」（11pt）→「3.68kg」（40pt heavy rounded。今は39pt）
- 3行目：「15粒」ピルと「つぎの一里塚 10時間・あと3時間52分」（12pt）を同じ行に畳む
- 今週のかけらは HUD に置かず、完走カードと Overview に置く
- **フォールバック**：瓶の表示高さが320pt を下回る端末（iPhone 12 mini の一部の状態など）と、文字サイズ AX5 のときは、今と同じく瓶の内側に置き、背後に楕円のスクリム（黒 α0.35、ぼかし12）を敷く。GemShowcase fixture で内側案と外側案を並べて撮影し、最終確認する

**瓶の中**：時間の核と軌道（◇）、核の名前（小さく）、宝石の山、その背面のジェムベッド（§7.9.1）、銅の首カラー（刻線）、ガラス v2、底の年輪（v1.3）、床の光。

**瓶の下の1行**：初回は「粒をタップすると、内訳を見られます」。出来事型のきっかけを設定していれば「朝食のあと：英語 25分」。

**その下**：テーマと時間のピッカー、主ボタン（変更なし）。

2.5kg 未満で出ている今のバー（「時間 25分 / 4時間10分」）は、次の名所の1行と、瓶の奥の透明な器（10面が250gごとに灯る）に置き換えます。

状態によって出るもの：区切った直後に今日の粒がひと巡り光る1行、おかえりのバナー、先週のかけら（週の最初の起動で一度）、見逃した節目のチップ（最大1枚）、「重さの旅を読み込みました」（一度）。

### 8.2 集中中（FocusView と Live Activity）

- **FocusView（D9、既定オフ）**
  - 設定「集中中に原石を表示」をオンにした人だけ、完成形の無色の原石を静止表示する。`ringAndTime` では時刻の上に56pt、`ringOnly` と `filledDial` ではリングの中央に80pt、`timeOnly` では出さない。
  - 途中の面の進捗、一時停止中の変化、中断時の消滅アニメーションは、どれも作らない。
  - **同期している `TimerDisplayMode` に新しい case は足さない**（旧バージョンの端末がマージで不正な値と判断するため）。設定は端末ローカルに持つ。
- **Live Activity（D10）**
  - ロック画面と Dynamic Island の展開表示に、静止した無色の原石（sessionID から決まる形）と、システムの進捗リング `ProgressView(timerInterval:)` を出す。テーマ色と生涯の進捗は出さない。`ContentState` と attributes は変えない。compact 表示は今のまま。
  - **stale のときは「時間になりました」だけ**を出す。拡張機能は、アプリがその回を記録するかどうか（時計の検証で `timerDemoted` になる場合など）を知らないため。
  - 「磨き上がり・+250g」は、アプリが記録を保存したあとに Activity を終了させるときの最終表示でだけ出す。既存の ContentState の範囲で表示できない場合は、「+Ng」は出さない。
  - 原石のレンダラは、UIKit と SpriteKit に依存しない SwiftUI として `Shared/` に切り出し、xcodegen で project を作り直す。`AppStore/review-notes.md` の Live Activity の表示項目を更新する。

### 8.3 完走カードと融合シート

- 主役は、この一粒の肖像（160pt、傾けると光る）と「+250g」。
- 次の一里塚・名所への進みを1行。
- 今週のかけらが1つ増える小さな動き（「今週 +1」）。
- 節目を越えた回だけ、名所カードか核の演出（§7.10）。
- **初回完走のときだけ**、「明日もこの時間に？（通知）」の任意の1行（D18）。
- 「詳しく」の中へ、会計の説明（粒は1完走に1つ、核の進みは時間で計算、など）を移す。
- ボタンは、主が「5分休む」、副が「閉じる」。共有チップは5秒後（既存。自動の提案は1日1回までという上限も既存）。
- 融合シートの文言は「10粒ぶんの時間が、ひとつの結晶に。重さはそのまま」（D8）。「ここで休む」は残す。
- **融合シートの色（ラウンド7）**：中央の×10 は、その結晶自身の色の配合（瓶の同じ×10 と同じ色）で描き、周りの10個も配合の割合どおりの色にする（6割が朱色・4割が瑠璃なら、朱色6個と瑠璃4個）。受け取りの記録には主な1色しか残らないので、表示のときに瓶の根の結晶を ID で引く。見つからないとき（すぐ×100 に繰り上がったときなど）は、今までどおり主な1色で描く。
- **完走カードで縮んだ瓶の、核のラベル（ラウンド7）**：縮んだ瓶でも「ジェムベッドと積もった粒の上に置く」規則をそのまま使い、入らないときは次の順に譲る。(1) 全部（名札・進みの1行目・「核まであと…」の2行目）、(2) 2行目を隠す、(3) ラベルを出さない（核は山の後ろに下がる）。ラベルがベッドや粒の後ろに透けて見えることはない（`JarLifetimeCoreLabelFit`）。2行目の高さは表示の有無にかかわらず同じ値を報告するので、表示が行き来しない。
- 融合シートの絵（ラウンド5で実装）：`FusionOrbitStage` の周りの10個は Home と同じばらの粒の焼き込み、中央は Home と同じ×10 の焼き込み（含むグラムの段のカット）。スポークは撤去し、輪と進みの弧は α0.3 以下（§7.14）。Overview の「生涯」のカメラの中央だけは時間の核（v4）を描く。
- 完走カードの表示中は、カードの上端の奥に見える「テーマと時間」の行を、30pt のフェードで隠す（ラウンド5。以前は文字が半分だけ切れて見えた）。
- **時間の核が生まれた回（2.5kg）**には、融合シートと同じ場所に「時間の核が生まれました」の静止画カードを出し、任意の共有ボタンを付ける（v1.2）。

### 8.4 休憩と休憩明け

- 休憩中は、その粒を大きく表示し、傾けると光る。「瓶の粒は、そのまま待っています。」は残す。
- 休憩明けは、主ボタン「ここで区切る」、文字リンク「もう一粒磨く」（D12、D13）。

### 8.5 Overview

今の「いま／結晶／年月」の3つのレンズを、次の3つに作り替えます。

- **いま**：今週のかけら（大きく）、時間の核と軌道、次の一里塚と名所、先週のかけら。
- **旅**
  - 名所を下から上へ並べる。到達済みの名所は標本カード（到達日と、その区間のテーマ色で満ちたシルエット）。次の1つは輪郭だけ、その先は霧。
  - 一里塚の数（◇・星・冠・ゾウ）を核の図で示す。
  - 100時間のあとに、ゾウの輪郭が現れる。
  - 上部の空に、年の星と星座。「はじまりと今」のカード。
- **棚**
  - 月の小瓶を年ごとの段に並べ、年の区切りに真鍮のブックエンドを置く。
  - タップすると月の瓶（Wrapped）が開く。年の段から「一年の重さ」を開ける。

**40年分でも重くしない**

- 月の小瓶と名所の到達日は、月ごとに（月、件数、合計グラム）の指紋でキャッシュし、差分だけ裏で再計算する（ModelActor）。
- 棚は Lazy で描画し、計算が終わった月から順に表示する。初回はスケルトンを出す。
- 受け入れ基準（FortyYear fixture、Simulator）：Overview を初めて開いてから最初の描画まで0.5秒以内、メインスレッドで50ms を超える fetch をしない、全体が埋まるまでの時間を記録する。

結晶の内訳は、瓶のタップと「棚」の中に移します。

**ラウンド5で先に揃えた見た目**：Overview の結晶の絵（`ProgressCrystalGlyph`：まとまり粒・整理階層・成果の星）は、Home の瓶と同じ焼き込み（`GemArtworkStone`：本体＋画面固定の光のリグ）を、含むグラムの段のカットで表示する（成果の星は銅の爪の八角ステップカット）。整理階層の×10 の段の色は金をやめた。生涯のカメラの「時間の核 3.75kg」のラベルは、核に重ねず軌道の下に置く。

**ラウンド7：同じものは同じ色に**

- 生涯のカメラの**時間の核**は、Home と同じテーマの割合の扇形で描く（以前は主な1色だけで、Home で多色の核が Overview では朱色一色だった）。扇形は `JarLifetimeCorePresentation.colorShares`（根の結晶のグラム × 色の配合 ＋ ばらの粒のグラム）の1つの関数で求め、Home と Overview の両方がこれを呼ぶ。Overview は同じ根の結晶（clusters）と、まだまとまっていない記録（currentRecords）を渡す。
- 軌道の**×10 などの結晶**は、その結晶自身の色の配合（`aggregateColors`）と含むグラムで描く（以前は主な1色）。
- 「×N」は Home と同じ銅の刻印の札（D26）にした（以前は濃紺の札）。
- **「いま」のレンズの今週の表示**：分銅のアイコンを、今週の実測グラムを1つの結晶にした Home と同じ絵（`AccumulationWeeklyPolicy.gemSpec`：含むグラムの段のカット、今週のテーマの割合の色）に替えた。下の「標準換算」の数字と、カード全体の読み上げは変えていない。今週の実測がまだないときは、チュートリアルと同じ無色のガラスの粒（「今週は、まだ透明。」）。自己申告の時間は色にも段にも入れない（数字と同じ）。

### 8.6 共有

- GemArtwork の画像を、SpriteKit と SwiftUI の両方の共有経路（`ShareJarGraphic`、`ShareGemShape`、`ShareSessionGem`、`ShareAggregatePebble`、`ShareAchievementGem`）で使い、見た目を割らない（worktree の実装中コードに `GemArtwork.bodyImage` が既にある）。
  - ラウンド3の時点：`ShareSessionGem`（ばら）と `ShareAggregatePebble`（まとまり粒）は実装済みで、融合シートから作る×10 のカードには瓶と同じステップカットが写る。`ShareAchievementGem` は旧描画のまま。
  - ラウンド3の時点では、瓶全体のカード（静止画と GIF）の瓶に核が写らず、瓶の上半分が空いて見えた。
- **時間の核は v1.2 から共有画像に合成する**（§7.9。ラウンド5で実装）。瓶全体のカード（静止画と GIF）には、瓶の舞台の光と核が瓶の背面に写る。GIF は核の呼吸・ガラスを横切る光・宝石の星で、コマごとに目に見えて変わる（隣り合うコマで画素の7〜12%が変わる）。
- ×10 のカードなど、スナップショットを使わない `ShareJarGraphic` の瓶も、Home の瓶の輪郭（`JarScene.jarPath`）と銅の首カラー、明るい内側の光で描く（ラウンド5。以前は首の細い旧い瓶だった）。
- **×10 のカードの結晶（ラウンド7）**：以前は小さな結晶が空の瓶の中ほどに浮いていた。今は結晶1つだけのカードを「その結晶の肖像」とし、瓶の幅の0.42（投稿110pt・ストーリー124pt まで）の大きさで中央に置き、**瓶の床に敷いたジェムベッド**（Home と同じベッドの絵を、その結晶の色の配合で焼いたもの。高さは瓶の15%）に半分ほど沈めて座らせる。結晶は浮き沈みも回転もしない（止まっている宝石）。GIF では光の呼吸と、ガラスを横切る光と、星が動く。生涯の時間の核はこのカードには入れない（核は瓶全体のカードの主役で、×10 のカードは1つの結晶の2.5kg を語るため）。
- 共有カードの結晶は、Home と同じ `GemArtworkStone`（本体＋画面固定の光のリグ）と、同じ銅の刻印の札（「×10」などの文言も Home と同じ）で描く。
- 共有の作成画面のタイトルは、ほぼ不透明なバーの中に表示する（ラウンド5。スクロールした内容がタイトルの下に透けて二重に見えていた）。
- 撮影中は不透明な背景ノードを差し込む。透明な SpriteView で加算した光が消えたり、暗い縁が出たりするのを防ぐため。撮影時は傾き0で、静止した星だけにする。
- 共有カード：v1.2 に「時間の核が生まれました」。v1.3 に名所カード（例：「私の100時間＝米俵1俵」）。v1.5 に月の小瓶、はじまりと今。どれも任意のボタンから共有し、共有しても何ももらえない。
- テーマ名と開始時刻は、既定では載せない。手動記録を含む場合はそう明記する。黒い石は含めない。
- GIF はディザ処理をかける（256色だとハローに縞が出るため）。きらめきは8フレームで一巡する決まった順序にする。

### 8.7 ウィジェット（D19）

- **データを持たない原石ウィジェット**を v1.2 に入れる。今の SF Symbol の代わりに、静止した無色の原石の絵（ホーム画面の小・中）を置き、タップで開始画面へ。アカウントのデータは出さない。ライト表示・Tinted 表示でも濁らないよう、濃紺の不透明なプレートを敷いた専用画像にする。
- 瓶・重さ・目標を表示するウィジェットは採用しません。のぞき見や App Group からのデータ露出が起き、プライバシーの境界が変わるためです。
- **ラウンド9で実装**（`PomoGemWidgets/JarWidgets.swift`、既存の種類 `PomoGemJarWidget` の小・中をそのまま差し替え。置いてあるウィジェットはそのまま新しい絵になる）
  - 原石は**コードで描く**（`Shared/RawStoneArtwork.swift`、SwiftUI の `Canvas`。画像素材を同梱しないので `ASSET_LICENSES.md` に行は足さない。UIKit・SpriteKit・データを使わない）。不揃いな10角のガードルから、4点の割れたテーブルへ上がる16面の粗い結晶で、正多面体（サイコロ・メダルに見える形）は避けた。無色（氷のような白、彩度は平均0.16未満）で、左上のランプから一度だけ照らす（ウィジェットは転がらないので、方向を持つ陰影を描いてよい）。星は2つだけで、どちらも静止。
  - 小：ワードマーク、原石、「集中を始める」。中：左に原石の標本カード（濃紺の不透明なプレート）、右に「今日のひと粒を積もう」と「タップしてアプリを開く」。文言は今までのものをそのまま使い、新しい文言は足していない。
  - フルカラーでは、ウィジェットの濃紺の背景（中はさらにプレート）の上に描くので、明るい壁紙でも濁らない。色合い調整（Tinted）のホーム画面では、システムが背景を描くのでプレートと床の影を描かず（影が淡い円に見えるため）、面の明暗だけで原石を見せる（`widgetRenderingMode`）。
  - タップは `pomogem://start`（`StartFocusLink.url`＝`AppLinks.startFocus`）。アプリは Home（開始画面）を前面にするだけで、タイマーは始めず、開いているシートも閉じない（`AppRouter.openStartLink`）。以前は URL を持たず、最後に開いていた画面で開いていた。ロック画面の小さなウィジェットも同じ URL を開く。
  - 拡張機能に足したソースは `Shared/RawStoneArtwork.swift` と `Shared/StartFocusLink.swift` の2つだけ（`project.yml` の `PomoGemWidgets` の `sources`。xcodegen で作り直した）。エンタイトルメントは空のまま（App Group も iCloud も持たない）で、ウィジェットはアプリの記録を読めない。テストで固定した（`testWidgetExtensionStaysDataFree`）。

### 8.8 オンボーディング

- 2枚目：「1分＝10g。25分で1粒（250g）。集中した時間が、重さになります。」
- 最後の画面：空の瓶、瓶の奥の無色透明の器、次に磨く無色の原石。
- 名所の全体やゾウは見せない。予告するのは「最初の一粒」だけ。

### 8.9 設定と積み上がり計画

- 設定に追加するもの（すべて端末ローカル）：「演出の強さ（標準／控えめ）」「集中中に原石を表示」（既定オフ）「Home に次の目標を表示」「きっかけ」「週の目安」。
  - 「演出の強さ」はラウンド9で実装：「集中」の節（タイマーの表示・向き・既定の時間）のすぐ下に「瓶の表示」の節を置き、分段の選択（標準／控えめ）と、選んだ値の説明の1行、フッター（「控えめにしても、粒の重さや数、融合、カットは変わりません。…この設定はこのiPhoneだけに保存され、iCloudでは同期しません。」）を出す（`JarEffectsSettingsSection`）。文字列は `Settings` の表に、コメントつきで置いた（l10n の規約）。
- 積み上がり計画：実績とつなぎ、選んだペースで次の名所が何月ごろになるかを表示する（Home には出さない）。地平線にシロナガスクジラ（約150t）の輪郭を置くのは、この画面だけ。

---

## 9. データと実装

### 9.1 既存データからの導出

すべて、既存の同期データから各端末で毎回導きます。**「到達した」という事実は保存しません。**

| 表示 | 入力 | 計算 | コスト |
|---|---|---|---|
| 一里塚（灯った◇・星・冠・ゾウ、区間の進み） | 生涯グラム（root＋loose、`HomeProjectionSupport.swift:988-1005`。HUD と同じ値） | 生涯分数＝g÷10。◇＝floor(分÷600) mod 10、星＝floor(分÷6,000) mod 10、冠＝floor(分÷60,000) mod 10、ゾウ＝floor(分÷600,000) | O(1) |
| 名所（到達・次・区間の進み） | 同上 | 新しい純関数 `WeightLandmarkLadder.state(grams:)`（静的な表と、1万時間ごとの群れ） | O(1) |
| 核の段、首カラーの刻線 | 同上 | 既存の `EffortProgressPolicy`／`majorMilestoneState` | O(1) |
| 巡、年輪 | 同上 | floor(g ÷ 2,500) を十進に分ける | O(1) |
| まとまり粒の格（A0〜A4） | AggregatePebble の grams | floor(log10(g ÷ 250g)) を段に割り当て | 体ごとに O(1) |
| 今週のかけら | `completionMetrics` の今週の実測グラム | floor(g ÷ 250)。`WeeklyProgressPolicy` の入力を回数から250g単位に変える | 既存 |
| 粒の見た目 | StudySession のテーマ色・source・id | UUID の FNV ハッシュから4変種とグリントの位置（`makeStonePath` と同じ手法）。長さは大きさ（既存の半径の規則）にだけ効く | 粒ごとに O(1) |
| まとまり粒の見た目 | level・pebbleCount・grams・colorMix・periodStart。ID は端末ごとに違いうるので、含まれる最小の sessionID などから種を作る | 同上 | 体ごとに O(1) |
| 核の扇形の色 | `lifetimeCoreColorHex` の重み（近似。§7.9） | 上位5色＋その他、5%刻み | O(可動体数) |
| 段を越えたかどうか | Reward Receipt の totalStudyGrams、grams、projectionIsLowerBound | 完走の前後で段を比べる（§5.6） | O(1) |
| 名所の到達日 | `AccumulationTimelineRepository` の月別グラムと、該当する月の session を endAt 順に累積 | Overview を開いたときに裏で計算し、月ごとの指紋でキャッシュする | 月単位 |
| 月の小瓶 | 同じリポジトリの月別の件数・グラムと、月内のテーマ別グラム | ModelActor で裏で計算し、月ごとの指紋でキャッシュする | 月1回（差分） |
| 年の星 | 年別のグラム | log10 | 年1回 |
| 粒の内訳チップ | session を id で1件 fetch | ― | 安い |
| おかえり | 最後の完走の endAt（降順、fetchLimit 1） | ― | 安い |
| 到達の保留 | `projectionIsLowerBound`／`isCloudVerificationPending` | 到達を宣言しない | ― |

同期で古い記録があとから届いたら、小瓶・星・到達日は演出なしで静かに更新します。本人が記録を削除した場合は正直に減らします（取り消しの演出は作りません）。

### 9.2 スキーマ変更の要否

**v1.2〜v1.5 に必須のスキーマ変更はありません。** 次のものはどれも変えません。

- CloudKit の同期モデル7つと、端末内モデル4つ
- `PomoGemStorageSnapshot.validateSchema`
- 書き出しの schemaVersion 3
- Reward Receipt の形式
- Live Activity の `ContentState` と attributes
- 同期している Prefs の enum（`TimerDisplayMode` など）

「明日もこの時間に？」は、**既存のリマインダーの項目**（同期している Prefs に既にある）に値を書くだけです。

新しく持つ状態は、すべて端末ローカルです（`AccountScopedLocalState` のキーで UserDefaults に保存し、activity reset で消す）。

- 祝福の基準（祝った最高の名所の番号、一里塚の数、核の段、基準を確定したかどうか）、演出を見た印（週・月・年）
- 一度きりの案内を見たかどうか
- 名所の到達日、月の小瓶、年の星のキャッシュ（月ごとの指紋付き）
- きっかけ（最大3件）、週の目安
- 設定（演出の強さ、原石の表示、次の目標の表示）。ただし演出の強さはラウンド9で、端末の見え方の設定としてアカウントに依存しないキー `jar.effects-intensity` に置いた（§7.6。記録のリセットでも消えない）
- 休憩をスキップした回数（`FocusRestCadenceStore` の拡張。「あと1本」の抑制とガードレール指標に使う）

**やってはいけないこと**

- 到達・解放・祝済みを、同期するカウンタや CloudKit の新しい項目として保存しない。オフラインの2台で壊れることは、レア台帳の反例で分かっている（EA:L603）。
- 同期している Prefs の enum に新しい case を足さない。
- cold Home の経路で全件走査をしない（1.5秒の予算を守る）。

**スキーマ変更になる任意の拡張（D24）**

- きっかけや週の目安を端末間で同期する場合：Prefs に「値＋revision＋mutationID」の群を追加する。`validateSchema`、書き出し、CloudKit 本番への追加配備、実機2台での検証、リリース済み store の fixture が必要になる。
- 月の小瓶に本人がメモを付ける場合：新しい同期モデルが必要になる。

どちらも、まず端末ローカルで出し、要望があれば別のリリースで扱うことを推奨します。

### 9.3 守る不変条件

- 物理体は円のまま、面積∝質量（`PebbleRadiusPolicy`）。倍率を掛けても比例は保つ。
- ノード名
  - worldNode の直下で `pebble.` を付けるのは PebbleNode だけ。新しい子ノードは `gem.*`、瓶は `jar.*`。
  - 既存の子ノード名とアクションキー（`aggregate.aura`＋`aggregate.aura.breath`、`pebble.earlyEffortAura`、`rare.*`、`achievement.mark`／`achievement.markBackdrop`、`obstacle.crack`／`obstacle.roughFacet`）は維持する。
  - 新しい演出は `ambient.*`／`drop.*` の名前にして、Reduce Motion のときの削除と、撮影時の非表示に乗せる。
- Reduce Motion でも物理は同じ。PebbleNode と aura にはアクションを付けない。
- 体数の上限は、学習系128体、黒い石36体、記念石12体。
- Reward Receipt：再起動しても演出を再生しない。
- 数字と記念石のマークは正立、コントラスト比7以上。黒い石は glow 0、加算ハローなし。
- 見た目に効く新しい descriptor の項目（段、変種など）は `hasSamePresentation` に加える。
- ジェムベッド（§7.9.1）は生涯グラムとテーマの割合だけから描き、物理ボディ・容量・融合・余白の計算に入れない。融合や黒い石で1ピクセルも変わらない。

### 9.4 段階リリース計画

工数の目安：S＝数日、M＝1〜2週、L＝3週以上。

どのリリースでも次を行います：年齢区分の質問票への再回答（画像と音声が変わるため）、実画面からのストア画像の撮り直しと hash の更新、EA・メタデータ・`review-notes.md` の更新、IAP の説明との照合。

**v1.2「煌めき」**：知人の要望に応える。価値・閾値・融合の規則は変えない。（合計 L）

| 要素 | 工数 |
|---|---|
| 描画の衛生（アイドル時の描画の規則、cull、ignoresSiblingOrder と z の棚卸し、アトラス、先焼き）。ラウンド3で実装済み（§7.13） | S〜M |
| 止まった瓶の省電力（描画ループを止める、モーションを毎秒5回・背景に間引く、Home が見えないときや学習の粒が無いときは止める）。ラウンド8で実装済み（§7.13） | S |
| GemShowcase fixture（Debug かつ Simulator 専用。既にコミット済みの fixture を拡張）と、最悪ケースの fixture | S |
| GemArtwork（段ごとのカット、方向を持たない焼き込み、画面固定のリグ、キャッシュ） | L |
| まとまり粒の格をグラム段で決める（カット・ハロー・グリント・半径・主役の選び方・融合演出） | M |
| 共有ハロー・静止した星・接地影、傾きのきらめき、自発のきらめき（上限つき） | M |
| ガラス v2、銅の首カラー（刻線は既存の到達痕を移す）。ラウンド5でリム・壁の鏡面・コースティクス・首カラーを明るくした | M |
| 床の光、玉ボケの見た目。ラウンド5で瓶の舞台の光（`JarStageArtwork`：床の光だまりと映り込み、輪郭の光、瓶の内側の光と静止した玉ボケ）に置き換え | S |
| 核 v3（放射状のブリリアント、静止画キャッシュ、テーマの割合の扇形、透明な器、分散光、大きさ、名前だけのラベル、実測した HUD の下に軌道を置く配置）。ラウンド2で実装済み。ラウンド5で v4（色相順の色の場、広い白熱の中心、左右のローブ、灯っていない◇を淡く、帯が足りないときに石が譲る、透明な水晶の器、焼き込みをメインスレッドの外へ）に更新 | M |
| ジェムベッド（「積み上がりの光」の結晶化、§7.9.1、D25）。ラウンド2で実装済み。ラウンド5で「光の層」に描き直し、暫定の投影で下げない・焼き込みをメインスレッドの外へ | S |
| 大きさ（D4、決定）：瓶全体の適応倍率（予算は内側の30%、上限2.4、下限1.0、4%刻みの段と2段の履歴、0.5秒の遷移）。ラウンド6で実装。全 fixture を11回ずつ沈降させて余白15%以上を確認（§7.5） | M |
| 黒い石の黒曜石化と銅の刻印札（D26、決定）。ラウンド6で実装 | S |
| HUD（瓶の口の上へ、巡ピル、フォールバック）、×N の札（ラウンド6で銅の刻印札に、D26）、Pro 月刻印の常時表示（ラウンド9で実装、D21） | M |
| 黒い石の暗いハロー、SpriteKit 側の Reduce Transparency とコントラスト対応 | S |
| 落下中の粒で山の光が瓶全体に広がる不具合（ラウンド3の録画で発見、§7.10）の修正。ラウンド5で修正済み | S |
| 共有経路での GemArtwork の共用（ばらとまとまり粒は実装済み。記念石は未実装）、核と瓶の舞台の光の合成と GIF の動き（ラウンド5で実装）、Home と同じ瓶の輪郭（ラウンド5）、撮影時の不透明な背景、「時間の核が生まれました」カード | M |
| 融合シートと Overview の結晶の絵（`FusionOrbitStage`、`ProgressCrystalGlyph`）を GemArtwork の画風に揃える。ラウンド5で実装（Overview の「いま」の今週の表示は未対応） | S〜M |
| 完走カードの組み替え（肖像が主役、会計の説明は「詳しく」へ、休憩が主ボタン、融合の文言、初回の「明日もこの時間に？」） | M |
| データを持たない原石ウィジェット、演出の強さの設定。ラウンド9で実装（§7.6、§8.7、§8.9） | S |
| glowWidth などの固定値テストを、意図を書いて更新 | M |

**v1.3「重さの旅」**（合計 M〜L）

| 要素 | 工数 |
|---|---|
| `WeightLandmarkLadder`（名所）と一里塚の純関数、ユニットテスト | S |
| HUD の次の目標、50%での切り替え、10倍先の「核まであと」の撤去、◇の一里塚への読み替え、星・冠・ゾウの軌道 | M |
| 祝福の基準（新しい端末・同期・同時通過・見逃し）、「重さの旅を読み込みました」 | M |
| 名所カード（テキストの絵葉書と、簡単なシルエット）と共有カード | M |
| 核の誕生・段の上昇の振り付け、一里塚の演出、ゾウ1頭ぶんのズームアウト | M |
| 今週のかけら（250g、完成形なし、完走カード） | S〜M |
| 巡の年輪 | M |
| 粒のタップで出る内訳チップ、一度きりの案内 | S〜M |
| 「あと1本」（D11 の条件つき） | S |
| 比較物の出典台帳、EA の改訂 | S |

**v1.4「習慣の輪」**（合計 M）

| 要素 | 工数 |
|---|---|
| 休憩明けの「ここで区切る」、区切った後の Home | S |
| if-then のきっかけ（端末内、最大3件、既存のリマインダーへの合流） | M |
| おかえり | S |
| 原石のレンダラ（`Shared/` に切り出し）、集中中の原石（既定オフ）、色が入る遷移 | M |
| Live Activity の原石（静止、stale の文言） | S〜M |

**v1.5「棚と星」**（合計 L）

| 要素 | 工数 |
|---|---|
| 月の小瓶（裏での計算、月ごとの指紋キャッシュ、棚のレンズ、封の演出） | L |
| 年の星と星座、一年の重さ | M |
| 名所の標本（自分の宝石の面で満ちるシルエット） | L（アート） |
| 共有カード（小瓶、はじまりと今） | M |
| テーマ別の生涯グラム（裏での全件走査とキャッシュ）で核の扇形を正確にする | M |
| 核の SpriteKit 化（任意。v1.2 の合成で共有には既に写っている） | M〜L |
| 積み上がり計画と実績の接続 | S〜M |

**v1.6 以降**（任意、判断次第）

| 要素 | 工数 |
|---|---|
| 面ごとの法線テクスチャと共有 SKShader による陰影（§7.3 の将来の改善） | M |
| きっかけと週の目安の同期（スキーマ変更） | L |
| Home に棚を一段だけ出す | M |
| HDR／EDR のきらめきの実験 | M |
| 同意制の最小計測 | L |

### 9.5 テスト

**新しいユニットテスト**

- 一里塚：600分ごとに◇が1つ増える、10個で星に畳まれる、40年 fixture で◇0・星1・冠6・ゾウ14（下限値のときは宣言しない）。
- 名所：単調性、分割しても結果が同じ（1分×N と N分×1 で同じ）、同時通過では最上位だけを祝う。
- 祝福の基準：FortyYear fixture を新しくインストールしたシナリオで、大きな演出が0回、チップが1回。
- まとまり粒の格：「1分×10（100g）」「10分×1」「100分×1」で見た目の格（カット・ハロー・グリント数）が同じ段。25分×10 は A1、25分×100 は A2。
- 長さで格が変わらない：25／60／90分の粒で、面の数と明度の幅が同じ。
- 回転しても光源が回らない：本体テクスチャの輝度の重心が中心から4%以内。`gem.lightRig.zRotation == −node.zRotation`。
- 融合の演出：A0 では閃光・リング・破片のノードが作られない。
- 今週のかけら：250gごとに1つ増え、上限を超えると内側の光だけが深まる。
- 黒い石：glow 0、加算ハローなし、暗いハローは通常合成。
- 最悪ケースの fixture：口の下に15%以上の余白。1回の沈降では±1〜2ポイント揺れるので、複数回の最小値で判定する（§7.5）。ラウンド6からは、全 fixture を Simulator の沈降プローブ（`POMOGEM_UI_TEST_SETTLE_PROBE=10`）で11回ずつ測る。
- 瓶全体の倍率（ラウンド6、`PebbleRadiusPolicyTests`・`GemBrillianceTests`）：面積∝質量が倍率を掛けても保たれる（`testJarScaleKeepsAreaProportionalToMassBetweenGems`）、荷重に対して単調で1〜2.4 を出ない（`testJarScaleFallsMonotonicallyWithLoadAndNeverLeavesItsBounds`）、下限は出荷中の大きさ（`testJarScaleNeverGoesBelowTheShippingSize`）、縮むのは即時・育つのは2段先から・出入りの繰り返しで1回しか変わらない（`testJarScaleHysteresisShrinksAtOnceAndGrowsOnlyPastTwoRungs`）、黒い石は学習の粒を超えない（`testBlackStonesScaleWithTheirOwnCapAndNeverOutgrowStudyGems`）、典型の瓶でばらの粒が幅の0.15〜0.20（`testTypicalLoadsShowLooseGemsAtAFifthToASixthOfTheJar`）、全 fixture で予算内か出荷中の大きさ（`testEveryShowcaseFixtureStaysWithinTheAreaBudgetOrAtTheShippingSize`）。場面の側では、復元で全体が1つの倍率になり融合の要求は拡大前の半径のまま（`testRestoreShowsEveryBodyAtOneJarScaleAndKeepsFusionUnscaled`）、融合で瓶が2段以上育つ（`testFusionLetsTheWholePileGrowBack`）、落ちてくる粒は着地後の倍率で生まれ山は着地を待つ（`testIncomingGemFallsAtTheScaleThePileTakesWhenItLands`）、遷移は終点で止まり揺れ戻さない（`testTransitionsSettleAtTheirTargetWithoutOscillating`）。
- D26（ラウンド6）：黒い石は9〜11角の不揃いな輪郭が物理の円の内側にあり、最も明るい画素でも輝度0.40未満、数は中心より下の小さな低コントラストの刻印（`testBlackStonesAreIrregularMatteObsidianWithASmallEngravedCount`）。×N は銅の小さな札で文言は同じ、倍率を変えても画面上の大きさが変わらない（`testAggregateCountIsASmallEngravedCopperTagBelowTheTable`）。焼いた石は同じ形・大きさで共有し、拡大した瓶では拡大した大きさで焼く（`testBlackStoneBakeIsOneSharedSpritePerShapeAndSize`）。
- 内側の光（ラウンド6）：加算・回転対称（輝度の重心が中心）・淡い色・面より手前、Reduce Transparency と撮影に従い、黒い石には付かない（`testLargeGemsGlowFromWithinWithoutDirectionalLight`）。ジェムベッドは表示倍率の半分で焼く（`testClampedArtworkScaleStillRebakesTheBed` を更新）。
- ジェムベッド（実装済み）：生涯グラムに単調、上限を超えない、飽和でちょうど24段、欠片の色が核の扇形と同じ（`testGemBedFollowsLifetimeGramsMonotoneAndCapped`）。融合と黒い石の前後で変わらず、物理ボディを持たず、粒より奥（`testFusionAndObstaclesDoNotChangeTheGemBed`）。
- 核 v4（実装済み）：20枠の順序と「上位5＋その他」、色の場が色相の順に並び割合どおりの角度を持つこと、離れた色相の継ぎ目が淡いこと、焼いた核の3時が珊瑚で9時が青（珊瑚と青が半分ずつ）、左右のローブの色、単色でも隣り合う面の輝度差、広い白熱の中心、透明な器が淡く色を持たないこと（`testCoreColorsFollowTheThemeShareFan`）。軌道の列が実測の HUD＋8pt より下、ベッドより上、石が譲る範囲と、入らない帯では埋もれとして扱うこと（`testOrbitPlacementClearsTheMeasuredHUDFrame`、`testShortStageKeepsTheOrbitMarkersByShrinkingTheStone`）。ラベルがベッドと静止した粒の6pt 上で止まること（`testCoreLabelsStayAboveTheBedAndTheSettledGems`）。
- 山の光（ラウンド5）：落下中の粒で光の大きさと位置が変わらず、どんな山でも床に座る帯に収まる（`testFallingGemNeverStretchesThePileLight`）。
- ジェムベッド（ラウンド5）：暫定の投影で下げない（`testGemBedHoldsWhileTheProjectionIsProvisional`）、別スレッドで焼き前のベッドを残す（`testGemBedBakesOffTheMainThreadAndKeepsThePreviousBed`）、丸めた倍率で焼き直す（`testClampedArtworkScaleStillRebakesTheBed`）。
- 止まった瓶の省電力（ラウンド8、`JarIdleEnergyTests`）：落ち着いた瓶は落ち着いた絵を描いてから SKView の描画ループそのものを止める（`testRestingJarDrawsItsSettledFrameThenStopsTheSKViewRenderLoop`）。光だけの描き直しでは描画ループが回っても物理は止まったまま（`testLightOnlyRedrawRunsTheRenderLoopButKeepsThePhysicsFrozen`）。着地・中身の入れ替え・タップ・揺らし・VoiceOver の操作は同じターンで描画ループと全速のモーションを戻し（`testLandingWakes…`、`testContentChangesWake…`、`testTapShakeAndVoiceOverActionsWake…`）、閾値を超えた傾き・前面への復帰・撮影・設定の切り替えは物理を起こさずに描き直す（`testTiltAboveTheThreshold…`、`testScenePhaseSnapshotAndSettingsRedraw…`）。Reduce Motion では傾きで光も描画も動かない（`testReduceMotionKeepsTheLightStill…`）。モーションは、起きている間は全速・メイン、落ち着くと毎秒5回・メインの外になり、静止した手の揺れではメインに来ず、意図した傾きで全速と描画に戻る（`testMotionObserverRunsFullWhileAwakeAndIdleOffMainWhileResting`）。揺らしの1つ目の山で全速に戻り、2つ目で瓶が起きる（`testShakePeak…`）。タップで起きた瓶はアイドル中に見た重力から始まる（`testTapAfterAnIdleTilt…`）。止めた後の遅れたサンプルと瓶の要求には応えない（`testStoppedObserverIgnores…`）。Home が見えないとき・学習の粒が無いときは止まる（`testMotionStopsWhenHomeIsHidden…`、`testOnlyStudyGemsKeepTheSensorOn`）。フィルタとモニタは単体でも確かめる（静止した手では起きない、Reduce Motion では揺らしの山だけ、64スレッドから同時に入れても起こすのは1回）。
- 描画の衛生（実装済み、`JarRenderingPerformanceTests`）：毎フレームの名前検索をしない、層が z の帯として交わらない、アトラスと単体テクスチャの画素が同じ、保持する画像の上限（画面に出ている画像は追い出さない、常駐のバイト数は保持量以上。テスト中は自動の作り直しを止める）、先焼き（復元が先焼きを引き継ぎ、どの画像も1回だけ焼く。復元の後にノードがその場で焼いた数が0）、止まった瓶の傾きの閾値（注入した時計で、見送ったサンプルが起きた瞬間に追いつく）。

- 演出の強さ（ラウンド9、`JarEffectsIntensityTests`）：Reduce Motion は控えめを含み、保存は端末の1つのキーで不明な値は標準（`testReduceMotionImpliesSubtleAndThePreferenceIsDeviceLocal`）。標準の値は今までの演出と同じで、控えめはすべての種類を弱める（`testSubtleCalmsEveryKindOfEffect`）。控えめの粒は本体・物理・カットが同じで光だけが変わり、自発のきらめきも傾きのきらめきも無い（`testSubtleGemKeepsItsBodyAndDimsOnlyItsLight`）。切り替えは生きている粒を両方向に照らし直す（`testSwitchingTheSettingRelightsALiveGemBothWays`）。着地の脈は短く星を光らせない（`testSubtleLandingPulseIsShorterAndFlaresNoStar`）。瓶は控えめの間きらめかず、あとから作る粒も設定を受け取る（`testSceneAtSubtleNeverTwinklesAndPassesTheSettingToEveryBody`）。控えめの融合は短く、A2 でも破片を出さない（`testSubtleFusionIsShorterAndThrowsNoShards`）。共有の核の光は控えめで弱まる（`testShareCoreLightsDimAtSubtle`）。設定の切り替えは物理を起こさずに描き直す（`testScenePhaseSnapshotAndSettingsRedrawTheRestingJar` に追加）。
- Pro の月刻印（ラウンド9、`ProMonthEngravingTests`）：札の2行目に月が入り、数の行の位置は変わらず、小さな札のまま（`testProEngravesTheMonthUnderTheCountAndKeepsTheCountWhereItWas`）。Pro でも結晶の見た目は札以外同じ（`testProAddsNoNewLookToTheCrystal`）。瓶は Pro の出入りに両方向で従う（`testTheJarFollowsProOnLiveCrystalsBothWays`）。刻印の月は融合シートの月と同じで、数字と点だけ（`testHallmarkNamesTheFusionSheetsMonth`）。
- 原石ウィジェット（ラウンド9、`RawStoneWidgetTests`）：原石は凸なガードルを16面がちょうど埋める粗い結晶で、左上から照らされる（`testRawStoneIsARoughCrystalLitFromTheUpperLeft`）。無色で、プレートは不透明な濃紺（`testRawStoneArtworkIsColourlessAndItsPlateIsOpaque`）。リンクは Home を前面にするだけ（`testStartLinkOnlyShowsHome`）。拡張機能はエンタイトルメントが空で、記録を読むコードを持たず、`project.yml` の共有ソースは3つだけ（`testWidgetExtensionStaysDataFree`）。

**既存テストの意図を保った更新**

- ラウンド9：`testLargeGemsGlowFromWithinWithoutDirectionalLight` は、内側の光の値を標準（Reduce Motion オフ）で確かめる形にした（Reduce Motion は控えめを含み、内側の光が0.8倍になるため）。Reduce Motion の静止した星の値は定数（`PebbleNode.reducedMotionStarAlpha`）から読むので、α0.42 への変更でもテストの意図は同じ。

- glowWidth の固定値（`GachaTests` :587／:701／:741）は、「standard のレアは quiet より強く光る」「記念石はばらより強く光る」「黒い石は0」という意味を、ハローの強さのプロパティで検証し直す。
- `ProgressPresentationTests` の `AggregatePresentation`（:1269、:1424）は、レベルではなくグラム段に対して単調であることを検証する。
- `orbitSlots` の意味が v1.3 で変わるので、該当テストを更新する。
- `PebbleRadiusPolicyTests` は、倍率を掛けても面積∝質量が保たれることを検証する（ラウンド6で実装）。黒い石の焼き込みを旧シェイプノードと比べるテストは、黒曜石の意匠のテストに置き換えた。

**UI テスト**：`JarInteractionUITests`（タップで跳ねて記録は変わらず、内訳チップが出る）、`DecimalFusionEndToEndUITests`、文字サイズ AX5。

専用の Simulator `PomoGem Gem Brilliance 20260924`（iPhone 17 Pro）と、iPhone 12 mini の Simulator（ラウンド5で作成：`PomoGem GemBrilliance Mini`）で、Reduce Motion のオンとオフ、HUD の内側案と外側案を並べて撮影します。実機には導入しません。

**Simulator での撮影手順（ラウンド3で、ブランチの HEAD に対して実施）**

- すべて Debug・Simulator・メモリ内ストアの UI テスト起動（`POMOGEM_LOCAL_PREVIEW=1`、`POMOGEM_UI_TEST_MODE=1`）。iCloud にはサインインしない。
- 瓶の状態は `POMOGEM_UI_TEST_GEM_SHOWCASE` で選ぶ：`first`（250g）、`home`（×10＋ばら5、3.75kg）、`tiers`（29.25kg）、`heavy`（約251kg）、`veteran`（約2.5t）、`gallery`（段の見本と JarSnapshotter の書き出し）、`stress`、`worstcase`（320pt の瓶、口の下の余白を表示）、`fusionfx`（9粒のあとに10粒目が落ちて×10に融合）。
- 比較用のプレビュー：`POMOGEM_UI_TEST_HUD=outside`（D5）、`POMOGEM_UI_TEST_REDUCE_MOTION=1`、`POMOGEM_UI_TEST_SPRITE_STATS=1`。D4 の `POMOGEM_UI_TEST_GEM_SIZE` はラウンド6で撤去した。余白の測定は `POMOGEM_UI_TEST_SETTLE_PROBE=<回数>`（§7.5）。文字サイズは `simctl ui <端末> content_size` で変える。
- ラウンド6：`fusionfx` は、ばら9粒に×100 を6個と×10 を5個を加えた瓶（倍率が予算で決まる瓶）にし、10→1 の融合で瓶全体が育つところを録画から取り出した（`gem-shots/r3/jar-fusion-moment.png`）。全 fixture を両方の Simulator で撮った（`gem-shots/r3/jar-*.png`、`jar-mini-*.png`）。
- 完走カード・融合シート・共有・Overview は、同じ fixture の上で本物の画面遷移（12秒のデモ集中を完走し、5回目で10粒が×10に融合）を UI テストから操作して撮る。書き出した GIF はアプリの tmp から取り出して、コマ（8枚）を確かめる。
- 動きの証拠：`fusionfx` を `POMOGEM_UI_TEST_FX_FRAMES=0`（アプリ内のコマの書き出しを止め、メインスレッドを止めないようにする）で起動し、`xcrun simctl io <端末> recordVideo` で録画する。着地と融合の開始時刻は映像の画素の変化から求め、そこから0／80／160／320／560／1100ms のコマを取り出す。コマの画素が互いに違うこと（ハッシュ）も確かめる。
- ラウンド5：専用の Simulator「PomoGem GemBrilliance」（iPhone 17 Pro）と、新しく作った「PomoGem GemBrilliance Mini」（iPhone 12 mini、375×812pt）で fixture を撮り直した（`gem-shots/r2/fix-*.png`）。完走カード・落下の途中・融合シート・×10 のカード・共有の作成画面・書き出した GIF・Overview は、scratch のコピーにだけ置いた UI テストで本物の画面遷移を操作して撮った（`fix-drop-a/b/c`、`fix-fusion-celebration`、`fix-share-*`、`fix-overview-*`）。書き出した GIF は tmp から取り出して、隣り合うコマで変わった画素の割合を数えた。比較の一覧は `cmp-home.png`・`cmp-core.png`・`cmp-heavy.png`（ラウンド3の版は `-r3` を付けて残した）。
- ラウンド9：演出の強さは起動引数 `-jar.effects-intensity subtle`（または `standard`）で選んで撮った（UserDefaults の引数ドメイン。設定画面での切り替えも操作して確かめた）。Pro の月刻印は `gallery` に `POMOGEM_UI_TEST_PRO_MONTHS=1` を足して撮る（購入そのものは再現しない）。ウィジェットは専用の Simulator のホーム画面に小・中を置き、フルカラー（明るい壁紙）と色合い調整の両方で撮り、タップで設定画面から Home に戻ることを確かめた（`gem-shots/r3/feat-*.png`）。
- 注意：`fusionfx` の録画には、着地の直後と×10 が生まれる瞬間に、瓶の SpriteKit の領域が一瞬（5〜10ms）灰色一色になるコマが入る。Debug の fixture の画面だけで起きる（アトラスの差し替えを止めても起きる。本物の Home の着地5回と融合1回の録画では0回）。証拠のコマを選ぶときは、このコマを避ける。

---

## 10. 計測と実験

### 10.1 前提

今の Analytics は何も記録しない実装です。そのため、**継続率への効果は主張しません。** 以下はどれも端末の中で既存データから計算でき、外には送りません。

### 10.2 伸ばしたい指標

| 指標 | 計算 | 見たいこと |
|---|---|---|
| 週の完走分数（中央値） | StudySession の週別の seconds | 習慣の量 |
| 活動した週の割合（直近8週） | 完走がある週の数 | 継続 |
| 空白（7日以上）後の再開率と、再開までの日数 | endAt の間隔 | 戻りやすさ（1か月目以降の継続を分ける最大の要因） |
| 2日目に戻った割合 | 初回完走の翌日に完走があるか | 「明日もこの時間に？」の効き目 |
| 2本目がある日の割合 | deviceDayKey ごとの本数 | 「もう一本」が健全に増えているか |
| きっかけの実行率 | 時刻型の通知から60分以内に開始したか（端末内の印） | きっかけが効いているか |
| 休憩を取った割合 | 休憩の開始とスキップ（`FocusRestCadenceStore` を拡張） | 終わりどころが機能しているか |

### 10.3 ガードレール指標（悪化させてはいけないもの）

| 指標 | 悪化したときの対応 |
|---|---|
| 21〜4時の完走の比率 | 上がったら夜間の誘導を見直す |
| 1日4時間を超えた日の割合 | 上がったら区切りを強める |
| 休憩スキップの連続 | 3回以上の連続が増えたら「もう一粒」の出し方を見直す |
| 10分未満の完走の比率 | 分割の兆候（Pro の1分タイマーを含む） |
| 手動記録の比率 | 急に増えたら、名所稼ぎの兆候 |
| 長さの分布の偏り | 特定の長さへの偏りが出ていないか |

### 10.4 端末の中だけで測れる範囲

| 区分 | 項目 |
|---|---|
| 既存データだけで測れる | 週の分数、活動した週、空白後の再開、2日目の再訪、夜間の比率、10分未満の比率、手動の比率、長さの分布、1日の本数 |
| 端末内の印を足せば測れる | きっかけの実行、休憩のスキップ、開始して中断した回数、案内を見たかどうか |
| 測れない（計測の追加が要る） | タイマー以外の画面にいた時間、利用者全体の継続率、群に分けた比較実験 |

### 10.5 進め方

1. Debug ビルド限定の「振り返り指標」画面を作る。オーナーと協力者の端末で、更新前の28日と更新後の28日を、本人の履歴で比べる。本人どうしの比較なので、群分けは要らない。
2. 数値は本人が任意で見せる形にし、インタビューと合わせて判断する。
3. 伸ばしたい指標が改善し、ガードレール指標が悪化しなかった要素だけを残す。
4. 利用者全体で測りたい場合は、同意を得た最小限の計測が必要になる。プライバシーポリシー、App Privacy の回答、PrivacyInfo の改訂を伴う（D23）。

---

## 11. オーナー判断事項

**最優先（v1.2 の前に必要）**：D2・D3・D4（決定）・D5・D8。**v1.2 で合わせて必要**：D14・D17・D18・D19・D21・D22・D25・D26。**v1.3 の前**：D6・D7・D11・D15・D16。

| # | 論点 | 選択肢 | 推奨 | トレードオフ |
|---|---|---|---|---|
| D1 | 「依存性」の定義と EA の改訂 | A：「集中を始める行動への習慣の引力」と定義し、EA に一里塚・名所、週のかけら、融合の意味づけ、見た目の格のグラム基準、演出の強さを追記する／B：今のまま | A。あわせて EA:L232 の誤記（「出し」→「出さず」）を直す | 方針を書き直す手間がかかる。代わりに「急かさないのに戻りたくなる」を製品の軸として言葉にできる |
| D2 | 蓋 | A：付けない／B：銅の首カラーだけ／C：蓋を付け、完走ごとに開閉する | B | C は「しまう」儀式が得られるが、毎回約0.5秒延び、アイコンやブランド文書と食い違う |
| D3 | 大きな多色の核 | A：アプリ内だけで許可する（テーマの割合の扇形、単色でも光学的な分散光と揺らぎ、「虹」とは呼ばない）。アイコンは今のまま／B：アイコンにも核を入れる | A | アプリ内とアイコンの印象に差が出る。B は、29pxでの見分けやすさとカジノ連想の再確認、素材台帳の更新が必要。ラウンド5の v4 は、割合は変えずに色相の順の大きな色の場として塗る（§7.9）。目標画像はさらに淡く白っぽいので、どこまで寄せるかは主観の確認で決める |
| D4 | 粒の大きさ | A：ばら×1.6・まとまり×1.2・記念石×1.15・黒い石×1.0（当初の推奨）／A'：ばらだけ×1.6／B：ばらだけ×1.35（まとまり粒・記念石・黒い石は×1.0）／C：宝石すべて一律×1.35／D：ばら×2／E：瓶全体の適応倍率 | **決定：E（2026-09-25、リードデザイナー）**。s＝clamp(√(内側の30% ÷ A0), 1.0, 2.4)、各体の比率（面積∝質量）はそのまま、黒い石は0.72倍（上限1.6）、4%刻みの段と2段の履歴、着地・融合・復元でだけ0.5秒かけて変わる。全 fixture で11回の沈降の最小余白15%以上を実測（§7.5） | 若い瓶の宝石は目標画像に近い大きさ（幅の約1/5）になり、重い瓶は出荷中の大きさのままなので最悪ケースの余白は変わらない。倍率の変わり目では、全体が0.5秒かけて縮む・育つ。中くらいの瓶（ばら9＋root 数個）では、山が瓶の約半分まで来て、瓶の中の核と重なることがある（HUD を外に出す D5 は保留） |
| D5 | HUD の位置 | A：瓶の口の上（コンパクト）、高さ不足と AX5 では内側にフォールバック／B：今のまま内側 | A | 瓶の中が核と宝石だけの舞台になる。iPhone 12 mini では瓶がやや低くなる。fixture で並べて確認 |
| D6 | Home の残り表示 | A：10倍先の「核まであと」を撤去し、一里塚・名所の1行と50%ルールにする／B：今のまま／C：残りを一切出さない | A（EA:L270-271 と整合） | 近い目標の引力を得る。そのかわり、区間の半分を過ぎたら残りを見せる |
| D7 | 一里塚 | A：10時間ごとに核の軌道の◇が灯り、十進で星・冠・ゾウに畳む／B：一里塚なし（名所と巡だけ） | A | 1日1本でも名前の付いた目標が24日以内に必ず来る。◇の意味が「核の段の進み」から変わるので、テストと説明の更新が要る |
| D8 | 見た目の格と融合の意味づけ | A：まとまり粒のカット・光・半径・融合演出を含むグラムの段で決め、融合の文言を「10粒ぶんの時間が、ひとつの結晶に。重さはそのまま」にする／B：今のままレベル基準、文言は「瓶の整理」 | A | B だと Pro の1分×10で結晶の格を先取りでき、課金で見た目の格を買える形になる。A は短い粒中心の人の結晶が控えめに見える（時間どおり） |
| D9 | 集中中の原石 | A：既定オン（進捗つき）／B：既定オフ。オンにしても完成形の静止、進捗なし、中断時の消滅なし | B | A は25分の間の期待を作れるが、途中の成果物が中断で消えるのは損失の演出に近く、集中中にスマホを見に行く動機にもなる（倫理レビューも B） |
| D10 | Live Activity の原石 | A：静止した無色の原石、stale は「時間になりました」、「+Ng」は記録の保存後だけ／B：今のまま | A | ロック画面での手がかりになる。`review-notes.md` の表示項目の更新が必要 |
| D11 | 「あと1本」 | A：HUD の目盛りの行に事実として出す（21時〜翌4時、休憩スキップ2連続後、1日4時間超、長い休憩の提案直後は出さない）／B：出さない／C：開始ボタンに出す | A | 決まった事実なのでニアミスではない。C は開始ボタンに引力を足すので採らない |
| D12 | 休憩明けの2択 | A：同じ大きさで並べる／B：常に「ここで区切る」が主、「もう一粒」は文字リンク | B | 1日あたりの本数は A のほうが伸びやすいが、4+ で学生が主な利用者のアプリとして、休憩へ向かう誘導を優先する（ICO 基準13） |
| D13 | 区切りの文言 | A：新しいボタンを「ここで区切る」にし、既存の中断「今日はここまで」は据え置く／B：既存の中断を「この回をやめる」に改名し、「今日はここまで」を区切りに使う | A | A は変更が最小（中断の文言は UI テスト約20箇所と監査文書が参照）。B は中断の意味がより正確になるが、改修とテスト更新が要る |
| D14 | 一粒ごとの個体差 | A：UUID から決まる同格の4変種とグリントの位置／B：長さで面の細かさ3段＋UUID／C：なし | A。審査メモと年齢区分の説明に「記録から一つに決まり、価値も希少度もない」と明記する | B は長い粒ほど良く見えて序列になり、休憩を飛ばす誘因と、Screen Time の10分粒が永久に粗い問題を生む |
| D15 | 内訳とヒントの文言 | A：粒をタップすると跳ねて内訳チップが出る。案内は「粒をタップすると、内訳を見られます」（初回だけ）／B：用語を「宝石」に統一する（ストアの文言も含む）／C：長押しで内訳 | A | 目標画像の「宝石」とは用語が違う |
| D16 | 今週のかけら | A：250gごとにかけらが増える、完成形なし、本人の目安があるときだけ弧／B：12面の結晶を250gごとに灯す／C：完走1回で1面（今の EA:L211） | A | B は1本/日の人が毎週「7/12」の欠けた結晶になり、12面が暗黙のノルマになる。C は1分×12回で埋まる分割の誘因がある |
| D17 | 演出の強さ「標準／控えめ」 | A：設定を新設し、自発のきらめきは「標準」のときだけ／B：入れない | A（**ラウンド9で実装**：控えめは自発のきらめき・傾きのきらめき・呼吸なし、ハロー0.7倍、着地と融合を短く。Reduce Motion は控えめを含む。§7.6、§8.9） | レアの quiet モードとは別の定義になる（こちらは端末ローカル） |
| D18 | 「明日もこの時間に？」 | A：初回の完走カードに任意の1行。タップしたときだけ既存のリマインダーに登録／B：出さない | A | 2日目の戻るきっかけを、意欲が最も高い瞬間に本人が選べる。通知の許可ダイアログがこのタイミングで出る |
| D19 | ウィジェット | A：データを持たない原石ウィジェットを v1.2 に／B：今のまま／C：データを表示する | A。C は採用しない（**ラウンド9で A を実装**。コードで描く無色の原石、`pomogem://start` で Home へ。§8.7） | C は最も強い手がかりになるが、アカウントに依存しない方針とプライバシーの境界が変わる |
| D20 | 月の小瓶の蓋 | A：小瓶にだけ蓋を付け、色はその月に最も積んだテーマ／B：蓋のない小瓶／C：誕生石の蓋 | A | ブランドに「Home の瓶は蓋なし、月の小瓶は封をする」という新しい線引きが要る。C は暦で入手時期が決まる収集になるので採用しない |
| D21 | Pro | 月刻印を、まとまり粒のプレートで常に見えるようにする。月の小瓶のラベルは全員無料。Pro 限定の新しい見た目は足さない。見た目の格はグラムで決まり、Pro の1分タイマーで先取りできない | この案のとおり（**ラウンド9で実装**：銅の札の2行目に「2026.9」。約束の文言は変えていない。§7.2） | 月刻印が、ようやく目に見える約束になる。IAP の審査画像と説明の更新が必要 |
| D22 | 目標画像の扱い | 雰囲気の参考にとどめる。出所（生成ツールと規約）を確かめるまでは、画像生成の参照にも使わない | この案のとおり | 実在のテーマ名が写っているので、外には出さない |
| D23 | 計測 | A：Debug 限定の端末内指標だけ／B：同意制の最小計測 | A から始める | B はプライバシーの説明一式の改訂が必要 |
| D24 | きっかけと週の目安の同期 | A：端末ローカル／B：Prefs に追加する（スキーマ変更） | A | 機種変更のときに引き継がれない |
| D25 | ジェムベッド（「積み上がりの光」の結晶化、§7.9.1） | A：物理ボディの背面に、生涯の重さで対数に育つ小さな結晶片の層を敷く（入力は生涯グラムとテーマの割合だけ、上限は瓶の内側の高さの18%、24段）／B：今までの「積み上がりの光」（ぼかした光の帯）のまま／C：A の上限を25〜30%に上げて、目標画像の山に近づける | **決定：A（2026-09-25、リードデザイナー）**。ラウンド6で「光の堆積」に再調整（欠片を小さく、コントラストと星を減らし、半分の解像度で焼いて奥でぼかす。不変条件は同じ。§7.9.1） | A は、融合で瓶が空いて見える問題を体数を増やさずに和らげ、生涯の重さを床の厚みとして見せる（融合でも黒い石でも変わらない）。代わりに、3.75kg では33pt と薄く、目標画像の山（瓶の約25〜30%）には届かない。ラウンド5で、暗い砂利に見えた欠片を淡い光の層に描き直した（明度0.62以上、彩度0.30〜0.45、最大10pt）。本物の粒より暗く小さいが、見分けにくいと感じる人がいれば、さらに彩度を下げる。C は目標画像に近づくが、動く粒の舞台を狭め、「粒ではないのに粒に見える層」の割合が増える。B は変化がなく安全だが、融合のあとの空き感が残る |
| D27 | 色を使わない区別（a11y-05 の瓶の部分、§7.12） | **決定（2026-09-25、リードデザイナー。ラウンド7で実装）**：Differentiate Without Color がオンのとき、テーマごとに12の図形の刻印を粒・結晶・核・共有・融合シート・Overview に入れる。図形は `SubjectPalette` の位置で決め、パレットの外の色は輪で囲む。オフでは見た目を変えない | 決定どおり | テーマの対応表（どの図形がどのテーマか）は、テーマの一覧やテーマの選択に図形を並べると分かりやすい。その画面は並行セッションの持ち分なので、`GemArtwork.themeMarkImage` を用意して渡す |
| D28 | 明るさの順序（pr-33 の瓶の部分、§7.4） | **決定（2026-09-25、リードデザイナー。ラウンド7で実装）**：テーマの色から粒の色への変換で、パレットの OKLab の明るさの順序を保つ。パレットは1本のリスト（`SubjectPalette`）にし、テストはそこから読む | 決定どおり | 以前は色相で明るさが決まっていた（緑・青緑が明るく、青・紫が暗い）。今は赤銅・空色・若草・緑青・青緑の粒が落ち着き、藍・瑠璃・菫・紅藤・朱色・珊瑚・琥珀が明るくなる（本体の L′ の平均は約0.60のまま）。明るさの近い組は色相と刻印で見分ける |
| D26 | カジノ連想の残り（§7.14、ラウンド5の見た目のレビュー） | A：黒い石のまとまりを、八角形の縁取りと中央の「×1,000」をやめた艶のない丸い小石の形にし、まとまり粒の札は「×」を数字の60%に縮める（文言は変えない）／B：札の文言を「10粒」「1千粒」に変える（×を使わない）／C：今のまま | **決定（2026-09-25、リードデザイナー）**：黒い石は不規則な艶のない黒曜石（八角形・明るい縁・中央の大きな数字なし）、数は石の下側の小さな低コントラストの刻印で、VoiceOver と内訳には全部出る。×N は首カラーと同じ銅の小さな刻印札（黒いカプセルでも倍率表示でもない）。表示の文言は今のまま（§7.14） | 最悪ケースの瓶では、黒い八角形の中央の「×1,000」がポーカーチップに、黒いカプセルの「×1万」がスロットの倍率表示に近い。黒い石の形は §7.2 で「変更しない」と決めてあり、札の文言は製品の文言なので、見た目のレビューの提案でもオーナー判断なしには変えない。B は倍率らしさが最も消えるが、画面・VoiceOver・ストア画像・テストの文言が変わる |

---

## 12. やらないこと

- 連続日数（streak）、連続の保護、連続が途切れたことの表示。累積の日数も、見た目や Home の表示には使わない。
- 失効・劣化の仕組み。中断すると割れる・枯れる・曇る・消える演出。
- 集中中に原石が一面ずつ磨かれていく進捗表示。
- 期間限定のもの、暦で入手時期が決まる収集（誕生石12種、季節限定）。
- ランキング、他人との比較、友人機能。
- ランダムな希少度やガチャ。レアの再有効化を、煌びやかさの手段にすること。
- 図鑑の見本盤、未発見のシルエット、達成率、NEW バッジ。
- 長さ・時間帯・曜日・季節で、見た目の格を変えること。
- 粒の基本の輝きを段階的に出し惜しみすること（1粒目を地味な原石にする設計）。
- まとまり粒の格を、粒の個数やレベルだけで決めること（課金で先取りできる形）。
- 場所の5段階（部屋）の全面実装、動物の星座、天秤の儀式。
- 課金で進行・輝き・見た目を買えるようにすること、2つ目の課金商品。
- 共有したときの報酬、自動投稿、テーマ名や開始時刻を既定で載せること。
- 次のタイマーの自動開始、「もう一本」を主ボタンにすること、開始ボタンに「あと1本」を付けること。
- 通知で到達・残り・「あと1本」を知らせること、ログインボーナス、サーバーからの push。
- ウィジェット・Live Activity・Dynamic Island に、生涯の進捗、テーマ色、瓶、重さを出すこと。
- 到達や解放を、同期するカウンタとして保存すること。
- 本体テクスチャに方向を持つ光を焼き込むこと（転がると光源が回って見える）。
- SKEffectNode、CIFilter、常に描き続ける演出。
- 目標画像を、ストア画像や公式素材、画像生成の参照に使うこと。
- UI・宣伝・社内の成功指標で「依存」「脳の報酬系」「熟達の保証」と言うこと。

---

## 付録A　検討した5案と採否

点数は10点満点です。「行動」は継続と引力、「倫理」は引力の大きさを倫理リスクで割ったもの、「実現性」は工学リスクあたりの価値を表します。

| 案 | 行動 | 倫理 | 実現性 | 採った要素 | 採らなかった要素 |
|---|---|---|---|---|---|
| 1 時間の天秤（重さの目盛り） | 6.5 | 8 | 6 | 重さの比較、米俵とゾウ、50%での表示の切り替え、祝福の扱い、週の進捗の質量化 | 天秤の儀式、時間帯で変わるカット、ガラスの左壁の目盛り |
| 2 鉱物図鑑 | 5.5 | 5 | 3.5 | 一粒の内訳（鑑定票）、記念石の爪留め、Pro 月刻印のプレート | カット見本盤、長さや時間帯で変わるカット、6頁の図鑑 |
| 3 研ぎの瓶 | 5 | 6 | 4 | 「量は融合でたたまれるので、成長は質と別の場所に担わせる」という原則、核の進化、融合の意味づけ、「おかえり」の文面、首カラーの刻線 | 1粒目の原石、12段のカット、粒ごとに過去の段を持たせること |
| 4 瓶が育つ世界 | 6 | 6 | 5 | 初日から全開の輝き、銅のカラー、月の小瓶の棚、年の星、見逃した節目の再生 | 場所の5段階、窓の空、誕生石の蓋 |
| 5 習慣ループ | 7.5 | 7 | 7 | 色が入る瞬間、if-then、「区切る」、「おかえり」、Live Activity、ガードレール指標 | 動物の星座、集中中に一面ずつ磨かれる進捗、「今日の最初のひと粒」の独立した演出、数字の数え上げ |

---

## 付録B　レッドチーム指摘と対応

| # | 重大度 | 指摘 | 対応 | 反映箇所 |
|---|---|---|---|---|
| 1 | high | 等比の目盛りだけでは、1年を過ぎると次の目標が年単位で遠のく | 10時間ごとの一里塚を追加し、核の軌道で十進に畳む。HUD は名所まで10時間以内なら名所、それ以外は一里塚を出す。「最大の空白日数」を受け入れ基準にした（1本/日で24日） | §0、§3.2、§5.2〜5.5、D7 |
| 2 | high | 目玉の時間の核が v1.5 まで共有画像に写らない | v1.2 で、スナップショットの背面と SwiftUI の共有経路の両方に核の静止画を合成する。「時間の核が生まれました」カードを v1.2 に前倒し | §7.9、§8.3、§8.6、§9.4 |
| 3 | high | ×1.35 では目標画像に遠く、D4 の理由付けが §1.2 と矛盾 | ばら×1.6を基準にし、×1.35は下限案に。矛盾した理由は撤回。まとまり粒は×1.3ではなく×1.2を推奨（root と黒い石が面積の大半を占め、最悪ケースで溢れる恐れがあるため。fixture で確認して決める）。閾値は内部面積から導き直す。最悪ケースの fixture を新設。**その後の実測（ラウンド1〜3）で、×1.6 は余白1〜2%（ばらだけでも9〜11%）で不合格だったため、推奨をばら×1.35（余白15〜16%）に改訂した**。**ラウンド6で固定倍率をやめ、瓶全体の適応倍率に決定（D4）** | §7.5、D4 |
| 4 | high | 焼き込んだ陰影が物理回転と一緒に回り、光源まで回る | 焼き込みは視線方向の光だけにし、方向を持つ光は画面固定のリグへ。回転対称のテストを追加。法線テクスチャ＋共有 SKShader は将来の改善として残す（赤チームの推奨 (a) は、描画層だけで今すぐ実装できて同じ不変条件を満たす方式に置き換えた） | §7.3、§7.15、§9.5 |
| 5 | high | 集中中の原石が、中断時の損失演出になっている | D9 を B（既定オフ）に変更。表示しても完成形の静止、進捗なし、消滅なし。Live Activity の原石も静止 | §4.1、§8.2、D9、D10 |
| 6 | high | 「今日はここまで」が既存の中断ボタンと同じ文言 | 新しい区切りのボタンを「ここで区切る」にする。既存の中断の文言は据え置き（UI テスト約20箇所が参照し、責めない言い方として定着しているため）。中断の改名は D13 の選択肢として残す（赤チームの推奨とは逆の側を変える理由を明記） | §4.1、§8.4、D13 |
| 7 | high | Pro の1分タイマーで結晶の見た目の格を先取りできる | まとまり粒のカット・光・半径・主役・融合演出を、含むグラムの段で決める。A0（2.5kg 未満）はばらと同じ格で、融合も静か。テスト「1分×10と10分×1と100分×1で同じ段」を追加 | §7.2、§7.5、§7.10、D8、D21 |
| 8 | medium | 多色の核が長いあいだ出てこない。単色の人は単色のまま | 光学的な性質（白い輝きと分散光）と、稼いだ多様性（テーマの割合）を分離。透明な器を初日から美しく描き、単色の核にも揺らぎと分散光 | §7.7、§7.9、D3 |
| 9 | medium | HUD が瓶の内側で、瓶の中が文字で混む | HUD を瓶の口の上に出す案を推奨（高さ不足と AX5 は内側にフォールバック）。核のラベルは名前だけにし、数値は撤去。今週のかけらは HUD から外す | §7.1、§8.1、D5 |
| 10 | medium | 2日目に戻るきっかけが弱い | 初回完走カードに「明日もこの時間に？」（既存のリマインダーを使う）。原石ウィジェットを v1.2 に前倒し。if-then の提案は初回の区切りでも出す | §4.2、§8.3、§8.7、D18、D19 |
| 11 | medium | 名所が核の段・融合と同じ点に重なり、報酬が集中する | 名所を1・2・5系列の時間に置き直し、核の段とずらした。核の段は核の演出だけで祝う | §5.2、§5.3 |
| 12 | medium | D10・D11 で倫理レビューの推奨を覆している | D12 を B に。「あと1本」は開始ボタンから外し HUD の事実表示に限定、夜間は21時から、休憩スキップ2連続で停止 | §4.1、§5.5、D11、D12 |
| 13 | medium | 長さで面の細かさ3段は実質的な序列 | 撤回。UUID の同格4変種だけにし、長さは大きさにだけ効く。テストを追加 | §7.2、D14 |
| 14 | medium | 12面の週の結晶は、軽い人には欠け、重い人にはノルマ | 完成形のない「かけら」の成長に変更。目安は本人が設定したときだけ | §4.2、D16 |
| 15 | medium | shouldRender と傾きのきらめきが両立しない | 傾きのデッドバンド、アイドル中は30fps上限、5秒静止で停止。予算表を状態別に分けた | §7.13 |
| 16 | medium | テクスチャキャッシュの鍵とメモリ予算が矛盾 | UUID は4変種とグリント位置に量子化、向きは回転に任せる。メモリは画面上の組み合わせで見積もる（最悪約40組・約7MB以下）。アトラスは任意で作り直さない | §7.13 |
| 17 | medium | 新しい端末で何年も前の節目が祝われうる | 祝福の基準の規則を明文化（検証済みの射影を初めて観測したら黙って採用、大きな演出は Receipt が下限値でない完走だけ、同期経由はチップ1枚） | §5.6、§9.5 |
| 18 | medium | 核の扇形の重みは粒数重みで、「質量で稼いだ多様性」は不正確 | v1.2〜v1.4 は近似と明記し「テーマの割合（おおよそ）」と呼ぶ。v1.5 で正確なテーマ別グラムに置き換え | §7.9、§9.4 |
| 19 | low | 月の小瓶の初回計算が重い | 月ごとの指紋で差分計算、Lazy 描画、受け入れ基準を追加 | §8.5 |
| 20 | low | Live Activity の「+250g」が実際の記録と食い違いうる | stale は「時間になりました」だけ。「+Ng」は保存後だけ | §8.2、D10 |
| 21 | low | ◇の意味が二つの尺度で混ざる | ◇を一里塚に統一し、灯った◇は核の一部として残る（星・冠・ゾウに畳まれる）。核の段は核の形で示す | §5.2、§7.9 |
| 22 | low | 「1万時間」が熟達の保証と読まれる。24時間だけ比較が無い | 名前はゾウを主、時間を副に。「時間は学力の証明ではありません」を常に添える。24時間は20時間・12kg（比較付き）に置き換え | §5.1、§5.3、§5.7 |

---

## 付録C　数値の検算

| 項目 | 計算 | 値 |
|---|---|---|
| 1分の重さ | 定数 | 10g |
| 25分の粒 | 25×10 | 250g |
| 巡 | 2,500g ÷ 10 | 250分＝4時間10分 |
| 一里塚 | 10時間×60×10 | 6kg |
| 100時間 | 6,000分×10 | 60kg（米俵1俵） |
| 1万時間 | 600,000分×10 | 6t（ゾウ1頭ぶん） |
| 1日1本で一里塚まで | 600 ÷ 25 | 24日 |
| 1日1本で100時間まで | 6,000 ÷ 25 | 240日（約8か月） |
| 1日4本で1万時間まで | 600,000 ÷ 100 | 6,000日（約16年5か月） |
| 40年×1日10時間 | 146,100時間 | 87.66t。一里塚は◇0・星1・冠6・ゾウ14 |
| 目標画像の数値 | 3.68kg＝368分＝6時間8分、25kg＝2,500分＝41時間40分、残り2,132分＝35時間32分 | 現行コードの出力と一致（目標画像が現行画面の再レンダリングである根拠） |
| ばら×1.6の直径 | 11.5×1.6×2 | 約37pt（今は23pt） |
| ばら×1.35の直径（改訂した推奨） | 11.5×1.35×2 | 約31pt |
| 最悪ケースの断面積（現行） | ばら9（25〜120分）＋root 18＋記念石12＋黒い石36 の Σπr² | 約6.58万pt²。320pt の瓶の内側（334×298pt＝約9.95万pt²）の66% |
| 同（ばら×1.35／ばら×1.6／当初の推奨） | ばらの断面積は×1.82／×2.56。当初の推奨では root ×1.44、記念石 ×1.32 も加わる | 約7.21万（72%）／7.77万（78%）／9.23万pt²（93%） |
| ジェムベッドの上限 | 瓶の内側の高さ398pt×18% | 約72pt（24段） |
| 適応倍率の予算（17 Pro の Home の瓶） | 306×398pt×0.30 | 約3.65万pt²。A0 が約6,340pt² を超えるまで s＝2.4 |
| 適応倍率の上限での25分の粒 | 11.5×2.4×2 | 約55pt（306pt の0.18、279pt の0.20） |
