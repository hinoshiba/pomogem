# Proタイマーの分・秒指定と互換性

更新日: 2026-09-13。開発中の追加機能を説明する文書です。提出済みのビルドに含まれることや、
本追加の実機・画面・iCloud同期試験が完了したことを示しません。

Homeと設定で、Proの集中時間を数値入力またはスクロールで指定します。範囲は1分0秒〜360分0秒
（60〜21,600秒）です。入力途中の空欄・範囲外・整数オーバーフローは確定できず、閉じるだけでは
設定を保存しません。数値入力の360分1秒は拒否し、ホイールで360分を選ぶ操作は秒も0へ移します。
無料の時間は従来どおり正確に25分・45分・60分・90分です。完走の質量も従来どおり
`floor(秒数 / 60) × 10g`で、例えば1分59秒は10gになります。

## タイマーの保存形式

`PomodoroDuration.custom(minutes:)`の意味とCodable形式を維持します。
`{"custom":{"minutes":40}}`は引き続き40分です。整数分の新しい指定も旧ケースへ正規化し、
端数秒だけ追加ケース`{"customSeconds":{"totalSeconds":2430}}`で40分30秒を保存します。
旧データの数値を秒として読み替えたり、端数秒タイマーを丸めた旧形式へ偽装したりしません。

実行中・一時停止・未確定の完了では、既存の絶対終了日時、残り秒数、セッションIDと活動世代を
引き継ぎます。iCloudのタイマーpayloadは既存のBytes属性内に保存します。別端末の稼働時間を
実測証拠として引き継がない既存の方針も維持します。

## 前回の設定と旧版の端末

Prefsには任意の`preferredFocusSeconds`と`preferredFocusSecondsMutationID`を追加します。
秒値は既存の分設定の同じmutation IDに結び付いている場合だけ採用し、同じ分の値と整合することも
確認します。旧版が分設定を新しく変更した場合、残っていた秒値はその新しい設定に結び付きません。
旧版が同じ変更の分とスタンプだけを複製した場合は、同じ変更に対応する有効な秒値を保持します。
同じスタンプに矛盾する複数の秒値がある場合は設定の更新を止めます。

**端数秒の設定・タイマーを端末間で使う前に、すべての端末を対応版へ更新してください。**
旧版は設定の分部分だけを表示・使用します。例えば40分30秒の設定は旧版では40分になります。
さらに旧版のCodableは新しいタイマーケースを読めず、既存の復旧走査ではそのpayloadを読み飛ばすため、
端数秒タイマーの引き継ぎ・検出・同時開始の抑止は保証できません。旧版が既存の記録を保持することと、
新しいタイマーを利用できることは別です。更新後も通信断中の全端末排他を保証する機能ではありません。

Prefsの追加属性にはCloudKit Production schemaへの反映が別途必要です。タイマーpayloadが既存の
Bytes属性に収まることだけを理由に、設定の同期まで準備済みとは扱いません。

## 検証状況

2026-09-13の中央単体試験は1,260件中1,254件成功、6件は別条件で実行するためスキップ、失敗0件でした。
次の新規テストと既存の保存・同期・書き出し回帰を含みます。旧SQLiteの移行2ケースも成功しました。画面・実機・CloudKitの追加検証は継続中です。

- `PomodoroSecondPrecisionTests`: 旧JSON、整数分の旧decoder互換、境界値、無料／Pro判定、
  ローカル保存・停止・再開とiCloud payloadの復元、異常payloadの拒否。
- `PreferredFocusPrecisionTests`: 秒設定と既存スタンプの関係、旧版の分変更、競合、保存・復元。
- `CustomDurationEditingStateTests`: 入力とホイール、上限、無効入力、取消し、確定の重複防止。
- `FocusActivityDurationLabelTests`: Live Activityの分・秒表示とアプリ表示の一致。

公開前に、中央単体試験、購入状態を含む通常・大きい文字の画面試験、実機での停止・再起動・完走、
追加schemaの確認と実際のiCloud送受信を別々の証拠として記録します。以前の分単位タイマーの成功を、
この追加の検証完了へ読み替えません。

StoreKitを用いる画面試験は、試験専用端末のXcodeローカルストアを初期化してから、
`TEST_RUNNER_POMOGEM_RUN_STOREKIT_TESTS=1`を指定し、
`-only-testing:PomoGemUITests/ProCustomDurationUITests -parallel-testing-enabled NO`で実行します。
実App Store購入やPro判定の上書きは行いません。未初期化の通常実行ではこの試験をスキップし、
その結果をPro画面の合格として扱いません。

旧モデルの移行試験では、変更前の通常アプリを終了して取得したoffline/online各2ストアを、
非公開のコピー上で現在の7 sourceモデルと4 projectionモデルへ開きました。各38行の属性、
日時のビット値、モデル間の関連、元ファイルのハッシュを確認し、追加2属性がNULLのままであること、
2,437秒の新設定を書き込み、コンテナを解放して再読込できることを検証しました。
これはローカル移行の結果で、Production schema公開や端末2台の送受信を示すものではありません。

このMacのiOS 26.5ではCLIからのSKTestSession変更が拒否されたため、Xcodeの
Manage StoreKit Transactionsで専用シミュレータにローカル試験取引を作成する方法も用意しました。
`TEST_RUNNER_POMOGEM_RUN_STOREKIT_TESTS=preseeded`は試験側の取引作成・削除を省略するだけで、
アプリの購入確認を変更しません。終了後にXcodeで当該試験取引を片付けます。

この方法で行った小型iPhoneシミュレータの通常アプリ画面試験は、5件すべて成功しました。
無効入力と上限値、40分30秒の開始・一時停止・プロセス再起動後の同じ残り秒の復元、設定画面で
25分30秒を25分へ戻す操作、最大文字サイズでキーボードを閉じて秒を入力・確定する操作、独立した
分・秒ホイールと入力の相互反映および取消し後の前の設定保持を含みます。画像も目視確認しました。

途中の失敗は保持しています。最大文字サイズでのキーボード上のスワイプと、親Pickerのラベルを
子PickerWheelから検索していたテスト操作を修正しました。再ビルド後に試験購入権利を確認できず
購入画面で中断した実行もあり、XcodeのローカルStoreKit環境を通常起動で再接続した後に上記5件を
再実行して成功しました。中断した実行や別の画面部品だけの試験を、通常アプリの成功に読み替えていません。
