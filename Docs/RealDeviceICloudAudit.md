# Physical-device iCloud lifecycle audit

Status: completed for the scoped single-device cases below. Broader release coverage remains unverified as listed under Scope and limitations. This record distinguishes an actual server round trip from a deterministic replay of import order. A passing account check is not evidence that user records uploaded or restored.

## Test setup

- Date: 2026-09-12
- Device: iPhone 12 mini, iOS 26.3.1 (a)
- App configuration: Release, with the actual CloudKit private database
- Signed CloudKit environment: Production, inspected separately from the build configuration
- Device deployment uses an existing Apple Development identity. This differs from the App Store distribution signature.
- Physical-device tests are opt-in. The ordinary app receives no preview or mock flags.
- Raw device logs, result bundles, screenshots, signing material, account information, and store files remain outside this repository.

The user authorized installing and uninstalling PomoGem and deleting this test account's existing PomoGem data. That authorization does not cover unrelated applications or accounts.

Each identity comparison uses the baseline for its own case. Authorized purges separate the ordinary UI round trip from the historical-reset dataset. Reusing an audit label does not establish that the underlying record is the same; the retained private checks compare the original logical and physical identities.

## Recorded evidence

| Case | Result | What this establishes |
| --- | --- | --- |
| Install and launch the Release app | Passed | The app installs on the physical device and reaches the real storage choice. |
| Read the real Production private CloudKit database | Passed | Account identity was stable. The later read independently observed the original theme and preferences in one readable custom zone. The default zone was explicitly skipped; this is not a claim that every zone was empty. |
| Seed through the ordinary UI | Passed after clean installation and authorized server purge | The real storage disclosure, onboarding, unique theme, one 30-minute/300g manual session, and nondefault keep-screen-awake=false were verified. The initial attempt correctly refused the previously initialized installation. |
| Reset on a fresh replica, then deliver older server history | Reproduced with production code on the physical host | A deterministic in-memory import-order regression displayed old records and physically removed a newly created completion. This is not yet a live CloudKit delivery reproduction. |
| Record or start a timer before initial reset-history hydration | Reproduced with production code on the physical host | Three deterministic cases fail before the preflight fix: a manual record with no known epoch, a manual record with incomplete older history, and a running timer plus its claim. These are not live delivery reproductions. |
| Reopen an existing physical CloudKit store | Original failure reproduced, in-place correction passed | The actual framework-created `<stem>_ckAssets/` directory was rejected by the inventory. The matching selection, successful-mount marker, both stores, one theme, and one preferences row were retained. The same two rows were observed on the server. The companion `.<stem>_SUPPORT/` is now included in exact inventory and cleanup coverage. Upgrading in place reopened Home and completed the live iCloud check; independent before/after SQLite inspection retained both original row IDs without app deletion. |
| Physical Release regression suite | 35 passed | Nine reset, twelve asynchronous preflight, four import-order, and ten filesystem-layout cases pass on the phone. These deterministic fixtures are distinguished from live server import. |
| Delete existing PomoGem synchronized private data | Passed | The fixed synchronized container acknowledged deletion of its one custom zone; a second server enumeration confirmed no custom zones remained with the same account identity. This uses the public CloudKit zone-deletion API, excludes the default zone and operations container, and does not claim Apple Settings UI coverage. |
| Full Debug unit suite on a fresh simulator | Passed | Latest recorded run: 811 passed, four intentional opt-in skips, zero failures (790 XCTest cases plus 25 Swift Testing cases). Physical UI/server cases remain separate evidence. |
| Independent server upload read | Passed after correcting the evidence decoder | The server returned the unique Subject, one manual StudySession of 1,800 seconds, and Prefs. The first strict reads failed on the actual Core Data archived source dictionary; a bounded secure dictionary decoder now passes against both the real server payload and five format regressions. |
| Cloud cold relaunch and reset containment in the real UI | Passed | The unique theme, 30-minute/300g record, and nondefault setting survive a full process relaunch. The cloud reset is disabled with its reason and leaves the same record totals intact. |
| Running timer across process termination | Passed | A real 25-minute timer resumed with elapsed wall time preserved, progressed again, and its cancellation remained durable across another relaunch. This is process termination, not uninstall. |
| Build 7 Release static analysis and app/Widget isolation | Passed | Generic Simulator Release analysis completed without compiler or analyzer findings. Both binaries report 1.0.1 (7); the forbidden test/preview tokens and real-device harness tokens were absent. |
| Paused timer across process termination | Passed | The saved remaining seconds stayed identical after full process relaunch and while paused, then decreased after explicit resume. Cancellation remained durable across another launch. |
| Uninstall/reinstall and ordinary iCloud restore | Passed | Fresh storage selection restored the exact original Subject and StudySession identities and 30-minute/300g totals without duplication. The original preference physical row remained; each new installation adds its intended writer replica, sharing the logical identity. The saved setting at revision 1 wins over the fresh default at revision 0. |
| Uninstall while a paused timer remains active | Passed | Server evidence confirmed the exact paused session and latest revision before removal. The clean reinstall displayed the explicit iCloud handoff; a new ownership record for the same session retained the identical fractional paused remainder. Resume and cancellation succeeded, and no additional StudySession appeared. |
| Theme deletion on the original synced record | Passed through server read, reinstall, and two cold launches | The named confirmation removed the last active theme and retained the existing record. The first UI helper incorrectly expected a picker in the empty-theme Home state; that helper was corrected. Independent server evidence confirms a Date deletion mark on the exact original Subject record. A fresh installation imported that same Subject identity and tombstone, retained the exact StudySession identity and 30-minute/300g totals, and kept the saved setting. The theme stayed absent through two cold launches. |
| Local-only setup and process relaunch | Passed through the ordinary UI and physical store inspection | The real local storage choice and onboarding reached an empty log before creating one 30-minute/300g manual record and saving keep-screen-awake=false. A full process relaunch retained the theme, record, setting, and local-only disclosure. Only the local source store existed; the theme and session identities differed from the original cloud records. No reset marker or timer was present. |
| Local-only data loss after uninstall | Passed through fresh UI setup and store identity checks | Reinstallation returned to storage selection and onboarding with empty history. Fresh local setup produced a new namespace, new logical and physical theme identities, and a new session identity; the earlier local rows were absent. The repeated audit label belonged to a newly created 30-minute/300g record, with the nondefault setting saved again. No cloud store or reset marker was present. |
| Local-only visible-record reset | Passed through the ordinary UI and another process launch | The enabled reset required its normal confirmation. The log and totals became empty while the theme and keep-screen-awake=false survived another launch. Independent store checks found zero sessions, the same logical and physical Subject row, the original Prefs row plus its new current-epoch replica, and one sequence-0 reset marker. |
| Cloud history after the local-only cases | Passed by an independent live server read | The original deleted Subject record and 1,800-second manual record kept their exact server fingerprints and epoch. Subject and StudySession counts and reset markers were unchanged; no active audit theme appeared on the server. This checks the relevant cloud history after online local-only setup, reinstall, and reset. |
| Historical reset seed through the real SwiftData exporter | Local seed passed; initial server check failed | The opt-in hosted setup saved six source rows: three reset markers with sequences 0/1/2, one theme, one manual session, and Prefs; the session and preferences use epoch 2. This prepares old-version history outside the disabled cloud-reset UI. After the first export hold, the strict server check observed the custom zone but zero records across 32 reads over about 176 seconds. The source was retained; uninstall was deferred until independent upload confirmation. |
| Reopen and independently verify the historical source | Source preservation and real server upload passed | A read-only observation held the reopened container for about 122 seconds, retaining the same six source identities and epochs without test-authored source changes. The later independent server read confirmed exactly three markers, one Subject, one StudySession, and one Prefs; all marker sequences and epochs matched the seed, the same Subject identity was present, and the single 1,800-second manual record used epoch 2. The reason the first hold did not export is not established. |
| Ordinary app restore with real historical reset data | Passed after server acknowledgment and actual uninstall/reinstall | The shipping UI restored the 30-minute/300g record and nondefault setting. Independent inspection matched the original Subject and StudySession logical and physical identities, all three exact marker identities/epochs/sequences, and the session's epoch 2. The original Prefs row remained alongside the new installation's writer in epoch 2, with one logical preferences identity and effective keep-screen-awake=false. No extra timer appeared. This exercises the real preflight and fresh CloudKit import, using the separately documented hosted seed and preserved-source reopen. |
| Create and cancel a timer after historical restore | Passed through the ordinary UI, relaunch, and store checks | A real 25-minute timer progressed across process termination and remained canceled after another cold launch. Its new timer revisions and ownership claim all used epoch 2 and one logical timer identity. The original Subject, manual StudySession, and all historical reset markers remained unchanged. |
| Actual 100% network loss during cloud launch | Passed with separately witnessed network conditioning | The runner first reached online Home, terminated the target, and emitted its readiness marker. Xcode's 100% loss condition was activated before the delayed relaunch. The real app then showed its network/timeout reason and retry action at the protected storage gate, with no Home, onboarding, or replacement local choice. All seven source-model tables retained identical field values across the offline attempt. The condition was stopped immediately afterward and returned to None. |
| Online cold recovery after network restoration | Passed through the real UI and independent store comparison | The app reopened Home and completed its live iCloud connection check with normal networking. All seven source-model tables had identical field values before the offline attempt and after online recovery, including timer and claim payloads. The Subject, manual StudySession, all three reset markers, and preferences baseline remained intact. Xcode's network condition remained None. |

## Release containment

Cancellation of the App Review submission for 1.0.1 (6) was requested after the reset regression reproduced. The UI returned the version item to Ready for Review; final submission cancellation processing must be rechecked. A replacement must not be submitted until the corrected behavior and required device cases have evidence.

The current corrective patch makes the cloud visible-record reset unavailable before it can change markers, preferences, local timers, notifications, or cleanup state. Local-only reset remains available. This containment does not by itself establish safe admission of activity before initial CloudKit hydration.

## Scope and limitations

The scoped single-device cases are complete. They used the development-signed Release build described above; an App Store-distributed binary still requires its own verification.

The first network-loss attempt blocked networking before iOS could authenticate the development-signed runner, so no app test executed. Networking was restored immediately. The later passing case explicitly established online trust before the separately witnessed network interruption; the earlier setup failure is not counted as an application result.

One phone cannot establish two-device concurrency or account-switch behavior. Offline devices that still hold data and later reconnect require separate evidence. A running timer expiring while the app is uninstalled, an unsent local change removed by uninstall, and local-only operation while offline are also not established by the completed cases. App uninstall and an iCloud server purge are different operations.

## CI availability

PR #9 CI did not start: GitHub reported an account payment or Actions spending-limit block. No runner or test step executed. Local test success is recorded separately and is not represented as a passing GitHub CI run.
