name: Tsumiben
options:
  bundleIdPrefix: com.hinoshiba
  developmentLanguage: ja
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: "5.10"
    MARKETING_VERSION: "1.0.0"
    CURRENT_PROJECT_VERSION: "1"
    TARGETED_DEVICE_FAMILY: "1"
    GENERATE_INFOPLIST_FILE: true
    ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
targets:
  Tsumiben:
    type: application
    platform: iOS
    supportedDestinations:
      - iOS
    info:
      path: Tsumiben/Info.plist
      properties:
        CFBundleDisplayName: つみべん
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        NSHumanReadableCopyright: Copyright © 2026 hinoshiba
        LSApplicationCategoryType: public.app-category.education
        UIApplicationSupportsIndirectInputEvents: true
        UILaunchScreen: {}
        UIRequiresFullScreen: true
        UIStatusBarStyle: UIStatusBarStyleLightContent
        UIUserInterfaceStyle: Dark
        NSPhotoLibraryAddUsageDescription: シェアカードを写真に保存します。
        NSMotionUsageDescription: 端末の傾きに合わせて瓶の粒を転がすために使います。
        NSSupportsLiveActivities: true
        NSSupportsLiveActivitiesFrequentUpdates: false
        ITSAppUsesNonExemptEncryption: false
        TSUMIBEN_PRIVACY_POLICY_URL: "https://tumiben.hinoshiba.com/privacy/"
        UIAppFonts:
          - ZenMaruGothic-Black.ttf
        UIBackgroundModes:
          - remote-notification
    sources:
      - path: Tsumiben
        excludes:
          - Resources/Products.storekit
      - path: Shared
    entitlements:
      path: Tsumiben/Tsumiben.entitlements
      properties:
        aps-environment: $(APS_ENVIRONMENT)
        com.apple.developer.icloud-container-identifiers:
          - iCloud.com.hinoshiba.tsumiben
        com.apple.developer.icloud-services:
          - CloudKit
        com.apple.security.application-groups:
          - group.com.hinoshiba.tsumiben
    settings:
      base:
        TARGETED_DEVICE_FAMILY: "1"
        SUPPORTS_MACCATALYST: false
        SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD: false
        SUPPORTS_XR_DESIGNED_FOR_IPHONE_IPAD: false
        CODE_SIGN_STYLE: Automatic
        PRODUCT_BUNDLE_IDENTIFIER: com.hinoshiba.tsumiben
        PRODUCT_NAME: Tsumiben
      configs:
        Debug:
          APS_ENVIRONMENT: development
        Release:
          APS_ENVIRONMENT: production
    dependencies:
      - target: TsumibenWidgets
        embed: true
        destinationFilters:
          - iOS
  TsumibenWidgets:
    type: app-extension
    platform: iOS
    info:
      path: TsumibenWidgets/Info.plist
      properties:
        CFBundleDisplayName: つみべん
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        NSHumanReadableCopyright: Copyright © 2026 hinoshiba
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
        NSSupportsLiveActivities: true
    sources:
      - path: TsumibenWidgets
        excludes:
          - PrivacyInfo.xcprivacy
      - path: Shared
      - path: TsumibenWidgets/PrivacyInfo.xcprivacy
        buildPhase: resources
    entitlements:
      path: TsumibenWidgets/TsumibenWidgets.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.hinoshiba.tsumiben
    settings:
      base:
        TARGETED_DEVICE_FAMILY: "1"
        PRODUCT_BUNDLE_IDENTIFIER: com.hinoshiba.tsumiben.widgets
        PRODUCT_NAME: TsumibenWidgets
        SKIP_INSTALL: true
  TsumibenTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: TsumibenTests
    settings:
      base:
        TARGETED_DEVICE_FAMILY: "1"
        PRODUCT_BUNDLE_IDENTIFIER: com.hinoshiba.tsumiben.tests
        GENERATE_INFOPLIST_FILE: true
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/Tsumiben.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/Tsumiben"
        BUNDLE_LOADER: "$(TEST_HOST)"
    dependencies:
      - target: Tsumiben
  TsumibenUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - path: TsumibenUITests
    settings:
      base:
        TARGETED_DEVICE_FAMILY: "1"
        PRODUCT_BUNDLE_IDENTIFIER: com.hinoshiba.tsumiben.uitests
        GENERATE_INFOPLIST_FILE: true
    dependencies:
      - target: Tsumiben
schemes:
  Tsumiben:
    build:
      targets:
        Tsumiben: all
        TsumibenTests: [test]
        TsumibenUITests: [test]
    run:
      config: Debug
      storeKitConfiguration: Tsumiben/Resources/Products.storekit
    test:
      config: Debug
      targets:
        - TsumibenTests
        - TsumibenUITests
    archive:
      config: Release
